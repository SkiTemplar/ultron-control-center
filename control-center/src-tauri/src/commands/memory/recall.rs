// Recall commands removed (cat10 hygiene 2026-06-19).
// `recall_last_session` and `recall_last_session_global` had no live frontend
// invoke() callers — they appeared only in comments. The inner implementations
// in `crate::recall` are preserved for potential future use.
