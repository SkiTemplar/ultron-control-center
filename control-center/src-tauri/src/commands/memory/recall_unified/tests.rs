// recall_unified/tests.rs — thematic test submodules (split from a single
// 949-line file, cat7.3). Each submodule covers one behavioural area of the
// unified hybrid recall; tests moved verbatim, only `use` paths adjusted.

mod budget;
mod e2e;
mod fusion;
mod governance;
mod redaction_floor;
mod temporal;
