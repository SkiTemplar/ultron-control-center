// ULTRON Control Center 2.0 — Mem0 panel
// Real implementation arrives in the next sub-commit of P1. This stub
// keeps the build green right after the Qdrant demolition.

export function Memory() {
  return (
    <div className="p-8 text-sm text-[var(--color-text-muted)]">
      Mem0 panel — implementacion en curso (P1 sub-commit 3).
    </div>
  );
}

export default Memory;
