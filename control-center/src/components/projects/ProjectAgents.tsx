export default function ProjectAgents(_props: { projectId: string }) {
  return (
    <div className="flex h-full items-center justify-center text-xs text-[var(--color-text-muted)]">
      Agents · pending (sub-commit 7)
    </div>
  );
}
