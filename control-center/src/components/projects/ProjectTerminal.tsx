export default function ProjectTerminal(_props: { projectId: string }) {
  return (
    <div className="flex h-full items-center justify-center text-xs text-[var(--color-text-muted)]">
      Terminal · pending (sub-commit 6)
    </div>
  );
}
