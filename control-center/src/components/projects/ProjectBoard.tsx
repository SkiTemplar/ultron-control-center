export default function ProjectBoard(_props: { projectId: string }) {
  return (
    <div className="flex h-full items-center justify-center text-xs text-[var(--color-text-muted)]">
      Board · pending (sub-commit 4)
    </div>
  );
}
