// Learn section content — guías prácticas para dominar ULTRON.
// Generado por el workflow `learn-content` (inspecciona el sistema real +
// fact-check). Regenerable; editable a mano.

export type LearnSection = {
  topic: string;
  title: string;
  icon: string;
  order: number;
  summary: string;
  markdown: string;
};

export const LEARN_SECTIONS: LearnSection[] = [
  {
    "topic": "modelo-mental",
    "title": "Modelo mental",
    "icon": "🧠",
    "order": 1,
    "summary": "Cómo está montado ULTRON de verdad (CLI que ejecuta vs app que observa, capas, skills/agentes/MCPs, routing lazy) para domarlo en vez de pelearte con él.",
    "markdown": "# Modelo mental de ULTRON\n\nEsto no es un tour de botones. Es el mapa de **cómo está montado tu sistema de verdad** y cómo pensar antes de teclear, para domarlo en vez de pelearte con él.\n\nLo primero, la verdad incómoda que ahorra horas:\n\n> **Claude Code (el CLI) es el cerebro que ejecuta. La app Tauri (`~/.ultron/control-center`) es la consola que observa y dispara.** No son lo mismo y no comparten estado en vivo. Casi todos los líos vienen de confundirlos.\n\n---\n\n## 1. Las dos mitades del sistema\n\n| Mitad | Qué es | Dónde vive | Qué hace |\n|---|---|---|---|\n| **Claude Code CLI** | El agente con el que hablas. Carga `CLAUDE.md`, skills, agentes, hooks, MCPs | Proceso de terminal | Razona, escribe código, delega, recall de memoria |\n| **App Tauri (Control Center)** | GUI Rust + React 19, v2.7.1 | `~/.ultron/control-center` | Kanban, AI Router, Memory Browser, Library, RunBatch, esta sección Learn |\n\nComparten **ficheros** (`~/.ultron/cockpit/*.json`, `brain.db`, los hooks en `~/.ultron/hooks/scripts`), **no un proceso**. La app lee/escribe JSON; el CLI los consume vía hooks. Por eso una decisión que tomas en el chat **no** cambia lo que ve la app hasta que un hook propone el candidate y el backend lo persiste, y viceversa.\n\n### Gotcha nº1 (memorízalo)\n**El AI Router (`ai_router.rs`, función `route(zone_id, prompt)`) NO rutea las llamadas del CLI.** Claude Code habla directo con Anthropic. El router solo actúa cuando *la app Tauri* hace una llamada (los 7 comandos `#[tauri::command]` del módulo: botón Test del AI Router, etc.). Consecuencia práctica: cuando te quedas sin cuota a mitad de conversación, **ningún hook re-enruta el CLI automáticamente**.\n\nY ojo, porque aquí el borrador viejo mentía: **la maquinaria de quota está MUERTA.** `quota-capture.js` fue de-registrado de `settings.json` (commit `cbb2d5c`) y `~/.ultron/cockpit/quota-state.json` ya no existe. No hay `react_to_rate_limit` vivo. El único camino real para cambiar de proveedor/modelo en la misma conversación es:\n- **`/model`** — comando nativo del CLI (cambia el modelo de Claude Code), o\n- la skill **`ecc:model-route`** — para deliberar a qué backend tirar.\n\n---\n\n## 2. Las capas (L0 → L3)\n\nULTRON está organizado en capas. Cuando le pides algo, está decidiendo en cuál entrar (jerarquía declarada en `~/.claude/skills/ultron/SKILL.md`):\n\n```\nL0  META      ultron · skill-creator · consolidate-memory · mcp-builder · update-config\n              (núcleo que orquesta y se mantiene a sí mismo)\n\nL1  PERSONAS  Expertos de dominio que viven .disabled y se INYECTAN on-demand.\n              11 existen en disco: terry-davis · don-claudio · mike-tyson ·\n              jordan-belfort · einstein · novalbos · pana · alfred · tio-gilito ·\n              warren · tolkien. (La lista canónica suma 12 con repo-evaluator/kirkardo.)\n              No son \"personajes\": son cargas de contexto especializado.\n\nL2  PLUGINS   ~20 herramientas técnicas transversales (superpowers:* · ecc:* ·\n              codex:* · feature-dev:* · pr-review-toolkit:* · agent-skills:*).\n              Proceso, no dominio.\n\nL2x EXTERNAL  Codex (gpt-5.5) y Gemini (3.1 pro/flash), read-only, vía /dual y /triple.\n\nL3  KNOWLEDGE 11 dominios curados en ~/.ultron-vault/10_KNOWLEDGE/. Se LEE antes de delegar.\n```\n\nLa regla mental: **L1 aporta *quién* sabe del tema, L2 aporta *cómo* se hace bien el proceso, L3 aporta *qué* sabemos ya.** Una tarea seria suele combinar las tres.\n\n### Ghost refs (no-op silencioso)\n`manolo-lama` y `profesor-fisica` aparecen en tablas viejas pero **NO existen en disco** (purgadas en R11.4). Si las invocas, no pasa nada — Claude Code no lanza error, simplemente no-opera. Antes de delegar a algo dudoso: `ls ~/.claude/skills/<nombre>` o `ls ~/.claude/agents/<nombre>.md`.\n\n### L3 — los 11 dominios reales del vault\n`claude-platform` · `competitive-intel` · `cpp-modern` · `cpp-ue5` · `csharp-unity` · `finance` · `opengl` · `python-modern` · `shell` · `supabase` · `typescript-web`. Si tu tarea cae en uno de estos, ULTRON debería leerlo antes de improvisar.\n\n---\n\n## 3. Skills vs Agentes vs Plugins vs MCPs\n\nEs la confusión más común. La diferencia real:\n\n| Concepto | Qué es técnicamente | Cómo \"entra\" | Ejemplo en tu sistema |\n|---|---|---|---|\n| **Skill** | Un `SKILL.md` (instrucciones + contexto) que se inyecta en TU conversación actual | Tú la invocas (`Skill` tool) o el dispatcher la inyecta solo | `hiper-plans`, `terry-davis`, `skill-creator`, `ui-ux-pro-max` |\n| **Agente** | Un sub-proceso aislado con su propio contexto, lanzado vía `Task` | Lo lanzas explícitamente; trabaja aparte y te devuelve un resultado | `code-reviewer`, `rust-engineer`, `debugger` (77 en `~/.claude/agents/`) |\n| **Plugin** | Un paquete que trae skills + comandos namespaced (`plugin:skill`) | Instalado; sus skills aparecen como `ecc:*`, `superpowers:*`, `codex:*` | ECC, superpowers, codex, feature-dev |\n| **MCP** | Un servidor externo que expone *tools* (no contexto): Gmail, GitHub, Qdrant, Playwright | Tools que el modelo llama directamente | `mcp__qdrant__qdrant-find`, `mcp__github-pat__*`, `mcp__claude_ai_Gmail__*` |\n\nLa distinción operativa que importa:\n- **Skill = contexto en tu hilo** (cuesta tokens, comparte tu conversación).\n- **Agente = trabajo en otro hilo** (no contamina tu contexto, pero no \"ve\" lo que tú ves; arranca limpio).\n- **MCP = una acción concreta** (manda un email, busca en Qdrant, abre un PR).\n\n> Regla práctica: si necesitas **razonar mucho sobre algo que no quieres que llene tu contexto** (leer 40 ficheros, auditar todo el repo) → **agente**. Si necesitas **un experto de dominio aquí mismo** → **skill**. Si necesitas **tocar el mundo exterior** → **MCP**.\n\n### Números reales (verificados en disco, iter-10)\n- **77** agentes activos en `~/.claude/agents/` (+1 `.disabled`: `database-admin`).\n- **11** skills core siempre activas (`keep_active: true` en `~/.ultron/cockpit/skill-lazy/skills-registry.json`): `ultron`, `skill-creator`, `hiper-plans`, `senior-engineer`, `business-strategist`, `gamedev-engineer`, `ui-designer`, `ui-ux-pro-max`, `continuous-learning-v2`, `docx`, `pdf`. Todo lo demás es lazy.\n- **~81** carpetas de skill `.disabled` en `~/.claude/skills/` + cientos de entradas lazy en el registro (incluye espejos de agentes y sub-skills de plugins).\n\n---\n\n## 4. El routing lazy: por qué casi todo está `.disabled`\n\nNo es un bug. Es el diseño. Tener cientos de skills activas a la vez reventaría el contexto cada sesión. Por eso:\n\n- El **núcleo mínimo** (los 11 `keep_active: true`) queda activo.\n- El resto vive `.disabled` y **un dispatcher las inyecta on-demand** según tu prompt.\n\nEl dispatcher real es `~/.ultron/cockpit/skill-lazy/routing-dispatcher.v2.js` (determinista, sin LLM) + `routing-dispatcher.v3.js` (semántico). Corre como hook `UserPromptSubmit` (registrado como `routing-dispatcher` en `~/.ultron/hooks/manifest.json`) y **emite `additionalContext` — sugiere, no fuerza.** Cómo puntúa (literal del código):\n\n```\nW_TRIGGER       = 100   (la skill nombrada explícitamente, p.ej. \"terry\")\nW_STRONG        = 60    (keyword fuerte de dominio: \"ue5\", \"refactoriza\")\nW_CONTEXT       = 25    (señal débil: \".cpp\", \"bug\", \"deploy\")\n\nHIGH_THRESHOLD      = 0.80   (umbral de inyección automática)\nLAZY_SCORE_THRESHOLD= 0.80   (lazy skill se inyecta si >= 0.80)\nPLANNING_LAZY_FLOOR = 0.65   (relajado SOLO para planning)\n```\n\nEl floor de planning (`0.65`) solo aplica a 3 skills: **`hiper-plans`, `superpowers:executing-plans`, `superpowers:dispatching-parallel-agents`** — y solo cuando el prompt tiene intención de planificar (keywords como `escribe un plan`, `planifica`, `ejecuta el plan`, `orquesta`, `paralelo`, `multi-agente`).\n\n**Lo que esto significa para ti:** si tu prompt no da señal fuerte, el dispatcher no inyecta nada y respondes con ULTRON pelado (MEDIUM). Si quieres garantizar que entra un experto, **nómbralo** (eso es `W_TRIGGER=100` → entra seguro). El routing automático es *advisory*: úsalo para lo trivial, **teclea el comando cuando importa.**\n\nTras tocar el routing, los harnesses tienen que quedar verdes:\n```bash\nnode ~/.ultron/cockpit/skill-lazy/_verify_final.js\nnode ~/.ultron/cockpit/skill-lazy/_accuracy_at3.js\n```\n\n---\n\n## 5. Cuándo ULTRON decide solo vs cuándo tecleas TÚ\n\nEsta es la tabla que más te va a ahorrar:\n\n| Situación | Quién decide | Qué haces |\n|---|---|---|\n| Tarea trivial, 1 dominio claro (\"arregla este typo en `.ts`\") | ULTRON solo | Nada. Lo enruta o lo hace inline |\n| Multi-dominio o ambiguo | ULTRON sube a HIGH solo | Lo dejas, pero verifica que eligió bien |\n| Quieres un experto concreto SÍ o SÍ | **Tú** | Nómbralo: \"terry, refactoriza X\" / `Skill: warren` |\n| Quieres un proceso formal (TDD, debug, review) | **Tú** | `superpowers:test-driven-development`, `superpowers:systematic-debugging`, skill `code-review` |\n| Plan de algo grande antes de tocar código | **Tú** | `hiper-plans` o `/high`; el entregable es la línea de corte de scope |\n| Trabajo pesado read-only (auditar todo el repo) | **Tú** | Lanza un **agente** (`Agent(Explore)` / general-purpose), no lo hagas inline |\n| Cambiar de modelo a mitad de sesión (cuota) | **Tú** | `/model` (nativo) o `ecc:model-route`. Nunca esperes auto-switch |\n| Segunda opinión adversarial | **Tú** | `/dual` (Codex), `/triple` (Codex+Gemini), o skill `second-opinion` |\n| N tareas independientes en paralelo | **Tú** | `superpowers:dispatching-parallel-agents` o 1 mensaje con N `Task` calls |\n\nLos **modos** (`/low /high /ultra /thinking /contrast /dual /triple /maxdual /learn`...) **NO son slash commands del CLI** — `~/.claude/commands/` no existe para ellos. Son **patrones de texto que la skill `ultron` detecta** en tu prompt. Tecléalos en la frase normal; no esperes autocompletado. (Excepción: `/model` SÍ es un comando nativo de Claude Code.)\n\n```\n/low       → respuesta corta, 0 delegación, máx 1 tool, ≤50 palabras (typos, factual)\n(default)  → MEDIUM, enruta sin deliberar de más\n/high      → feature dura, bug misterioso, multi-dominio (≥2 personas → HIGH siempre)\n/ultra     → decisión arquitectónica crítica\n/thinking  → apila 3+ enfoques con trade-offs antes de actuar (/nothinking desactiva)\n/contrast  → fuerza alternativas; --blank ignora memoria; --dual mete fase DEVIL vía Codex\n/dual /triple → trae a Codex/Gemini como peers read-only (mini/maxi escalan el modo)\n/learn     → overlay didáctico (bloque NOVALBOS tras cada cambio de código)\n```\n\nRegla de oro de ULTRON: **el modo más bajo que cumpla.** No pidas `/ultra` para un typo.\n\n---\n\n## 6. Memoria: el sistema propio (NO Mem0)\n\nULTRON no usa Mem0 (está muerto, de-registrado, no reintroducir). La memoria es:\n\n- **`brain.db`** (SQLite + FTS5) = fuente de verdad, **escritor único** vía `MemoryService`.\n- **Qdrant nativo** (E5-large, 1024d, en `D:\\Ultron\\qdrant\\`) = recall semántico denso.\n- **Recall híbrido RRF** (sparse FTS5 + denso E5) + re-ranker.\n- Sidecar **`~/.ultron/bin/ultron-memory.exe`**. Subcomandos reales:\n  `resume · orchestrate · recall [--cross|--all-projects] · stats · reindex · catalog · eval [--golden] · eval-full · reconcile · warmup · doctor · candidate · capture · edge · deprecate`.\n\n**La regla de oro que cambia cómo piensas:** los hooks **PROPONEN candidates; NUNCA escriben memoria directa.** El único escritor de `brain.db` es `MemoryService` (el sidecar). Auto-recall estilo Hermes vía hooks en `~/.ultron/hooks/scripts`:\n- `memory-session-resume.js` → `SessionStart` (resume acotado: workflows activos, tareas, decisiones, pinned).\n- `memory-orchestrate.js` → `UserPromptSubmit` (intent → workflow → agentes → memorias relevantes).\n- Captura: `session-end-summary.js`, `subagent-harvest.js`, `posttoolfail-capture.js` → todos proponen candidates pending al inbox gobernado; **nunca auto-promueven.**\n\nComo dueño: la memoria se captura sola, pero tras tocar el Rust de `memory/` hay que **rebuild + redeploy del sidecar** y verificar:\n```bash\n~/.ultron/bin/ultron-memory.exe doctor    # 10 checks de salud\n~/.ultron/bin/ultron-memory.exe eval      # recall@8 (objetivo ~0.917)\n```\nUn binario viejo = \"parece que no se aplicó\".\n\n---\n\n## 7. Cómo pensar ANTES de pedir algo (checklist de 5s)\n\n1. **¿1 dominio o varios?** Uno → deja que ULTRON enrute. Varios → `/high` y nombra a los expertos.\n2. **¿Contexto aquí o trabajo aparte?** Aquí → skill. Aparte/pesado → agente.\n3. **¿Necesito un *proceso* (test, debug, review, plan)?** Sí → invoca el plugin explícito, no confíes en que el routing lo adivine.\n4. **¿Toco el mundo exterior (mail, repo, DB, navegador)?** → es un MCP, asegúrate de que está conectado.\n5. **¿Esto va a durar y debería recordarse?** → es memoria; deja que el hook proponga el candidate o pide guardarlo.\n\nY la disciplina de verificación (Kirkardo), porque un agente no \"ve\" tu GUI ni tu binario:\n\n> **Binario fresco = aplicado.** Verifica en runtime (`doctor`/`eval`/`cargo`/`tsc`/abrir la app), no por claim. Nada cuenta como hecho hasta que está **cableado** (comando en `lib.rs` + UI que lo consume + `git add` de lo nuevo).\n\n---\n\n## 8. Errores mentales que cometerás (y el atajo)\n\n| Síntoma | Causa real | Atajo |\n|---|---|---|\n| \"No se aplicó mi cambio de Rust\" | La app estaba abierta → no relinkó el binario | Cierra ULTRON, `npm run build:local` |\n| \"El sistema no me cambió de modelo al quedarme sin cuota\" | No hay quota machinery viva; el router no toca el CLI | `/model` o `ecc:model-route` |\n| \"Invoqué una skill y no hizo nada\" | Estaba `.disabled` y no la nombraste con fuerza, o es ghost ref | Nómbrala explícita; verifica con `ls ~/.claude/skills/<x>` |\n| \"El agente no sabe lo que hablamos antes\" | Arranca con contexto limpio | Pásale el contexto en el brief del `Task` |\n| \"`/high` no autocompletó\" | No es comando del CLI, es texto que detecta `ultron` | Tecléalo en la frase, sin esperar UI |\n| \"El `.ps1` peta el parser\" | Em-dash / no-ASCII en PowerShell 5.1 | Mantén los `.ps1` en ASCII puro |\n| \"El recall no encuentra algo que sé que guardé\" | Sidecar viejo o índice desincronizado | `ultron-memory.exe reindex` + `doctor` |\n\n---\n\n**En una frase:** ULTRON es un *orquestador con capas* donde el CLI ejecuta y la app observa; las skills traen expertos a tu hilo, los agentes trabajan aparte, los MCPs tocan el mundo, los hooks proponen pero solo `MemoryService` escribe, y casi todo está apagado a propósito para que el routing solo encienda lo que tu prompt justifica. Cuando importa, **no confíes en el auto-routing: nómbralo tú.**"
  },
  {
    "topic": "skills",
    "title": "Skills",
    "icon": "🧩",
    "order": 2,
    "summary": "Como funciona el lazy-routing de skills (v3 sobre v2 + Qdrant), cuando dejar el auto-routing y cuando teclear /skill, y como crear/registrar una skill propia.",
    "markdown": "# Skills — domar el catalogo\n\nTu sistema tiene **188 skills registradas** (`cockpit/skill-lazy/skills-registry.json`, verificado: `node -e` cuenta 188 entradas) pero solo **11 viven activas** todo el tiempo (`keep_active: true`). Las otras 177 estan `.disabled` en disco y el sistema te las **inyecta on-demand** segun lo que escribas. Esta guia es sobre como funciona ese mecanismo de verdad y como exprimirlo — no sobre botones.\n\n---\n\n## 1. El modelo mental: lazy por defecto\n\nUna skill es una carpeta en `~/.claude/skills/<nombre>/` con un `SKILL.md` (frontmatter `name` + `description` + cuerpo). Cargar todas a la vez serian ~200k tokens de contexto muerto. Por eso ULTRON usa **lazy loading**:\n\n- Las skills inactivas tienen la **carpeta renombrada a `<nombre>.disabled`** (lo hace `apply-lazy-skills.ps1`). Claude Code ignora cualquier carpeta que no sea exactamente `<nombre>/SKILL.md`, asi que no las ve.\n- En cada prompt tuyo, el hook **`UserPromptSubmit`** ejecuta el dispatcher (ver seccion 7: el wireado real es **v3**, que envuelve a v2). El script puntua tu prompt contra todas las skills/personas/agentes, y si una gana con confianza alta **lee su `SKILL.md` desde el `.disabled` y lo inyecta** en el contexto bajo el bloque `--- [skill-inyectada: NOMBRE] ---`.\n\nO sea: la skill se \"activa\" en esa respuesta concreta sin tocar disco ni reiniciar nada. La regla de oro del CLAUDE.md es real: **no actives skills en masa**. El nucleo minimo debe quedarse pequeno; el resto entra por routing.\n\n### El nucleo activo (11 skills `keep_active: true`)\n\nEstas estan SIEMPRE cargadas (ids y tokens verificados contra `skills-registry.json`). Son las que no quieres que dependan del azar del routing:\n\n| Skill | Tokens | Para que |\n|-------|--------|----------|\n| `ultron` | 3187 | Orquestador maestro; se activa con \"Ultron\" o tareas multi-dominio/ambiguas |\n| `skill-creator` | 1773 | Crear/editar/validar/empaquetar skills (meta-skill) |\n| `hiper-plans` | 1975 | Planificacion profunda spec-driven (PRD, design doc) |\n| `senior-engineer` | 2427 | Criterio de ingenieria, decisiones tecnicas |\n| `continuous-learning-v2` | 3391 | Sistema de instintos que observa sesiones via hooks |\n| `business-strategist` | 1295 | Preguntas de negocio B2B SaaS (nicho, pricing, GTM) |\n| `gamedev-engineer` | 1768 | Gamedev general |\n| `ui-designer` | 1671 | Diseno UI base |\n| `ui-ux-pro-max` | 6987 | UI/UX nivel pro (67 estilos, 161 paletas, 16 stacks) |\n| `docx` | 5044 | Generar/leer Word |\n| `pdf` | 2125 | Manipular PDF (merge/split/OCR/forms) |\n\n> Nota honesta: ese nucleo pesa ~30k tokens (suma exacta de los 11 = 31443). Si te sobra contexto, `ui-ux-pro-max` (6987) y `docx` (5044) son las candidatas obvias a mover a lazy si rara vez las usas — entre las dos son ~12k, casi el 40% del nucleo.\n\n### El umbral de inyeccion (lo que decide si entra o no)\n\nEl dispatcher puntua con pesos `W_TRIGGER=100`, `W_STRONG=60`, `W_CONTEXT=25` y normaliza a `confidence = score/100` (cap a 1.0):\n\n- **>= 0.80 (HIGH)** -> hint de routing + **inyecta el `SKILL.md`** (si la skill es `lazy_loadable`).\n- **0.50–0.80 (MED)** -> solo te sugiere 1-2 candidatos como hint, **no inyecta**.\n- **< 0.50** -> silencio total.\n\nExcepcion FASE 7 (real, en el codigo): las skills de planning de un allowlist cerrado — `hiper-plans`, `superpowers:executing-plans`, `superpowers:dispatching-parallel-agents` (constante `PLANNING_LAZY_SKILLS`) — bajan el piso a **0.65** (`PLANNING_LAZY_FLOOR`) si tu prompt contiene una keyword de plan explicita: `escribe un plan`, `planifica`, `ejecuta el plan`, `orquesta`, `paralelo`, `multi-agente`, `multiagente` (constante `STRONG_PLANNING_KEYWORDS`). Esas skills ademas saltan el gate `lazy_loadable` del registry.\n\n**Consecuencia practica:** un match flojo (MED) NO te trae la skill — solo te avisa. Si quieres la skill de verdad, o subes la senal (usa su trigger exacto) o la invocas tu mismo.\n\n---\n\n## 2. Las 12 personas L1 (reales, en disco)\n\nLas \"personas\" son skills de Layer 1 con voz/dominio propios. Las 12 que **existen** como carpeta `<nombre>.disabled` en `~/.claude/skills/` (verificadas `ls`):\n\n| Persona | Triggers que la disparan | Dominio |\n|---------|--------------------------|---------|\n| `terry-davis` | \"terry\", \"terry davis\", \"modo terry\" | Debug/refactor/arquitectura, codigo en general |\n| `don-claudio` | \"don claudio\", \"don\", \"modo don\" | UE5/Unreal, Blueprints, GAS, netcode, gamedev AAA |\n| `mike-tyson` | \"mike\", \"mike tyson\", \"modo mike\" | UX, design system, wireframes, accesibilidad |\n| `jordan-belfort` | \"jordan\", \"jordan belfort\", \"modo jordan\" | Monetizacion, pricing, pitch, GTM, B2B |\n| `einstein` | \"einstein\", \"modo einstein\" | Investigacion cientifica, papers, state of the art |\n| `pana` | \"pana\", \"modo pana\", \"cierra el dia\", \"modo manana\" | Briefing diario, email/agenda/Notion/Spotify |\n| `tio-gilito` | \"tio gilito\", \"gilito\" | Finanzas personales, Kutxabank, ahorro, gastos |\n| `warren` | \"warren\" | Inversion, ETF, cartera, analisis fundamental |\n| `tolkien` | \"tolkien\", \"modo tolkien\", \"escribe el libro\", \"siguiente capitulo\" | Escritura de ficcion, arcos narrativos |\n| `novalbos` | \"novalbos\", \"modo aprendizaje\", \"/learn\" | Bajo nivel: Vulkan/CUDA/SIMD/backprop, apuntes |\n| `alfred` | \"alfred\", \"modo alfred\" | Admin del PC: procesos, registro, tareas, archivos |\n| `repo-evaluator` | \"kirkardo\", \"ponme nota\", \"evalua mi codigo\", \"gradeame\" | Corrector estilo profesor (Kirkardo) |\n\n> **Ghosts confirmados (vigilar):** el dispatcher (`PERSONAS[]`, lineas ~241-281 de `routing-dispatcher.v2.js`) tambien lista `profesor-fisica` y `manolo-lama`, pero **NO tienen carpeta en disco** (`ls ~/.claude/skills/profesor-fisica*` y `manolo-lama*` -> \"No such file or directory\"). Pueden ganar el routing y luego fallar la inyeccion silenciosamente: `resolveSkillMdPath` no encuentra el `SKILL.md` y `readWithTimeout` devuelve `null`, asi que cae al hint sin contenido (sin error). Si las quieres usar, hay que crearles el `SKILL.md`; si no, quitarlas del array.\n\nLas personas se disparan limpio por **nombre propio** (trigger fuerte = 100 pts -> confianza 1.0 instantanea, ya supera el 0.80). Es el caso de uso mas fiable del routing: escribe \"Tio Gilito, cuanto llevo gastado este mes\" y entra sola.\n\n---\n\n## 3. Skills tecnicas mas utiles (lazy, entran por routing)\n\nDe las 177 lazy, las que mas rendimiento dan en el dia a dia, con la senal que las dispara (de `PLUGINS[]` en `routing-dispatcher.v2.js`):\n\n| Skill | Senal / trigger | Para que sirve |\n|-------|-----------------|----------------|\n| `superpowers:systematic-debugging` | \"no encuentro el bug\", \"debug sistematico\" | Bug sin causa obvia, metodico |\n| `superpowers:test-driven-development` | \"tdd\", \"test-driven development\" | RED-GREEN-refactor |\n| `superpowers:brainstorming` | \"brainstorming\", \"lluvia de ideas\" | Explorar antes de implementar |\n| `superpowers:executing-plans` | \"ejecuta el plan\", \"execute the plan\" | Ejecutar un plan escrito con checkpoints (piso 0.65) |\n| `superpowers:dispatching-parallel-agents` | \"agentes en paralelo\", \"multi-agente\" | Lanzar varios agentes independientes (piso 0.65) |\n| `superpowers:verification-before-completion` | \"verifica antes de completar\" | No dar por hecho sin evidencia (muy Kirkardo) |\n| `second-opinion` | \"second opinion\", \"codex review\", \"gemini review\" | Segunda opinion externa |\n| `security-scan` | \"security scan\", \"security review\", \"owasp\", \"cve\" | Auditoria de seguridad |\n| `webapp-testing` | \"playwright\", \"webapp testing\", \"e2e\" | Test E2E de la webapp con browser |\n| `git-conflict-resolver` | \"merge conflict\", \"git bisect\", \"interactive rebase\" | Resolver lios de git |\n| `database-migrations` | \"db migration\", \"zero-downtime\", \"alembic\", \"prisma migrate\" | Migraciones de esquema seguras |\n| `mcp-builder` | \"crear mcp\", \"crear servidor mcp\" | Construir un servidor MCP |\n| `ecc:hookify` | \"hookify\", \"crea un hook\" | Generar hooks de settings.json |\n| `consolidate-memory` | \"consolida memoria\", \"ordena .ultron\", \"limpia el index\" | Limpiar/fusionar memorias |\n| `prompt-optimizer` | \"optimiza el prompt\", \"mejora mi prompt\" | Reescribir un prompt |\n| `council` | \"council\", \"convoca el council\" | 4 voces para una decision ambigua |\n| `frontend-design` | \"frontend design\", \"glassmorphism\" | Estilos visuales |\n\nTambien hay un catalogo de **patterns por lenguaje** (`rust-patterns`, `python-patterns`, `postgres-patterns`, `redis-patterns`, `vite-patterns`, `cpp-coding-standards`...) y **testing por lenguaje** (`python-testing`, `rust-testing`, `cpp-testing`). Entran solas cuando mencionas el lenguaje + la intencion, pero ojo: `rust-patterns` se dispara por la frase `\"rust patterns\"` o por `\"borrow checker\"`/`\"lifetimes\"`/`\"tokio\"` (strong), NO por la palabra suelta \"rust\". El context \"rust\" pesa solo 25 (no llega a 0.80 solo). Si quieres el patterns de un lenguaje a tiro fijo, teclealo.\n\n> **Skills vs agentes (matiz real).** El mismo dispatcher rankea ademas **77 agentes** (`AGENTS[]`, p.ej. `rust-engineer`, `security-auditor`, `debugger`). Pero los agentes **NO se inyectan como `SKILL.md`** — solo se te sugieren como hint. El filtro en `fetchLazySkillContent` es explicito: `if (c.kind !== 'plugin' && c.kind !== 'persona') return false;` (los agents viven en `~/.claude/agents/`, no en `skills/`). Para usar un agente lo lanzas tu via la tool Task.\n\n### ECC: indice aparte (y se auto-re-inyecta, no solo explicito)\n\nLas **232 skills** del plugin ECC (`ls ~/.claude/plugins/cache/ecc/ecc/*/skills/*/` -> 232) viven en `~/.claude/plugins/cache/ecc/` y se indexan **por separado** (`matchBestEccSkill`, tokens via `parseEccSkillTokens`: bigrams de description + seccion \"When to Use\"). NO contaminan el ranking principal — solo se consultan **despues** de conocer al ganador, y **solo si tu candidato principal ya es HIGH (>=0.80)**.\n\nFloor de re-inyeccion ECC: **70 raw** normal (`ECC_MATCH_MIN_RAW`), **50 raw** si la skill ECC es de planning (id matchea `/plan|spec|prd|architecture|design/`, constante `ECC_PLANNING_MATCH_MIN_RAW`).\n\n**Esto es observable y real.** Probado en vivo con `\"resuelve el merge conflict del rebase\"`: el dispatcher inyecta `git-conflict-resolver` (100%) Y ademas re-inyecta `ecc:git-workflow` porque el top candidato era HIGH. O sea: cuando tu prompt activa fuerte una skill propia, puedes recibir la ECC equivalente \"de regalo\". Aun asi, para una skill ECC concreta que no caza por similitud, lo limpio es invocarla explicita: `/ecc:rust-review`, `/ecc:security-scan`, `/ecc:plan`, etc.\n\n---\n\n## 4. ¿Teclear `/skill` o dejar el auto-routing?\n\nEl routing es **advisory**: te sugiere e inyecta, pero no es determinista al 100%. Regla practica:\n\n**Deja el auto-routing cuando:**\n- Usas el nombre de una persona (\"Alfred...\", \"Tio Gilito...\") -> trigger fuerte (100 pts), entra seguro.\n- Tu prompt es claro y especifico en su dominio y usa la frase-trigger (\"resuelve este merge conflict en X\").\n- No te importa demasiado que entre o no (la skill es un nice-to-have).\n\n**Teclea la skill explicitamente (tool Skill / `/nombre`) cuando:**\n- **Importa de verdad** y no quieres depender de cruzar 0.80. Ej: vas a refactorizar en serio -> invoca `senior-engineer` o el `rust-patterns`/`python-patterns` a mano (recuerda: el patterns NO entra por la palabra suelta del lenguaje).\n- El match seria solo **MED (0.50-0.80)**: ahi el sistema NO inyecta, solo avisa. Si la quieres, pidela.\n- Quieres una **persona ghost** (`profesor-fisica`/`manolo-lama`) — no disparan por inyeccion porque no tienen carpeta.\n- Es una skill **ECC** concreta que no caza por similitud (`/ecc:rust-review`).\n- Es un **agente** (`rust-engineer`, `security-auditor`): nunca se inyecta, lo lanzas por Task.\n\n> Atajo mental: prompt corto y ambiguo -> el routing acertara poco y caera al fallback semantico (v3), mejor invoca. Prompt con la frase-trigger exacta -> dejalo correr.\n\nSi dudas de que hara el routing con un prompt, verificalo en seco. Sobre el motor v2 (deterministico, lo que ves es el scoring + la inyeccion):\n\n```powershell\necho '{\"prompt\":\"resuelve el merge conflict del rebase\"}' | node C:\\Users\\USER\\.ultron\\cockpit\\skill-lazy\\routing-dispatcher.v2.js\n```\n\nSobre el wireado real (v3 = v2 + fallback semantico Qdrant), lo que de verdad corre el hook:\n\n```powershell\necho '{\"prompt\":\"como organizo mi backlog\"}' | node C:\\Users\\USER\\.claude\\scripts\\routing-dispatcher.js\n```\n\nTe devuelve el `additionalContext` con el candidato, el `confidence` y los `signals` que matchearon (y el bloque `[skill-inyectada: ...]` si supera el umbral). Util para entender por que (no) entra una skill.\n\n---\n\n## 5. Crear una skill nueva\n\nEl flujo correcto es la meta-skill **`skill-creator`** (ya activa en el nucleo). Dispara con \"crea una skill para X\", \"mejora la skill Y\", \"por que no triggerea mi skill\". Trae scripts bundled en `~/.claude/skills/skill-creator/scripts/` (verificado): `quick_validate.py`, `package_skill.py`, `improve_description.py`, `run_eval.py`, `run_loop.py`.\n\nPasos reales:\n\n1. **Generala:** `crea una skill para <tarea>` (o desde la Library tab de la app: New skill -> Generate with AI, que abre una sesion precargada con esta skill).\n2. **Estructura minima** en `~/.claude/skills/<nombre>/SKILL.md`:\n   ```markdown\n   ---\n   name: mi-skill\n   description: >\n     Frase potente con los TRIGGERS reales que escribirias.\n     \"haz X\", \"cuando Y\", en ES y EN. Esto es lo que el dispatcher puntua.\n   ---\n   # Mi Skill\n   ## When to use this skill\n   ...\n   ```\n   El `description` es **lo que el routing lee** (`parseSkillMdTokens` saca tokens de name+description: triggers desde el nombre, context desde palabras >=5 chars del description). Una descripcion vaga = no triggerea nunca. Pon **frases multi-palabra concretas**, no palabras sueltas (las frases con espacio matchean por substring; las palabras sueltas exigen word-boundary).\n3. **Valida:** `uv run python ~/.claude/skills/skill-creator/scripts/quick_validate.py <ruta>`.\n4. **Registrala en el catalogo lazy:** `node C:\\Users\\USER\\.ultron\\cockpit\\skill-lazy\\sync-registry.js` para que entre en `skills-registry.json` con su `token_estimate`. Decide `keep_active: true` (nucleo) o `lazy_loadable: true` (entra por routing).\n5. **Si la quieres lazy**, anadela al array `PLUGINS[]` de `routing-dispatcher.v2.js` con sus `triggers`/`strong`/`context` (el autodiscovery de `~/.ultron/skills/` la cogeria con context debil; para senal fuerte, hardcodeala). Luego `apply-lazy-skills.ps1` renombra su carpeta a `.disabled` para que solo entre on-demand.\n6. **Verifica el routing** (obligatorio tras tocar routing, dice el CLAUDE.md):\n   ```powershell\n   node C:\\Users\\USER\\.ultron\\cockpit\\skill-lazy\\_verify_final.js\n   node C:\\Users\\USER\\.ultron\\cockpit\\skill-lazy\\_accuracy_at3.js\n   ```\n   Ambos deben quedar **verdes**.\n7. **Despliega el dispatcher si lo tocaste:** el motor que corre el hook NO es la copia de `cockpit/`, sino la de `~/.claude/scripts/` (ver seccion 7). Tras editar v2/v3 en cockpit hay que copiarlas a `~/.claude/scripts/routing-dispatcher.v2.js` y `routing-dispatcher.js`. Si no, sigues corriendo la version vieja (clasico \"no se aplico\").\n\n> **Gotcha de Windows:** los `.ps1` deben ser **ASCII puro** (sin em-dash) o PowerShell 5.1 rompe el parser. Y si anades triggers, usa **frases/palabras completas**, nunca substrings cortos: el historico \"arr\"->\"arregla\", \"irr\"->\"irregular\", \"helm\"->\"overwhelmed\" fue catastrofe, por eso `hasToken` exige word-boundaries y solo permite substring en frases con espacio.\n\n---\n\n## 6. Tabla rapida: senal -> que entra\n\n| Lo que escribes | Que dispara |\n|-----------------|-------------|\n| \"Ultron, ...\" / tarea ambigua multi-dominio | `ultron` (orquestador) |\n| \"Alfred, mata el proceso X\" | persona `alfred` |\n| \"Tio Gilito, mis ahorros\" | persona `tio-gilito` |\n| \"Don Claudio, el GAS de UE5\" | persona `don-claudio` |\n| \"kirkardo\" / \"ponme nota\" | persona `repo-evaluator` |\n| \"no encuentro el bug\" | `superpowers:systematic-debugging` |\n| \"test-driven development\" / \"tdd\" | `superpowers:test-driven-development` |\n| \"planifica exhaustivamente\" / \"escribe un plan\" | `hiper-plans` (piso 0.65 con keyword de plan) |\n| \"agentes en paralelo\" / \"multi-agente\" | `superpowers:dispatching-parallel-agents` (piso 0.65) |\n| \"merge conflict\" / \"git bisect\" | `git-conflict-resolver` (+ `ecc:git-workflow` de regalo si HIGH) |\n| \"owasp\" / \"security scan\" | `security-scan` (+ agente `security-auditor` como hint) |\n| \"crear skill\" / \"skill creator\" | `skill-creator:skill-creator` |\n| \"crea un hook\" / \"hookify\" | `ecc:hookify` |\n| \"rust patterns\" / \"borrow checker\" | `rust-patterns` (+ agente `rust-engineer` como hint) |\n| \"playwright\" / \"e2e\" | `webapp-testing` |\n| \"consolida memoria\" / \"ordena .ultron\" | `consolidate-memory` |\n\n---\n\n## 7. La pieza que casi nadie tiene clara: que script corre el hook\n\nEsto importa para no perseguir fantasmas al editar routing.\n\n- El hook `UserPromptSubmit` esta wireado en `~/.claude/settings.json` (linea ~152) a **`node C:/Users/USER/.claude/scripts/routing-dispatcher.js`**.\n- Ese fichero es **v3** (no v2). v3 `require()`a `./routing-dispatcher.v2.js` (la copia desplegada en `~/.claude/scripts/`) para TODO el scoring deterministico, y anade **una sola** ruta nueva: cuando el top candidato deterministico tiene `confidence < 0.80` (`SEMANTIC_FALLBACK_THRESHOLD`), consulta la coleccion `ultron_skills` de **Qdrant** (subproceso `uv run python ~/.ultron/scripts/cockpit/embed_skills.py query \"<texto>\" --top 3`) y anade el top-3 semantico como hint suplementario. Cualquier fallo del subproceso se traga en silencio; el hint deterministico de v2 siempre se emite.\n- **Hay dos copias de cada dispatcher:** las de `cockpit/skill-lazy/` (fuente de verdad en git) y las de `~/.claude/scripts/` (runtime desplegado). El v3 desplegado es identico al de cockpit; el v2 desplegado tiene drift de bytes (CRLF) pero **misma logica** (mismas 1674 lineas). La leccion: editar cockpit no cambia el runtime hasta que copias a `~/.claude/scripts/`.\n\nCold-start: la primera llamada semantica tras reiniciar puede tardar 9-12 s (carga el modelo ~400 MB de sentence-transformers). En caliente, ~200-800 ms.\n\n---\n\n## Rutas y archivos clave\n\n- Dispatcher deterministico (motor): `cockpit/skill-lazy/routing-dispatcher.v2.js` (tablas `PERSONAS`/`PLUGINS`/`AGENTS`, scoring `scoreEntry`, inyeccion lazy `fetchLazySkillContent`, ECC `matchBestEccSkill`).\n- Dispatcher semantico (envoltorio + Qdrant fallback): `cockpit/skill-lazy/routing-dispatcher.v3.js`.\n- **Runtime real del hook:** `~/.claude/scripts/routing-dispatcher.js` (= v3) + `~/.claude/scripts/routing-dispatcher.v2.js` (motor desplegado).\n- Embeddings semanticos: `~/.ultron/scripts/cockpit/embed_skills.py` (subcomandos `init`/`index`/`query`/`status`).\n- Catalogo: `cockpit/skill-lazy/skills-registry.json` (188 entradas; 11 `keep_active`, 177 `lazy_loadable`).\n- Activar/desactivar: `apply-lazy-skills.ps1` (skills propias) y `apply-lazy-ecc.ps1` (plugin ECC).\n- Sincronizar catalogo: `cockpit/skill-lazy/sync-registry.js`.\n- Verificacion: `_verify_final.js` y `_accuracy_at3.js`.\n- Skills en disco: `~/.claude/skills/<nombre>[.disabled]/SKILL.md`. ECC: `~/.claude/plugins/cache/ecc/ecc/<version>/skills/<nombre>[.disabled]/SKILL.md` (232 skills).\n- Validador de skills nuevas: `~/.claude/skills/skill-creator/scripts/quick_validate.py`.\n- Log del routing: `~/.claude/logs/routing-dispatcher.jsonl`."
  },
  {
    "topic": "agentes",
    "title": "Agentes",
    "icon": "🤖",
    "order": 3,
    "summary": "Cuándo y cómo delegar trabajo a subagentes especializados, paralelizarlos sin conflictos, y los gotchas reales de ULTRON (provider-router, worktree stale, no-op silencioso).",
    "markdown": "# Agentes\n\nUn **agente** (subagente) es un Claude separado, con su propio contexto y su propio set de tools, que se lanza con la **tool `Task`** (en este sistema también la verás como `Agent(...)`) para que haga un trabajo acotado y te devuelva un resultado. No es magia ni un servicio remoto: es un `.md` con front-matter en `~/.claude/agents/`. La gracia es triple: (1) **no contamina tu contexto** (el agente quema sus propios tokens leyendo 40 ficheros y a ti te devuelve 20 líneas), (2) **paralelizas** (varios a la vez sobre problemas independientes), y (3) **especializas** (un prompt afinado para revisar Rust o auditar seguridad rinde más que el genérico).\n\n> **Regla de oro** (de `~/.claude/rules/common/agents.md`): si delegas a un agente que **no existe en disco, Claude Code NO da error — hace no-op silencioso**. Esto causó fallos reales (auditoría KIRKARDO 11, sprint 2026-05-27, con una tabla de agentes fantasma). Verifica el nombre antes de delegar.\n\n---\n\n## El catálogo real (verificado en disco)\n\nA día de hoy hay **78 agentes activos** en `~/.claude/agents/` (`*.md`), más uno desactivado (`database-admin.md.disabled` — el vivo es `database-administrator`). Verifícalo tú mismo:\n\n```powershell\n# Cuantos activos (PowerShell)\n(Get-ChildItem ~/.claude/agents/*.md).Count        # -> 78\n\n# Existe el que quiero antes de delegar?\nTest-Path ~/.claude/agents/rust-engineer.md         # -> True\n```\n\n```bash\n# Bash equivalente\nls ~/.claude/agents/*.md | wc -l                    # -> 78\nls ~/.claude/agents/<nombre>.md                     # error si no existe\n```\n\nCada agente es un fichero así (ejemplo **real**, `~/.claude/agents/code-reviewer.md`, front-matter literal):\n\n```yaml\n---\nname: code-reviewer\ndescription: \"Use this agent when you need to conduct comprehensive code reviews...\"\ntools: Read, Write, Edit, Bash, Glob, Grep\nmodel: claude-opus-4-7\n---\nYou are a senior code reviewer...\n```\n\n- **`name`** = el slug que pasas a `Task` (`subagent_type`). Es lo que tecleas.\n- **`tools`** = lo que el agente puede tocar (casi todos los del catálogo llevan `Read, Write, Edit, Bash, Glob, Grep`).\n- **`model`** = qué modelo usa, y **no es uniforme**: `code-reviewer` corre en `claude-opus-4-7`, mientras `rust-engineer`, `database-administrator` y `ultron-skill-editor` corren en `claude-sonnet-4-6`. Si te importa el modelo de un agente concreto, abre su `.md` y míralo.\n\nPara tunear un agente, edita su `.md`. Para **editar skills** (`SKILL.md`) hay un agente dedicado, `ultron-skill-editor` (se dispara también desde la zona `skill_edit` del AI Router y el botón AI-edit de la pestaña Skills).\n\n---\n\n## Cuándo delegar (y cuándo NO)\n\n**Delega cuando:**\n\n- La tarea es **acotada y verificable** (\"revisa este diff\", \"encuentra por qué falla este test\", \"audita auth\").\n- Va a **quemar mucho contexto de lectura** que a ti no te interesa retener (explorar 30 ficheros, mapear un módulo).\n- Tienes **2+ subtareas independientes** → paralelo (ver abajo).\n- Necesitas una **segunda perspectiva especializada** (seguridad, performance, arquitectura).\n\n**NO delegues cuando:**\n\n- Es un one-liner o un edit que harías más rápido tú mismo (el spawn tiene coste de tokens y de tiempo).\n- La tarea necesita **ida y vuelta contigo** (un agente no \"ve\" la GUI ni te pregunta — manda un resultado y se acabó; para UI el **mandamiento 7** de tu `CLAUDE.md` exige verificación visual tuya).\n- El estado es **compartido y secuencial** (dos agentes editando `lib.rs`/`App.tsx` a la vez = conflicto).\n\nDe `rules/common/agents.md`, casos donde **ni hace falta que lo pidas**: feature multi-capa → `fullstack-developer`; código recién escrito → `code-reviewer`; bug/test roto → `debugger` PRIMERO; decisión arquitectónica → `architect-reviewer`; cambio sensible de seguridad → `security-auditor`.\n\n---\n\n## Los agentes más útiles por situación\n\n### Review y calidad\n\n| Agente | Cuándo |\n|--------|--------|\n| `code-reviewer` | Tras escribir cualquier código no trivial |\n| `architect-reviewer` | Decisiones de arquitectura, límites de módulos |\n| `security-auditor` | Auth, secretos, input de usuario, pagos |\n| `qa-expert` | Estrategia de test del ciclo entero |\n| `refactoring-specialist` | Limpiar código sin cambiar comportamiento |\n| `penetration-tester` | Testing ofensivo autorizado, hardening |\n| `accessibility-tester` | Auditoría WCAG de componentes UI |\n\n### Debug y diagnóstico\n\n| Agente | Cuándo |\n|--------|--------|\n| `debugger` | **Primero** ante cualquier bug o test que falla |\n| `error-detective` | Correlacionar errores entre servicios/logs |\n| `performance-engineer` | Perfilar y optimizar hot paths |\n| `legacy-modernizer` | Migración incremental de código viejo |\n\n### Especialistas de lenguaje (para ULTRON)\n\n| Agente | Para qué en este repo |\n|--------|------------------------|\n| `rust-engineer` | El backend Tauri (`control-center/src-tauri/src/`), el sidecar `ultron-memory` |\n| `typescript-pro` / `react-specialist` | El frontend React 19 (`control-center/src/`) |\n| `python-pro` | Scripts `cockpit/` (recuerda: UV, nunca `pip`/`python -m`) |\n| `powershell-5.1-expert` | Scripts `.ps1` (ASCII puro, sin em-dash) |\n| `golang-pro`, `cpp-pro`, `java-architect`, `swift-expert`, `kotlin-specialist`, `csharp-developer` | Otros stacks |\n\n### Infra y ops\n\n`docker-expert`, `kubernetes-specialist`, `terraform-engineer`, `devops-engineer`, `deployment-engineer`, `database-administrator`, `data-engineer`, `sre-engineer`, `incident-responder`, `dependency-manager` (audita CVEs/conflictos de versión).\n\n### Meta — los `ultron-*` (internos de tu sistema)\n\nEstos son **tuyos**, hechos a medida del Control Center (11 en total):\n\n| Agente | Propósito |\n|--------|-----------|\n| `ultron-arch` | Diagnóstico de alto acoplamiento / violaciones SOLID |\n| `ultron-perf` | O(n²), N+1, I/O en hot path |\n| `ultron-security` | Scan OWASP repo-wide (fase 0 del protocolo `/maxdual`) |\n| `ultron-context` | Comprime el codebase a JSON para Codex |\n| `ultron-metadata` | Recolección de metadata (fase 0a) |\n| `ultron-docs` / `ultron-changelog` | Refrescan README/CHANGELOG desde el git log |\n| `ultron-refactor` / `ultron-test` | Refactor que preserva comportamiento / tests framework-aware |\n| `ultron-self-improve` | Audita la telemetría de routing |\n| `ultron-skill-editor` | Edita `SKILL.md` con reglas de lint |\n| `ultron-news` | Curación de noticias IA con scoring |\n\n> **Honestidad sobre dos números:** la tabla en `rules/common/agents.md` dice \"77 agentes (a 2026-05-27)\"; el disco hoy tiene **78** y la skill `ultron` también dice \"78 agents\". Toma **el disco como verdad**, no el comentario stale. Y ojo: varios `ultron-*` (metadata, security, context, arch, perf) están pensados para correr **dentro** del protocolo `/maxdual` Phase 0 — fuera de ese flujo pueden quedarse a medias.\n\n---\n\n## Cómo invocarlos (tool `Task`)\n\nNo hay atajo de teclado mágico: **describes lo que quieres y Claude lanza la `Task`**. Lo único que controlas tú es el **prompt**. Plantilla mental de lo que tienes que teclear:\n\n```\nLanza el agente <slug> para <objetivo concreto y acotado>.\nContexto: <ruta(s) ABSOLUTAS + qué buscar>.\nDevuelve: <forma del entregable: hallazgos con fichero:línea, diff, veredicto>.\nNO toques: <archivos/áreas prohibidas>.\nVerifica: <cargo build / tsc / _verify_final.js / doctor>.\n```\n\nEjemplos reales para este repo:\n\n```\n# Review del backend Rust que acabas de tocar\nUsa code-reviewer sobre el diff de\ncontrol-center/src-tauri/src/agent_orchestration.rs.\nSeveridad CRITICAL/HIGH/MEDIUM/LOW con fichero:linea. No reescribas, solo reporta.\n\n# Debug PRIMERO, siempre\nLanza debugger: el harness cockpit/skill-lazy/_verify_final.js sale en rojo tras mi\nultimo cambio en routing-dispatcher.v2.js. Causa raiz, no parchees aun.\n\n# Especialista de lenguaje DESPUES del debugger\nrust-engineer: el sidecar no compila con --features qdrant. Arregla y deja\ncargo build --release --features qdrant --bin ultron-memory verde, 0 warnings.\n```\n\n**Claves de un buen prompt de agente** (te ahorran iteraciones):\n\n- **Rutas absolutas.** El agente arranca con cwd reseteado entre llamadas bash; las rutas relativas le fallan.\n- **Pide la forma exacta del output** (`fichero:línea`, diff, tabla). Si no, te devuelve prosa.\n- **Acota qué NO debe tocar.** Imprescindible en paralelo.\n- **Recuérdale las verificaciones de runtime** de tu `CLAUDE.md` (`cargo build`, `tsc`, `node cockpit/skill-lazy/_verify_final.js`, `bin/ultron-memory.exe doctor`). Un agente reclamará \"hecho\" sin verificar si no se lo exiges.\n\n---\n\n## Varios agentes en paralelo (el patrón que más rinde)\n\nSi las tareas son **independientes** (no comparten estado, no hay orden), lánzalos **a la vez** — Claude puede emitir varias `Task` en un solo turno. De `rules/common/agents.md`:\n\n```\n# BIEN — en paralelo, ficheros disjuntos\nLanza 3 agentes simultaneos:\n1. security-auditor      -> modulo auth (control-center/src-tauri/src/auth.rs)\n2. performance-engineer  -> recall hibrido (control-center/src-tauri/src/recall.rs + memory/)\n3. typescript-pro        -> tipos compartidos del frontend (control-center/src/)\n\n# MAL — secuencial sin necesidad\nPrimero el 1, luego el 2, luego el 3\n```\n\n**Análisis multi-perspectiva** (council de Anthropic) para una decisión gorda: lanza en paralelo `architect-reviewer` (diseño) + `security-auditor` (amenazas) + `code-reviewer` (corrección) + `qa-expert` (cobertura) + `performance-engineer` (latencia/coste), y **tú** sintetizas los cinco veredictos.\n\n**Tu propia experiencia** (memoria `parallel-agents-overnight-pattern`) confirma el patrón y le pone número: **5–8 agentes paralelos donde cada uno crea archivos NUEVOS y devuelve un \"wiring snippet\"**, y el cableado final (en `lib.rs`/`App.tsx`) lo haces tú al cerrar. Validado en el drop v15.1.4/v15.1.5/v15.2 (8 agentes), **~60 min de pared** para trabajo que en secuencial habrían sido 6+ horas. El truco es la regla explícita en cada prompt: *\"NO toques lib.rs/App.tsx; devuelve el wiring como informe final\"*. Así el merge final es mecánico (5–10 min) y no hay conflictos.\n\n> **Regla:** paralelo para **crear / explorar / auditar**; secuencial (o tú a mano) para **integrar**. Si B necesita el output de A, o dos agentes editarían el mismo fichero, secuencia — o usa `multi-agent-coordinator` / `agent-organizer` para orquestar el estado compartido.\n\n---\n\n## ULTRON elige el proveedor por ti (provider-router)\n\nEsto es **propio de tu sistema** y conviene saberlo: cuando delegas **desde el Control Center** (no desde el CLI), el comando `delegate_task_to_agent` llama a `provider_router::infer_pty_provider` (módulo inline en `control-center/src-tauri/src/agent_orchestration.rs`, líneas ~34–106) y decide **en qué CLI agéntico** corre el agente, sin que lo pidas. La lógica es pura O(1), sin red:\n\n| Tipo de agente | Proveedor | Por qué |\n|----------------|-----------|---------|\n| **Light** (`code-reviewer`, `qa-expert`, `ultron-docs`, `ultron-changelog`, `ultron-news`, `documentation-engineer`, `accessibility-tester`, `knowledge-synthesizer`, `dx-optimizer`) | **Gemini CLI** (free vía OAuth) | Review/docs/qa son baratos |\n| **Code-heavy** (`rust-engineer`, `cpp-pro`, `backend-developer`, `golang-pro`, `python-pro`, `typescript-pro`, `fullstack-developer`, `refactoring-specialist`, `legacy-modernizer`) | **Codex CLI** | Implementación pesada (zona `code-edit`) |\n| Cualquier otro | **Claude** | Fallback duro |\n\nDetalles que importan (todos verificados en el código y sus tests):\n\n- El match es por **substring**: la lista usa `s == a || s.contains(a)`. Un slug que *contenga* \"code-reviewer\" o \"rust-engineer\" también cae en su bucket. No nombres agentes con substrings ambiguos.\n- **`use_cheap_model` manda sobre todo:** si marcas la tarea como low-cost, va a Gemini **aunque sea code-heavy** (hay test explícito `cheap_flag_overrides_code_heavy`).\n- Si el CLI elegido **no está en PATH**, la capa PTY **degrada a Claude** automáticamente (`pty::cli_on_path`) — un `codex`/`gemini` sin instalar **nunca** rompe la delegación.\n- **Implicación práctica:** si quieres que un review concreto lo haga **Claude y no Gemini**, no lo mandes por la ruta de ULTRON con flag cheap; pídelo directo en el chat.\n\n---\n\n## Isolation por worktree (el gotcha que ya te mordió)\n\nPara que un agente trabaje **aislado**, en este sistema se lanza con `isolation: \"worktree\"` (p.ej. `Agent(general-purpose)` con `isolation: worktree`, según `skills/ultron/SKILL.md`): un git worktree separado sobre la misma `.git`. Suena bien para cambios grandes mientras tú sigues en tu rama, **pero tiene una trampa real**.\n\n**Lo que pasó de verdad** (memoria `subagent-worktree-stale-base`, 2026-06-04): el worktree del subagente partió de un **commit VIEJO** (`b42837f`, semanas atrás), **no del HEAD**. Consecuencia: el agente de Finance **recreó desde cero** `finance.rs`/`Finance.tsx` (su base no tenía el finance v2 de `cccdaf2`), y el de Hooks **reinventó** un naming que el HEAD ya tenía mejor. Ninguno de los dos commits era mergeable tal cual.\n\n**Antídoto (en orden de preferencia):**\n\n1. **Si el agente necesita el estado ACTUAL del repo, NO uses `isolation: worktree`.** Déjalo en el worktree principal (o commitea + verifica la base antes).\n2. Si lo usas igualmente, **antes de mergear** comprueba la base:\n   ```bash\n   git merge-base HEAD <rama-del-worktree>   # ¿es tu HEAD reciente?\n   git show <rama-del-worktree> --stat       # ¿qué tocó?\n   ```\n   Si la base es vieja, **porta/reconcilia a mano** en vez de cherry-pick a ciegas.\n3. Si las firmas coinciden (p.ej. comandos Tauri), un `git checkout <rama> -- <archivos>` puntual reconcilia sin merge.\n\nComo es monorepo único (`~/.ultron/`, un solo `.git`, sin submódulos), un worktree mal basado mete regresiones sutiles que `tsc` no caza. Relacionado: `parallel-agent-contract-drift`, `stale-binary-not-applied`.\n\n---\n\n## Nota de honestidad sobre `/maxdual`\n\n`/maxdual` (y `/minidual`, `/dual`) **no son ficheros de slash-command** — el directorio `~/.claude/commands/` está **vacío** ahora mismo. Son **triggers de protocolo** definidos dentro de la skill `ultron` (`skills/ultron/protocols.md`, `mode-*.md`) e `hiper-plans`: cuando los tecleas, el orquestador entra en su modo dual/devil con sus fases 0a/0b. Funcionan porque la skill los reconoce, no porque haya un `.md` en `commands/`. Si esperabas autocompletado de slash-command nativo, no lo hay; escríbelo y deja que la skill `ultron` lo capture.\n\n---\n\n## Checklist antes de delegar\n\n1. **¿Existe el agente?** `Test-Path ~/.claude/agents/<slug>.md`. Si no → no-op silencioso.\n2. **¿Acotada y verificable?** Si necesita tu ojo (UI), no delegues a ciegas (mandamiento 7).\n3. **¿Independientes?** → paralelo, ficheros disjuntos. **¿Dependientes / mismo fichero?** → secuencial o `multi-agent-coordinator`.\n4. **Prompt:** rutas absolutas + forma del output + qué NO tocar + qué verificación correr.\n5. **¿`isolation: worktree`?** → `git merge-base HEAD <rama>` antes de mergear (o mejor, no lo uses para trabajo dependiente del estado).\n6. **Tras código del agente:** pásalo por `code-reviewer` y por las verificaciones de runtime de tu `CLAUDE.md`. **Binario fresco = aplicado** (rebuild + cerrar ULTRON antes)."
  },
  {
    "topic": "comandos",
    "title": "Comandos",
    "icon": "⌨️",
    "order": 4,
    "summary": "Que teclear dentro de Claude Code para domar tu ULTRON: slash-commands reales (ECC, codex, oficiales) vs skills via Skill tool, con flags verificados y flujos copiables.",
    "markdown": "# Comandos — domando los slash-commands de tu sistema\n\nEsta guia NO es una lista de botones de la UI. Es **que teclear** dentro de una sesion de Claude Code para sacarle el maximo a tu propio ULTRON. Todo lo de aqui esta **verificado contra tu disco** (`~/.claude/plugins/cache/`, `installed_plugins.json`, `~/.ultron/docs/COMMANDS.md`), no inventado. Donde el borrador anterior mentia, lo digo explicitamente.\n\n## Lo primero: comando ≠ skill ≠ accion nativa\n\nHay **tres** cosas que la gente mete en el mismo saco y NO lo son:\n\n| Que es | Como se dispara | Ejemplo real |\n|---|---|---|\n| **Slash-command** (de plugin o built-in) | Tecleas `/` en la sesion de Claude | `/ecc:plan`, `/codex:review`, `/review` |\n| **Skill** | Se invoca por el *Skill tool* (le dices \"usa la skill X\" o el dispatcher la inyecta) — **NO** se teclea con `/` | `superpowers:brainstorming`, `superpowers:systematic-debugging`, `ultron`, `hiper-plans` |\n| **Accion nativa del Control Center** | `Ctrl+K` en la app Tauri | `Qdrant re-embed vault`, `Run Doctor`, `Spawn Codex session` |\n\n> **Trampa #1 (el borrador anterior la tenia mal):** `superpowers:brainstorming`, `:systematic-debugging`, `:dispatching-parallel-agents`, `:writing-plans` son **SKILLS, no slash-commands**. Si tecleas `/superpowers:brainstorming` no pasa nada util. La unica forma de slash-command que tuvo superpowers son `/brainstorm`, `/write-plan`, `/execute-plan` (solo en la version 5.0.7 instalada) y **estan DEPRECADOS** — su propio `.md` dice literalmente *\"Deprecated - use the superpowers:brainstorming skill instead\"*. Para usarlas: pide la skill por nombre o deja que el routing las sugiera.\n\nEsta guia cubre **slash-commands**. La palette `Ctrl+K` y el alias `ultron` de shell estan en `C:\\Users\\USER\\.ultron\\docs\\COMMANDS.md` §1 y §4.\n\n---\n\n## Que tienes instalado de verdad\n\nVerificado en `installed_plugins.json` (2026-06-08):\n\n| Plugin | Version | Comandos `/` reales | Notas |\n|---|---|---|---|\n| `ecc@ecc` | **2.0.0-rc.1** | **74** activos | 75 ficheros en `commands/`, pero `code-review.md.disabled` esta apagado |\n| `codex@openai-codex` | 1.0.4 | 7 (`review`, `adversarial-review`, `rescue`, `result`, `status`, `cancel`, `setup`) | Modelo externo |\n| `superpowers` | 5.0.7 **y** 5.1.0 | 3 (DEPRECADOS) en 5.0.7; **0** en 5.1.0 | Su valor son las SKILLS, no los comandos |\n| `commit-commands@claude-plugins-official` | — | `commit`, `commit-push-pr`, `clean_gone` | |\n| `feature-dev@claude-plugins-official` | — | `feature-dev` | |\n| `code-review@claude-plugins-official` | — | `code-review` | |\n| `pr-review-toolkit@claude-plugins-official` | — | `review-pr` | |\n| `agent-skills@addy-agent-skills` | 1.0.0 | `spec`,`plan`,`build`,`test`,`review`,`ship`,`code-simplify` | Cada uno invoca su skill |\n| `code-simplifier@claude-plugins-official` | 1.0.0 | **ninguno** — es un **AGENTE** (`code-simplifier`) | No tecleas `/code-simplifier` |\n| `frontend-design@claude-plugins-official` | — | **ninguno** — es una **SKILL** | |\n| `security-guidance`, `github`, `skill-creator`, `playwright`, `context7`, `rust-analyzer-lsp` | — | aportan agentes/skills/MCP, no slash-commands de cabecera | |\n\n> **Como se invoca un comando de plugin:** prefijo `plugin:` cuando hay colision (`/ecc:plan` vs el `/plan` de agent-skills). Si el nombre es unico, Claude resuelve el corto (`/aside`). Cuando dudes, usa el prefijo. Los **built-in** del CLI (`/review`, `/security-review`, `/verify`, `/run`, `/loop`, `/schedule`, `/init`, `/code-review`, `/simplify`) tambien existen y a veces chocan con los de plugin — el prefijo desambigua.\n\n> **Trampa #2:** los comandos NO viven en `marketplaces/ecc/commands/` (eso es el repo del marketplace), viven en la **cache instalada**: `~/.claude/plugins/cache/ecc/ecc/2.0.0-rc.1/commands/`. Para listar lo real: `ls ~/.claude/plugins/cache/ecc/ecc/*/commands/` o teclea `/` y lee el autocompletado.\n\n---\n\n## Los que de verdad usas a diario\n\nOrdenados por frecuencia real para tu stack (Tauri + Rust + memoria gobernada).\n\n| Comando | Que hace | Cuando teclearlo |\n|---|---|---|\n| `/ecc:plan` | Reformula requisitos, evalua riesgos, plan paso a paso. **Corre inline y ESPERA tu CONFIRM antes de tocar codigo.** Acepta `[feature description \\| path/to/*.prd.md]` | Antes de cualquier feature no trivial. Tu \"planificar es cortar scope\" hecho comando |\n| `/ecc:quality-gate` | Pipeline de calidad: detecta toolchain, formatter, lint, type checks, lista de remediacion. `[path\\|.] [--fix] [--strict]` | Tu mandamiento \"build verde de verdad\". Antes de decir \"hecho\" |\n| `/ecc:rust-review` | Review especifico de Rust (idioms, borrow, unsafe, errores) | Tras tocar el backend Tauri o el sidecar `ultron-memory` |\n| `/codex:review` | Review **con modelo externo** (Codex CLI) contra git state. `[--wait\\|--background] [--base <ref>] [--scope auto\\|working-tree\\|branch]` | Segunda opinion independiente. Tu patron \"validacion cruzada Codex+Gemini\" |\n| `/ecc:rust-build` | Invoca el agente `rust-build-resolver`: arregla build + borrow checker incrementalmente (`cargo check`/`clippy`/`fmt` tras cada fix) | Build del sidecar o backend Tauri roto |\n| `/ecc:build-fix` | Triaje de build generico, delega al resolver del lenguaje correcto | Cuando `cargo`/`tsc`/`npm run build:local` peta y no sabes por donde |\n| `/ecc:save-session` | Guarda estado de sesion en `~/.claude/session-data/` | Antes de cerrar, sobre todo en runs nocturnos |\n| `/ecc:resume-session` | Carga la ultima sesion guardada y retoma con contexto | Al arrancar (complementa tu hook `memory-session-resume.js`) |\n| `/aside` | Responde una pregunta lateral **sin perder** el contexto de la tarea actual | \"que hace este error?\" a media tarea, sin descarrilar |\n| `/commit-commands:commit` | Crea un commit git con mensaje conforme | Cuando pides explicitamente commit (respeta tu `git-workflow.md`) |\n| `/codex:rescue` | Delega investigacion/fix a un subagente Codex. `[--background\\|--wait] [--resume\\|--fresh] [--model <m\\|spark>] [--effort none..xhigh]` | Bug duro donde quieres un segundo cerebro en paralelo |\n| `/ecc:checkpoint create <nombre>` | Corre `/verify quick`, stash/commit y registra en `.claude/checkpoints.log`. Subcomandos: `create\\|verify\\|list` | Marcar un punto estable antes de algo arriesgado |\n\n> Para **revisar codigo no-Rust** o universal: usa `/code-review:code-review` (plugin) o el built-in `/review`. **`/ecc:code-review` NO existe** (esta como `.disabled` en disco).\n\n---\n\n## ECC — el catalogo grande (74 comandos, conoce las familias)\n\nNo te los aprendas todos. Las familias que tocan tu repo:\n\n### Nucleo de flujo\n| Comando | Que hace |\n|---|---|\n| `/ecc:plan` | Plan + riesgos, espera confirm, inline |\n| `/ecc:plan-prd` | Genera un PRD estructurado (**no existe `/ecc:prp-prd`**) |\n| `/ecc:prp-plan` -> `/ecc:prp-implement` -> `/ecc:prp-commit` -> `/ecc:prp-pr` | Pipeline PRP completo (plan -> implementa -> commit -> PR) |\n| `/ecc:quality-gate` | Gate de calidad antes de merge |\n| `/ecc:refactor-clean` | Quita codigo muerto, consolida duplicados |\n| `/ecc:test-coverage` | Reporta cobertura e identifica huecos (tu minimo 80%) |\n| `/ecc:harness-audit` | Scorecard determinista del repo (`[scope] [--format text\\|json] [--root path]`). **Esto, no `/ecc:context-budget`** (eso es una skill, no un comando) |\n\n### Multi-modelo (tu estilo \"council\")\n| Comando | Que hace |\n|---|---|\n| `/ecc:multi-plan` / `/ecc:multi-workflow` | Planificacion / desarrollo colaborativo multi-modelo |\n| `/ecc:multi-backend` / `/ecc:multi-frontend` / `/ecc:multi-execute` | Idem enfocado back / front / ejecucion |\n| `/ecc:santa-loop` | Loop adversarial: **Claude Opus + un modelo externo**, sin contexto compartido, ambos deben devolver **NICE** antes de shippear; si uno dice NAUGHTY, arregla, commit y re-corre revisores frescos — **hasta 3 rondas**. Acepta `[file-or-glob \\| description]` |\n\n### Aprendizaje (sistema de instintos de ECC — OJO, NO es tu memoria)\n| Comando | Que hace |\n|---|---|\n| `/ecc:learn` / `/ecc:learn-eval` | Extrae patrones de la sesion como skills candidatas (eval = autoevalua antes de guardar) |\n| `/ecc:evolve` | Analiza instintos aprendidos, sugiere estructuras de skill |\n| `/ecc:promote` / `/ecc:prune` | Promueve instintos a global / borra pending >30 dias sin promover |\n| `/ecc:instinct-status` / `/ecc:instinct-export` / `/ecc:instinct-import` | Estado/exporta/importa instintos con confidence |\n| `/ecc:sessions` | Navega/busca historial de sesiones |\n\n> **Trampa #3 (honestidad):** `learn`/`instinct`/`evolve`/`promote`/`prune` son del sistema **continuous-learning-v2** de ECC, **independiente** de tu memoria gobernada (`brain.db` + Qdrant). **NO escriben en tu SoT.** Tu memoria real la pueblan los hooks (`memory-orchestrate.js`, `stop-compress-session.js`, `session-end-summary.js`) via `ultron-memory candidate` -> inbox gobernada, y solo `MemoryService` (el sidecar Rust) escribe `brain.db`. Si quieres que algo entre en `brain.db`, va por la inbox, no por `/ecc:learn`.\n\n### Lenguaje-especificos\nTu stack es Rust: triada `/ecc:rust-build`, `/ecc:rust-review`, `/ecc:rust-test`. Hay equivalentes para go, kotlin, cpp, flutter, python (`/ecc:python-review`), fastapi (`/ecc:fastapi-review`), gradle. Para este monorepo Tauri **solo los de Rust** tocan; el resto es ruido salvo que abras otro proyecto.\n\n### Infra / docs / modelo\n`/ecc:update-docs`, `/ecc:update-codemaps`, `/ecc:model-route` (recomienda tier `haiku\\|sonnet\\|opus` segun complejidad/riesgo, `[task] [--budget low\\|med\\|high]`), `/ecc:cost-report`, `/ecc:loop-start` + `/ecc:loop-status`, `/ecc:hookify` (+ `-list`/`-configure`/`-help`), `/ecc:skill-create`, `/ecc:skill-health`, `/ecc:security-scan`.\n\n---\n\n## Otros plugins (lo que aporta cada uno)\n\n| Comando | Plugin | Que hace |\n|---|---|---|\n| `/codex:adversarial-review` | codex | Review que **desafia el enfoque y el diseno**, no solo el codigo |\n| `/codex:setup` | codex | Verifica que el Codex CLI esta listo + toggle del review-gate al cerrar sesion |\n| `/codex:result` / `/codex:status` / `/codex:cancel` | codex | Recoge salida / consulta estado / cancela un rescue o review en background |\n| `/commit-commands:commit-push-pr` | commit-commands | Commit + push + abrir PR de una |\n| `/commit-commands:clean_gone` | commit-commands | Limpia ramas locales cuyo remote ya no existe |\n| `/feature-dev:feature-dev` | feature-dev | Desarrollo guiado de feature con foco en arquitectura y entendimiento del codebase |\n| `/code-review:code-review` | code-review | Review de calidad universal del diff actual |\n| `/pr-review-toolkit:review-pr` | pr-review-toolkit | Review de un PR concreto |\n| `/agent-skills:spec` -> `:plan` -> `:build` -> `:test` -> `:review` -> `:ship` | agent-skills | Pipeline spec-driven (cada uno invoca su skill) |\n| `/agent-skills:code-simplify` | agent-skills | Simplifica sin cambiar comportamiento |\n\n> Lo de **superpowers** y **frontend-design** NO va por slash-command. Pide la skill: `superpowers:brainstorming`, `superpowers:systematic-debugging`, `superpowers:writing-plans`, `superpowers:dispatching-parallel-agents`, `frontend-design`. El `code-simplifier` de oficiales es un **agente** que invocas con el Task/Agent tool, no `/`.\n\n---\n\n## Flujos copiables (encadenados como los usas tu)\n\n**Feature nueva, disciplina Kirkardo:**\n```text\n(skill) superpowers:brainstorming   -> idea difusa a requisitos\n/ecc:plan                            -> plan + riesgos, ESPERA confirm\n... implementar ...\n/ecc:rust-review                     -> review del lenguaje\n/codex:review --base main            -> segunda opinion independiente\n/ecc:quality-gate --strict           -> lint/type/format VERDE\n/commit-commands:commit\n```\n\n**Bug duro:**\n```text\n(skill) superpowers:systematic-debugging          -> metodo, no parchear a ciegas\n/codex:rescue --background \"investiga X y propon fix\"\n/ecc:rust-build                                    -> si el borrow checker grita\n/ecc:quality-gate\n```\n\n**Cierre de sesion / dia siguiente:**\n```text\n/ecc:save-session     (al cerrar)\n/ecc:learn-eval       (patrones de ECC, autoevaluados — NO toca brain.db)\n--- al dia siguiente ---\n/ecc:resume-session   (+ tu hook memory-session-resume.js ya inyecta el resume del SoT)\n```\n\n**Decision arquitectonica con rigor maximo:**\n```text\n/ecc:multi-plan       (varios modelos planifican)\n/ecc:santa-loop       (Opus + modelo externo, ambos NICE, hasta 3 rondas)\n```\n\n---\n\n## Reglas para no pegarte tiros\n\n1. **Comando ≠ skill.** Lo que se teclea con `/` esta en `commands/`. Lo que es skill (superpowers, ultron, hiper-plans, frontend-design) se invoca por el Skill tool. No teclees `/superpowers:*` esperando magia.\n2. **El routing es advisory; el comando es explicito.** Tu `routing-dispatcher.js` *sugiere* skills por intencion (UserPromptSubmit, scoring determinista, sin LLM). Cuando algo importa de verdad, **teclea el comando** en vez de fiarte de que el dispatcher acierte.\n3. **No actives skills en masa.** Viven `.disabled` por diseno (lazy). Los comandos de plugin NO necesitan activar skills — el comando trae lo suyo.\n4. **`/ecc:learn` ≠ tu memoria.** El SoT (`brain.db`) solo lo escribe `MemoryService` via la inbox gobernada. El learn/instinct de ECC vive aparte.\n5. **`Ctrl+K` para lo nativo, `/` para lo cognitivo.** Re-embed Qdrant, doctor, mantenimiento, navegacion -> palette del Control Center (`docs/COMMANDS.md` §1-2). Planificar/revisar/codear -> slash-commands.\n6. **Verifica antes de fiarte de tu memoria.** `ls ~/.claude/plugins/cache/ecc/ecc/*/commands/` o `/` + autocompletado. Las versiones cambian (tienes superpowers 5.0.7 **y** 5.1.0 a la vez, y 5.1.0 ya no trae comandos).\n7. **Lee el `argument-hint`.** Muchos llevan flags (`/codex:review --base main --scope branch`, `/ecc:checkpoint create iter-10`, `/ecc:model-route \"refactor router\" --budget high`, `/ecc:quality-gate . --strict`). El autocompletado los muestra.\n"
  },
  {
    "topic": "prompting",
    "title": "Prompting y modos",
    "icon": "🎚️",
    "order": 5,
    "summary": "Qué modo, overlay y peer pedir en cada caso, qué frase dispara cada comportamiento, y cómo promptear para que ULTRON cierre la tarea.",
    "markdown": "# Prompting y modos\n\nEsta guía es sobre **cómo hablarle a ULTRON** dentro de Claude Code, no sobre los botones del Control Center. El sistema vive en la skill `ultron` (`~/.claude/skills/ultron/`): un índice (`SKILL.md`) que cascadea a `mode-{low,medium,high,ultra,learn}.md` y `protocols.md`, más specs externas en `references/{dual,triple}-mode-protocol.md`. Domarlo es saber **qué modo pedir, cuándo, y qué frase dispara qué comportamiento**.\n\n---\n\n## 1. La verdad incómoda: los modos NO son slash commands\n\nEstá escrito literalmente en `SKILL.md § 📛 SLASH COMMANDS` (y verificado: `~/.claude/commands/` **no existe en disco**):\n\n> Los tokens `/low /high /ultra /maxdual /dual /triple /contrast /learn /thinking /minidual /minitriple /maxtriple /nothinking /nolearn` son **patrones de texto que ULTRON detecta en el prompt**, no slash commands registrados en Claude Code CLI.\n\nConsecuencias prácticas:\n\n- **No esperes autocompletado.** Teclear `/ul` no te ofrece `/ultra`. Escribes el token entero y ya.\n- Funcionan **en cualquier parte del mensaje**: `arregla este bug /high` o `/high arregla este bug` da igual.\n- Solo los reconoce ULTRON **cuando la skill `ultron` está activa**. Si la sesión no la cargó (no dijiste \"Ultron\", no era multi-dominio/ambigua), un `/ultra` suelto puede pasar desapercibido. Por eso: **si el modo importa, ancla la sesión** — empieza con `Ultron, /ultra ...`.\n- Sí existen slash commands **reales** del harness (los lazy-cargados): `/code-review`, `/verify`, `/run`, `/simplify`, y los namespaced `ecc:*`, `codex:*`, `superpowers:*`. Esos están registrados de verdad. No los confundas con los overlays de texto de ULTRON.\n\n---\n\n## 2. Los 4 modos base (esfuerzo creciente)\n\nEl selector vive en `SKILL.md § SELECTOR DE MODO`. **Regla de oro (literal): \"modo más bajo que cumpla\".** Sin trigger, ULTRON arranca en **MEDIUM**.\n\n| Modo | Token | Razonamiento | Plan Mode | Subagentes | Verificación | Cuándo |\n|---|---|---|---|---|---|---|\n| ⚡ **LOW** | `/low` | FAST | No | No | No | Typo, factual, ≤2 pasos, cambiar 1 valor, \"¿qué hace esta función?\" |\n| 🎯 **MEDIUM** (default) | — | STANDARD | Solo si feature HARD o >3 archivos | Solo si >3 queries cross-file | Si toca >3 archivos o >5 tool calls | Tarea de 1 dominio con path claro |\n| 🔥 **HIGH** | `/high` | THINKING si debug/arq | **Obligatorio** | En paralelo si >2 tareas | **Obligatoria** | Feature HARD, bug misterioso, multi-dominio (≥2 personas) |\n| 💀 **ULTRA** | `/ultra` | THINKING siempre | **Siempre, aprobado por ti** | Máximos, `isolation: \"worktree\"` | Obligatoria + review formal | Arquitectura crítica nueva, sin límite de tokens/tiempo |\n\nDetalles que cambian tu forma de promptear (todos verificados en los `mode-*.md`):\n\n- **LOW se autolimita**: 0 delegación, **máx 1 tool call**, respuesta **≤50 palabras** (excepto código). Si la tarea crece, ULTRON **para** y dice *\"Esto merece MEDIUM. ¿Continúo?\"*. Úsalo para preguntas rápidas; no le pidas refactors.\n- **Multi-dominio fuerza HIGH siempre.** Si tu prompt toca p. ej. código + diseño UI (`terry-davis` + `mike-tyson`), ULTRON sube a HIGH aunque no lo pidas — tiene que dividir y delegar persona por persona (`mode-high.md § ROUTING MATRIX`).\n- **MEDIUM tiene umbrales concretos** (`mode-medium.md § TABLA DE DECISIÓN`): ≤3 archivos → directo; 4-5 archivos → Plan Mode; **>5 archivos → sube a HIGH solo**.\n- **ULTRA tiene un GATE explícito** (`mode-ultra.md § PROTOCOLO 0. GATE`): antes de ejecutar muestra *\"⚠️ ULTRA activado — sesión sin límite... Scope estimado: [X]. ¿Confirmas?\"*. **Excepción real:** si escribiste `/ultra` tú mismo en el mensaje, el gate es implícito y continúa. Si el scope es pequeño, te sugerirá bajar a HIGH. Muestra `[ULTRA MODE 💀]` en la primera respuesta.\n- ULTRA hace **bootstrap completo** (`mode-ultra.md § PROTOCOLO 1. BOOTSTRAP`): ejecuta `memory.md § CC WAKE-UP` (Glob de los `MEMORY.md` activos), consulta el índice FTS5 (`brain_index.py query \"<scope>\"`) **antes** de cualquier Read masivo, y reusa `loaded_knowledge` de `current-session.json`. Cuesta turnos iniciales pero evita re-leer.\n\n**Cómo escalar a media tarea**: si estás en MEDIUM y el bug se pone turbio, no reformules todo — escribe `/high` en el siguiente mensaje. ULTRON lee `mode-high.md`, activa Plan Mode y engancha `superpowers:systematic-debugging`.\n\n---\n\n## 3. Overlays que se APILAN (no reemplazan el modo)\n\nNo eligen nivel de esfuerzo: modifican *cómo razona o quién critica*. Se montan encima del modo activo. `/thinking` y LOW se resuelven **inline en `SKILL.md`** (no cascadean a otro archivo).\n\n### `/thinking` — comparar enfoques antes de tocar nada\nFuerza: **3+ enfoques con trade-offs → comparar → elegir con justificación → recién entonces tools** (0 tools en la fase de pensar). Apila sobre cualquier modo. Desactiva con `/nothinking`.\n\nÚsalo cuando hay decisión real y no quieres que se lance al primer enfoque obvio: `/thinking ¿migro el estado de Zustand a Context o lo dejo? compara`.\n\n### `/contrast` — romper el anclaje cognitivo\nProtocolo de 6 fases en `protocols.md § CONTRASTE COGNITIVO`. Para cuando *\"siempre acabamos en la misma solución\"*. Cada fase declara su header en el output (así verificas que se ejecutó):\n\n```\nFASE 1 FREEZE    — NO leer PROJECT.md/MEMORY.md todavía; solo lo que acabas de escribir\nFASE 2 BLANK     — reformular el problema en 1 frase, sin jerga del proyecto\nFASE 3 DIVERGE   — EXACTAMENTE 3 enfoques; 1 obligatorio = antítesis del obvio\nFASE 4 DEVIL     — 1 argumento sólido en contra de cada uno (si no lo hay, el enfoque es débil)\nFASE 5 INTEGRATE — ahora SÍ lee memoria real; marca [VIABLE]/[BLOQUEADO: motivo]/[REQUIERE-DECISIÓN]\nFASE 6 DECIDE    — propone ganador (puede ser el camino habitual, si sobrevive al contraste)\n```\n\nVariantes (verificadas):\n- `/contrast --blank` — en FASE 5 la memoria del proyecto **no vota**; solo detecta contradicciones técnicas duras (p. ej. schema de BD incompatible). Para cuando la propia memoria te ancla. Si los 3 enfoques quedan `[BLOQUEADO]`, escala a `--blank` o a ULTRA automáticamente.\n- `/contrast --dual` — la FASE 4 DEVIL la hace **Codex** (peer externo), no Claude. **Auto-escala a HIGH** si estabas en MEDIUM (`SKILL.md` lo confirma).\n- Triggers en lenguaje natural que también lo disparan: *\"dame alternativas\"*, *\"piensa diferente\"*, *\"empieza de cero\"*, *\"y si lo hiciéramos diferente\"*, *\"piensa fuera del cajón\"*.\n- Composición: `/contrast` + `/thinking` → CONTRAST define los 3 enfoques, THINKING elige entre ellos.\n\n### `/learn` — modo apuntes (overlay NOVALBOS)\nDefinido en `mode-learn.md`. Tras **cada cambio de lógica** ULTRON inserta un bloque didáctico (lo inserta ULTRON, no `terry-davis`):\n\n```\n📚 NOVALBOS DICE\n🔄 CAMBIO:   [qué se cambió — 1 frase]\n🧠 POR QUÉ:  [razón técnica real, no \"para que funcione\"]\n⚠️  EVITA:   [bug/antipatrón que esto resuelve]\n💡 REGLA:    [frase memorizable]\n```\n\n- Se calibra por modo: `/learn`+LOW → solo CAMBIO + REGLA (2 líneas). `/learn`+HIGH → bloque completo + conexión conceptual. `/learn`+ULTRA → además al cierre pregunta *\"¿Guardo estos apuntes en Notion o en NotebookLM?\"*.\n- Al cerrar emite la tabla **\"APRENDIZAJES DE ESTA SESIÓN\"**. Desactiva con `/nolearn`.\n- **No** genera bloque para cambios triviales (typo, formato, comentario). **Sí** para cualquier cambio de lógica, aunque sea 1 línea.\n\nÚsalo cuando estés tocando un dominio que quieres *aprender* (shaders, Rust, lo que sea), no cuando solo quieres que salga rápido.\n\n---\n\n## 4. Peers externos: Dual y Triple (Codex + Gemini)\n\nULTRON puede pedir crítica a modelos externos **read-only**. Claude **decide**; los peers **sugieren** y jamás escriben. Spec en `references/dual-mode-protocol.md` y `triple-mode-protocol.md`.\n\n**Backends reales (a v15.0.1, ya NO el plumbing casero):**\n- **Codex** = `gpt-5.5`, auth por suscripción ChatGPT (`codex login`), pin en `~/.codex/config.toml`. Se invoca por el **plugin oficial `codex@openai-codex`**, mapeado así: `/minidual`→`/codex:review` · `/dual`→`/codex:adversarial-review` · `/maxdual`→`/codex:adversarial-review` + `/codex:rescue` para sub-tareas pesadas. El `shared-duet.ps1` casero quedó **deprecado** en `~/.ultron/scripts/_legacy/`.\n- **Gemini** = `gemini-3.1-pro` (o `flash`), auth OAuth/suscripción, vía helper `gemini-peer.ps1` → escribe a `~/.ultron/.tmp/gemini-out/` → Claude lo `Read`. Ventana grande (~1M tokens) para repo-wide/imágenes/long context.\n\n### Tabla de decisión (qué token usar)\n\n| Necesitas | Token | Rounds | Backend |\n|---|---|---|---|\n| Sanity check de 1 pregunta | `/minidual` | 1 | 1 peer |\n| Feature/refactor con dudas | `/dual` | 3 | 1 peer (auto-pick) |\n| Arquitectura crítica / código a prod | `/maxdual` | 5 | 1 peer + Phase 0 |\n| Que 2 peers debatan desde ángulos distintos (rápido) | `/minitriple` | 1 | Codex + Gemini en paralelo |\n| Debate sustancial de 2 peers | `/triple` | 3 | Codex + Gemini |\n| Decisión irreversible / diseño del sistema | `/maxtriple` | 5 | ambos, 5 rounds |\n| Solo review final del diff (puntual, no overlay) | skill `second-opinion` | — | — |\n\n### Gating por modo (NO se salta — `*-mode-protocol.md § Hard rules`)\n\n| Modo | MiniDual | Dual | MaxDual | MiniTriple | Triple | MaxTriple |\n|---|---|---|---|---|---|---|\n| LOW | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |\n| MEDIUM | ✅ con trigger | ❌ | ❌ | ❌ | ❌ | ❌ |\n| HIGH | ✅ auto | ✅ con trigger | ⚠️ solo si lo pides | ✅ con trigger | ⚠️ con trigger | ❌ |\n| ULTRA | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |\n\nSi pides `/dual` estando en MEDIUM, ULTRON responde *\"Subiendo a HIGH para Dual completo. ¿OK?\"* antes de ejecutar. Para forzar el salto: *\"usa MaxDual aunque sea HIGH\"* — ULTRON lo respeta y lo anota como **override** en el log de sesión (regla literal del protocolo).\n\n### Auto-routing de peer (`/dual` sin flag)\nULTRON elige Codex o Gemini según señales y te lo reporta (`[Dual auto-routing] peer elegido: Codex — señal: surgical fix en 1 archivo`). Señales reales de `protocols.md`:\n\n| Señal en tu prompt | Peer |\n|---|---|\n| \"todo el repo\", \">N files\", \"cross-cutting\", imagen, long context | **Gemini** (ventana ~1M) |\n| \"surgical\", \"este archivo\", \"este método\", \"token efficiency\", \"terminal/CLI\" | **Codex** (gpt-5.5, ~72% menos output tokens) |\n| Default sin señales | **Codex** |\n\nOverride manual: `/dual --codex`, `/dual --gemini`, `/dual --rounds=N` (Dual 1-5, MaxDual 3-8).\n\n### `/maxdual` y Phase 0\n`/maxdual` **siempre** corre **Phase 0** primero: diagnóstico paralelo con los agentes `ultron-metadata` (secuencial) → luego `ultron-security`, `ultron-arch`, `ultron-perf` y `ultron-context` (este último solo si >50 files) en un solo mensaje. Eso da un `ConsolidatedDiagnostic` comprimido a Codex antes de criticar, cacheado en `~/.ultron/sessions/<fecha>/phase0-<hash>.json`. Reglas duras: peers **siempre** `read-only`; si Codex marca `verdict: \"block\"` / \"concerns\", ULTRON **para** y te avisa.\n\n**Caps diarios sin permiso explícito** (los pide ULTRON automáticamente al superarlos): MiniDual ilimitado · **Dual 3/día** · **MaxDual 1/día** · MiniTriple ilimitado · **Triple 3/día** · **MaxTriple 10/día**.\n\n**Cuándo NO usar peers:** preguntas triviales y **dominios no técnicos** — negocio, finanzas, física, narrativa. Ahí mandan las personas (`warren`, `jordan-belfort`, `tolkien`...), no Codex. Y si solo quieres revisión de un diff ya escrito, usa `second-opinion` (skill), no Dual.\n\n---\n\n## 5. Técnicas de prompting que funcionan AQUÍ\n\nLo que el sistema premia (y lo que la disciplina Kirkardo del `CLAUDE.md` del proyecto exige).\n\n### a) Da el ENTREGABLE LITERAL, no un proxy\n`CLAUDE.md` del repo, mandamiento 1: *\"El feedback literal del usuario ES el entregable — ejecutar su lista, no una proxy.\"* Si quieres que toque `ai_router.rs`, dilo; no digas \"mejora el routing\".\n\n### b) Define DONE antes de pedir (TASK-SPEC)\n`protocols.md § TASK-SPEC` se activa solo en tareas **≥20 min, O ≥3 archivos, O HIGH/ULTRA** — y entonces ULTRON te hará 3 preguntas (DONE/SCOPE/MVP). Si TÚ das el spec, te las ahorras. Formato canónico copiable:\n\n```\nTASK:  [qué hacer, 1 línea]\nWHY:   [por qué importa]\nDONE:  [criterio verificable: ¿test pasa? ¿archivo existe? ¿app abre?]\nSCOPE: [tocar: X, Y | NO tocar: A, B]\nMVP:   [versión mínima útil]\n```\nSin `DONE` claro entras en loop infinito de \"mejoras\". Regla literal del protocolo: *\"si no hay criterio DONE claro → loop infinito\"*. Es la causa #1 de sesiones que no cierran.\n\n### c) Pide verificación en RUNTIME, no claims\nEl proyecto manda *\"Verificar en runtime (eval/doctor/cargo/tsc/abrir la app), no claims.\"* Prompts que funcionan, con comandos reales del repo:\n- `...y verifícalo con bin/ultron-memory.exe doctor` (o `... eval` para recall)\n- `...rebuild con npm run build:local y confirma que compila (cargo 0 warnings + tsc 0)`\n- `...corre node cockpit/skill-lazy/_verify_final.js y pégame la salida` (harness de routing)\n\n### d) Recuerda el gotcha del binario stale\nSi tocas Rust y ULTRON está abierto, el `.exe` **no se relinka** (el build hace el frontend pero salta el link). Anótalo en el prompt: *\"voy a cerrar ULTRON antes; rebuild de verdad, no asumas que aplicó\"*. Hay memoria entera sobre esto (\"Stale binary = no aplicado\").\n\n### e) Marca el SCOPE para no dispersar\nHay un hook PostToolUse que avisa *\"SCOPE WARNING: N files modified — Consider whether changes are too scattered\"*. Acota en el prompt: `SCOPE: solo control-center/src/components/Memory*.tsx, NO toques Rust`.\n\n### f) Si se atasca, nómbralo\n`protocols.md § ANTI-STUCK` se dispara si repites \"no funciona / ya lo probé / sigue igual\" 2+ veces, o tras 3+ tool calls fallidos. Puedes invocarlo: *\"estás en bucle, cambia de enfoque\"* → ULTRON declara `[ANTI-STUCK]`, reformula el problema sin jerga (≤2 frases) y pivota a un enfoque radicalmente distinto, escalando LOW→MEDIUM→HIGH→ULTRA→externo.\n\n---\n\n## 6. Plantillas de prompt copiables\n\n**Fix rápido (LOW):**\n```\n/low ¿qué hace move_card en kanban.rs? una frase.\n```\n\n**Feature acotada con verificación (HIGH):**\n```\n/high Cablea el comando analyze_local_repo en la UI de Library.\nTASK:  botón \"Analyze\" que invoque el comando Tauri y pinte el resultado\nDONE:  build:local compila (cargo 0 warnings + tsc 0) y el botón devuelve datos reales\nSCOPE: tocar control-center/src/components/Library*, lib.rs | NO tocar memoria\nVerifícalo abriendo la app (la UI la confirmo yo).\n```\n\n**Decisión de arquitectura con peer externo (ULTRA + dual):**\n```\n/ultra /dual Decide si separo codegraph del recall de memoria o lo dejo acoplado.\nContexto: brain.db es escritor único vía MemoryService; 478 codebase_fact contaminaban recall.\nQuiero 3 enfoques, que Codex haga de abogado del diablo, y un veredicto reversible.\n```\n\n**Romper anclaje (contrast --blank):**\n```\n/contrast --blank Siempre acabo resolviendo el fallback del AI Router con más providers.\nEl problema real, sin la memoria del proyecto: dame 3 enfoques, uno la antítesis.\n```\n\n**Aprender mientras se implementa (learn + high):**\n```\n/high /learn Implementa el recall híbrido RRF sparse+dense.\nQuiero el bloque NOVALBOS DICE tras cada cambio y los apuntes al cierre.\n```\n\n**Debate irreversible (maxtriple, ULTRA):**\n```\n/ultra /maxtriple Diseño del esquema canónico de brain.db v4 (migración irreversible).\nQue Codex y Gemini debatan; escala a mí si los verdicts divergen.\n```\n\n---\n\n## 7. Tabla de bolsillo\n\n| Quiero... | Escribo |\n|---|---|\n| Respuesta corta y barata | `/low` |\n| Que compare opciones antes de actuar | `/thinking` |\n| Salir del enfoque de siempre | `/contrast` (o `--blank` / `--dual`) |\n| Feature seria con plan + verificación | `/high` |\n| Arquitectura crítica sin límite | `/ultra` |\n| Sanity check externo de 1 pregunta | `/minidual` |\n| Crítica externa de una decisión | `/dual` (auto-pick) o `/maxdual` (ULTRA) |\n| Dos peers debatiendo | `/minitriple` / `/triple` / `/maxtriple` |\n| Aprender mientras codeo | `/learn` (+ `/nolearn` para apagar) |\n| Que deje de pensar tanto | `/nothinking` |\n| Review puntual de un diff (sin overlay) | skill `second-opinion` |\n\n> **Lo que ULTRON NO puede hacer (honestidad, `SKILL.md § LIMITACIÓN — CUOTA Y AI ROUTER`):** cambiar el proveedor del CLI a mitad de conversación. `ai_router.rs` solo rutea las llamadas de la **app Tauri**, no las de Claude Code (que habla directo con Anthropic). Ni `quota-capture.js` ni `react_to_rate_limit` re-enrutan el CLI: solo persisten señales en `~/.ultron/cockpit/quota-state.json` para que la app reaccione. El camino real para forzar otro modelo en la misma sesión es la skill `ecc:model-route`, o el `/model` interno del CLI (o reabrir con `--model`).\n"
  },
  {
    "topic": "memoria",
    "title": "Memoria de ULTRON",
    "icon": "🧠",
    "order": 6,
    "summary": "Como inspeccionar, consultar, gobernar y limpiar tu sistema de memoria propio (sidecar Rust + Qdrant) con los comandos reales de tu maquina.",
    "markdown": "# Memoria de ULTRON — domarla, no admirarla\n\nEsto no es Mem0 ni un grafo de conocimiento de plugin. Es tu sistema propio, en Rust, con un **escritor unico** y un inbox gobernado por bandas de confianza. Aqui aprendes a leerlo, consultarlo, alimentarlo y limpiarlo a mano — con los comandos, rutas y numeros REALES de tu maquina (verificados con `doctor`/`stats`/`eval` el 2026-06-07).\n\n> Regla de oro del sistema: **solo `MemoryService` (a traves del sidecar `ultron-memory.exe`) escribe `brain.db`**. Los hooks NUNCA escriben memoria directa: solo *proponen* candidatos. Escribir a Qdrant o Mem0 directo esta prohibido por diseno (`hooks/manifest.json` -> `writer_paths_forbidden: [\"qdrant_direct\",\"mem0\"]`, `writer_paths_allowed: [\"MemoryService\",\"NONE\"]`).\n\n---\n\n## 1. Arquitectura en una pantalla\n\n| Pieza | Que es | Ruta real |\n|---|---|---|\n| **brain.db** | Fuente de verdad. SQLite + FTS5, event-sourced (`memory_event` append-only), `user_version=4` | `C:\\Users\\USER\\.ultron\\brain.db` |\n| **Qdrant nativo** | Recall denso (E5-large, **1024d**). Auto-launch | `D:\\Ultron\\qdrant\\` |\n| **Sidecar memoria** | El binario que hace todo. **Escritor unico** | `C:\\Users\\USER\\.ultron\\bin\\ultron-memory.exe` |\n| **Sidecar embeddings** | Genera los vectores E5 | `C:\\Users\\USER\\.ultron\\bin\\ultron-embed.exe` |\n| **Hooks** | Capturan e inyectan contexto (solo proponen) | `C:\\Users\\USER\\.ultron\\hooks\\scripts\\` |\n| **Manifest de hooks** | SoT de que hook esta vivo y si escribe | `C:\\Users\\USER\\.ultron\\hooks\\manifest.json` |\n| **Config auto-approve** | Flag + umbral de aprobacion | `C:\\Users\\USER\\.ultron\\cockpit\\memory-settings.json` |\n\n**Tres colecciones Qdrant** (`doctor` de hoy):\n- `ultron_memory` — **1480 pts · 1024d** (las memorias gobernadas, lo que importa).\n- `ultron_catalog` — **197 pts · 1024d** (agentes + skills, para auto-routing). Lo llenan `catalog --agents` (**78 agentes**) y `catalog --skills` (**47 skills**).\n- `ultron_sessions` — **72 pts · 384d, `write_dead:true`**. Coleccion vieja, dimension incompatible: ya **no se escribe** (`stop-compress-session.js` dejo de upsertear ahi en `d3a16ff`). Legado: no la mires para recall.\n\n### Recall hibrido (lo que pasa cuando consultas)\n1. **Sparse**: FTS5 sobre brain.db (palabras exactas, BM25).\n2. **Dense**: E5-large en Qdrant (similitud semantica).\n3. **Fusion RRF** (Reciprocal Rank Fusion) + re-ranker. En el JSON de `recall`, el campo `reason` te dice de donde sale cada item. Las tres formas reales que veras:\n   - `\"sparse#1\"` — solo lo pesco FTS5.\n   - `\"dense#5\"` — solo lo pesco E5.\n   - `\"dense#10 + sparse#24\"` — aparecio en ambas listas: **eso es la fusion RRF en accion** (los items que coinciden en sparse y dense suben).\n\n   Cada entrada trae ademas `score` (fusionado), `dense_score`, `sparse_rank`/`dense_rank`, `scope`, `token_estimate`, y al final del JSON un `total_tokens` (lo que costaria inyectar todo el resultado).\n\n---\n\n## 2. El sidecar: tu navaja suiza (comandos reales)\n\nSin argumentos el binario escupe el uso exacto:\n\n```\nultron-memory <resume|orchestrate|recall [--cross|--all-projects]|stats|reindex|\n  catalog [--agents|--skills]|eval [--golden]|eval-full|reconcile|warmup|doctor|\n  candidate|capture|edge|deprecate --type <T> [--dry-run] [--reason R]> [--project X] [args]\n```\n\n> **Define un alias una vez por sesion** y deja de teclear la ruta entera:\n> ```powershell\n> function um { & \"$env:USERPROFILE\\.ultron\\bin\\ultron-memory.exe\" @args }\n> um stats; um recall \"ai router fallback\"\n> ```\n\n### Los cuatro que usaras a diario\n\n**`doctor`** — chequeo de salud (JSON, 10 checks). Lo primero tras tocar memoria o si \"algo huele raro\":\n```powershell\num doctor\n```\nHoy devuelve `max_severity: \"warn\"` (y por eso **exit code 1** aunque el sistema este sano — el unico warn es cosmetico). Checks de hoy: `sqlite ok · 1477 active`, las 3 colecciones Qdrant, `evals recall@8=0.917 · leaks=0`, `versions binario-en-uso == desplegado`, `disk libre 138GB`, `deprecation_deadlines sin vencidos`. El warn: `reconcile drift=3 (orphan=3)` — 1480 puntos en Qdrant vs 1477 activos en SQLite (3 vectores huerfanos). No rompe recall.\n\n**`stats`** — el pulso en una linea:\n```powershell\num stats\n```\n```json\n{\"active\":1477,\"candidates_pending\":42,\"deprecated\":478,\"pending\":0,\"rejected\":0,\"stale\":0}\n```\nLee asi: **1477 memorias vivas**, **42 candidatos esperando tu Accept/Reject** en el inbox, **478 deprecadas** (la purga de `codebase_fact` de iter-10 — bloat sacado del recall sin borrarlo). `candidates_pending` sube solo segun captura: si se dispara, hay un hook metiendo ruido.\n\n**`recall \"<consulta>\"`** — busca como busca el sistema. El test de la verdad:\n```powershell\num recall \"como funciona el recall hibrido\"\n```\nFlags:\n- `--cross` — anade memorias de scope `global` ademas de las del proyecto.\n- `--all-projects` — ignora el filtro de proyecto del todo (mantiene los gates de seguridad). Util para \"donde decidi X\" cuando no recuerdas en que repo fue.\n- `--project <id>` — fuerza un proyecto concreto. El id por defecto se deriva del cwd conservando el punto inicial (`.ultron` no es `ultron`: perder ese punto fue un bug real, ver `claude_sessions.rs:103`).\n\n**`eval`** — mide la calidad del recall contra un set de queries con respuesta esperada:\n```powershell\num eval\n```\nHoy: `recall_at_k=0.917` (11/12), `secret_leak_count=0`, `stale_leak_count=0`. El `per_query` te dice que consulta falla — ahora mismo solo *\"candidato de memoria pendiente de validacion\"* (`matched:false`). Ese es tu **caso negativo permanente**: si tocas el ranking y `recall@8` baja de 0.9 o aparece `secret_leak>0`, lo has roto. `eval --golden` y `eval-full` corren sets mas grandes.\n\n### Los de mantenimiento (menos frecuentes)\n\n| Comando | Que hace | Cuando |\n|---|---|---|\n| `reconcile` | Compara SQLite activo vs puntos Qdrant; lista `orphan`/`missing` por id | Cuando `doctor` marca drift y quieres ver QUE ids |\n| `reindex` | Reconstruye el indice denso desde la fuente de verdad | Tras cambio de modelo de embeddings o drift grande |\n| `warmup` | Calienta E5 (el primer embed en frio tarda ~2-3s) | Al arrancar, para que el primer recall no timeoutee |\n| `catalog --agents` / `--skills` | Indexa agentes (78) / skills (47) en `ultron_catalog` | Tras anadir/quitar agentes en `~/.claude/agents` o skills |\n| `deprecate --type <T> --dry-run` | Cuenta cuantas memorias de un tipo deprecaria | ANTES de purgar un tipo en masa |\n| `resume` / `orchestrate` | Lo que consumen los hooks de sesion (no para teclear a mano) | — |\n| `candidate` / `capture` / `edge` | Inyeccion de candidatos / aristas. Leen JSON por stdin | Los usan los hooks; rara vez tu |\n\n---\n\n## 3. Como se captura sola (el flujo que ya corre)\n\nNo tienes que decir \"recuerda esto\". El sistema observa via hooks. **Todos proponen candidatos; el inbox decide.** Hooks vivos segun `manifest.json` (HEAD `de69662`, iter-10 FASE 6), agrupados por si tocan memoria gobernada:\n\n**Solo leen / inyectan contexto (`writer_path: NONE`):**\n\n| Hook | Evento | Que hace |\n|---|---|---|\n| `memory-session-resume.js` | SessionStart | Inyecta resume acotado (workflows activos, tareas, decisiones, **pinned**, next action) via `ultron-memory resume` |\n| `memory-orchestrate.js` | UserPromptSubmit | Enruta el prompt -> workflow + agentes + **memorias relevantes** via `ultron-memory orchestrate` |\n| `routing-dispatcher.js` | UserPromptSubmit | Sugiere skill/persona por intencion (scoring determinista, sin LLM) |\n| `save-user-prompt.js` | UserPromptSubmit | Archiva el prompt en el inbox markdown diario (`~/.claude/memory/inbox`) — candidates a promover por `consolidate-memory`, NO el SoT |\n| `load-cross-project-memory.js` | SessionStart | Inyecta el indice resumido de `MEMORY.md` de proyectos recientes |\n| `session-start-override.js` | SessionStart | Fallback de resumen previo cuando el match por worktree de ECC falla |\n| `workday-session-linker.js` | SessionStart | Enlaza la sesion al Workday in_progress (datos de workday, no memoria) |\n| `kanban-update-reminder.js` | Stop | Recuerda actualizar el kanban si se completo una tarea |\n| `batch-capture.js` | Stop | Encola comandos REJECTED/FAILED al Run Batch (`~/.ultron/batches/`) |\n| `stop-compress-session.js` | Stop | Comprime la sesion a `decisions-pending.jsonl` (panel Decisions). Ya NO escribe a Qdrant |\n| `precompact-preserve-l0.js` | PreCompact | Preserva el estado L0 en scratch `~/.ultron/.tmp/context.md` antes de compactar |\n| `notify-relay.js` | Notification | Reenvia notificaciones de Claude Code a un log de scratch |\n\n**Proponen candidatos al inbox (`writer_path: MemoryService`, `writes_memory: true`):**\n\n| Hook | Evento | Que propone | Tipo |\n|---|---|---|---|\n| `session-end-summary.js` | SessionEnd | Resume del tail de la sesion (rule-based, sin LLM) | `session_summary` |\n| `subagent-harvest.js` | SubagentStop | Resultado no trivial de un subagente | `agent_note` |\n| `posttoolfail-capture.js` | PostToolUse | Si un tool falla (`is_error`/exit!=0) -> error + snippet. Si el tool tuvo exito, sale en ~120ms sin tocar el sidecar | `error_resolution` |\n\nLo clave: **incluso los tres \"que escriben\" NO escriben `brain.db` directamente**. Llaman a `ultron-memory candidate`, que mete la propuesta como `pending`. **Nunca auto-promueven** desde el hook. Todos son fail-safe (`failure_policy: no_op`): error -> exit 0, jamas bloquean el tool ni abortan la compactacion.\n\nHooks **de-registrados** (no reactivar): `mem0-sync` (Mem0 muerto), `quota-capture` (Quota quitado en cbb2d5c), `session-recall-inject` (reemplazado por el resume canonico), `workday-auto-update` (es una tarea programada de Windows, no un hook de sesion).\n\n---\n\n## 4. El inbox de candidatos: Accept / Reject\n\nTienes **42 candidatos pending** ahora mismo. La GUI los expone en la pestana **Memory** (Memory Inbox + Memory Browser); por debajo son comandos Tauri reales (`commands/memory/inbox.rs`):\n\n| Accion en la UI | Comando Tauri | Efecto |\n|---|---|---|\n| Ver inbox | `memory_inbox_list` | Lista candidatos pending |\n| Accept | `memory_candidate_approve` | Promueve a memoria ACTIVE (validada por ti) |\n| Accept todo | `memory_inbox_approve_all` | Drena toda la cola |\n| Accept solo limpios | `memory_inbox_approve_clean` | Aprueba los CLEAN; deja secretos/contradicciones para revision manual |\n| Editar antes de aceptar | `memory_candidate_edit` | Retoca summary/content/importance/confidence |\n| Reject | `memory_candidate_reject` | Nunca llega a ser memoria |\n\n### Auto-approve: la politica de 3 bandas (con red de seguridad)\n\n**Estado actual de tu maquina: `auto_approve` esta ENCENDIDO** (`memory-settings.json` -> `{\"auto_approve\": true, \"auto_approve_threshold\": 0.9}`). Por eso entran candidatos al inbox solo los que NO cumplen para auto-promoverse. El default del codigo es `false` (`DEFAULT_AUTO_APPROVE_THRESHOLD = 0.85`), pero tu lo subiste a 0.9 y lo activaste.\n\nCuando esta ON, cada candidato CLEAN se clasifica en `classify_band` (`memory/auto_approve.rs`):\n\n| Banda | Condicion | Destino |\n|---|---|---|\n| **A — Approve** | `confidence >= threshold` (0.9) **y** el tipo NO es decision/architecture | Se promueve solo a ACTIVE |\n| **B — Pending** | `0.55 <= confidence < 0.9`, **O** el tipo es `decision`/`architecture` (interpretar intencion siempre lleva ojo humano, aunque la confianza sea alta) | Cae al inbox |\n| **C — Reject** | `confidence < 0.55` (`REJECT_THRESHOLD`) | Auto-rechazado como ruido |\n\nEncima de las bandas hay una **salvaguarda de seguridad innegociable** (`candidate_is_clean`): **secretos y contradicciones SIEMPRE se quedan en el inbox**, pase lo que pase con la confianza o el toggle. El auto-approve nunca pasa por encima de la seguridad.\n\nComandos del toggle: `memory_auto_approve_get` / `memory_auto_approve_set` (al togglear NO se resetea tu `threshold` tuneado). Para limpiar el backlog existente de golpe sin tocar lo flagueado: `memory_inbox_approve_clean`.\n\n> Recomendacion practica: con auto-approve ON y threshold 0.9, lo que cae al inbox es senal de verdad — interpretaciones (decisions/architecture) y dudas de confianza media. Revisa el inbox 1-2 veces al dia: Accept lo que aporta, Reject el ruido residual. Si notas demasiado ruido entrando como ACTIVE, baja a `false` temporalmente con `memory_auto_approve_set` mientras cazas el hook culpable con el Memory Browser.\n\n---\n\n## 5. Pinear, deprecar, olvidar (gobernanza post-active)\n\nUna vez una memoria es ACTIVE, la controlas con estas acciones (todas en `inbox.rs`, todas pasan por `MemoryService` y registran `memory_event`):\n\n| Quiero... | Comando Tauri | Que pasa de verdad |\n|---|---|---|\n| **Pinear** (que SIEMPRE salga en recall + Session Resume) | `memory_item_pin` / `memory_item_unpin` | Marca `pinned`; el resume de SessionStart la lista en `pinned` |\n| **Editar** una memoria viva | `memory_item_edit` | Cambia title/summary/content/importance/confidence |\n| **Re-etiquetar** | `memory_item_relabel` | Cambia scope (`Scope::parse`) o tipo (`MemoryType::parse`) |\n| **Deprecar** (sacar de recall, conservar fila) | `memory_item_deprecate` | Status=Deprecated + **se retira del indice denso** |\n| **Cuarentena** (sospechosa) | `memory_item_quarantine` | Held out de recall + retirada del indice |\n| **\"No usar esto otra vez\"** | `memory_do_not_use` | Status=Rejected + retirada. El recall re-chequea status al cargar: doble bloqueo |\n| **OLVIDAR (borrado duro, irreversible)** | `memory_forget` | **Purga la fila de brain.db y el vector de Qdrant**. Solo sobrevive un evento `forgotten` en el log. Para right-to-be-forgotten / secreto filtrado |\n| **Purgar un tipo en masa** | `memory_bulk_deprecate` (con `dry_run`) | Deprecada todas las activas de un tipo. Asi se sacaron los 478 `codebase_fact` |\n| **Ver el historial de un item** | `memory_item_history` | El `memory_event` log de esa memoria (quien, cuando, que cambio) |\n| **Listar/filtrar activas** | `memory_items_list` | El Memory Browser: filtra por tipo/status |\n\nLa diferencia que importa: **deprecate ≠ forget**. Deprecate la esconde del recall pero la fila sigue (reversible, auditable). Forget la **destruye** (irreversible). Para limpiar bloat usa deprecate; reserva forget para datos personales o secretos.\n\n---\n\n## 6. Aprovecharla a diario — flujos concretos\n\n**Antes de re-implementar algo, pregunta a tu propia memoria.** Tu peor enemigo es rehacer una decision ya tomada:\n```powershell\num recall \"ai router fallback metrica real\" --cross\num recall \"por que descartamos mem0\" --all-projects\n```\n\n**\"Stale binary\": el bug que parece bug y no lo es.** Si tras cambiar Rust de memoria \"no se aplica nada\", casi siempre es el `.exe` viejo (ULTRON abierto bloquea el relink). El check honesto:\n```powershell\num doctor   # check \"versions\": deployed_mtime == running_mtime ?\n```\nSi no coinciden: cierra ULTRON, `cargo build --release --features qdrant --bin ultron-memory`, copia a `bin\\ultron-memory.exe`, y `doctor` otra vez.\n\n**Tras cualquier cambio en el Rust de memoria:** el ritual no negociable es `doctor` + `eval`. Si `recall@8` cae por debajo de 0.9 o aparece `secret_leak>0`, has regresionado. No lo des por \"hecho\" hasta ver el JSON verde. (Recuerda: `doctor` da exit 1 mientras haya el warn cosmetico de drift — mira `max_severity`, no el exit code.)\n\n**Cuando una memoria es importante de verdad** (una decision arquitectonica, una constraint): pinearla desde la pestana Memory. Asi entra en *cada* SessionStart sin depender de que el recall la pesque.\n\n**Sembrar memoria a mano** se hace proponiendo un candidato por stdin (`ultron-memory candidate` lee JSON por stdin — con stdin vacio da `parse candidate json: EOF while parsing a value at line 2 column 0`, es lo esperado). En la practica no lo tecleas tu: lo hacen los hooks. Para meter una nota tuya, lo limpio es escribirla en una sesion y dejar que `session-end-summary` la proponga, o usar la UI.\n\n**Higiene periodica:** `um stats`. Si `deprecated` o `candidates_pending` se disparan, hay un capturador metiendo ruido — abre el Memory Browser (`memory_items_list` filtra por tipo/status), identifica que tipo domina y haz `deprecate --type <ese> --dry-run` antes de purgar.\n\n---\n\n## 7. Cosas que NO son verdad (no te lien)\n\n- **Mem0 NO se usa.** Esta muerto y prohibido reintroducirlo. Si un doc viejo lo menciona, es stale.\n- **El MCP `mcp__qdrant__` (qdrant-find/qdrant-store) NO es tu memoria gobernada.** Es acceso crudo a Qdrant. La memoria de verdad pasa SIEMPRE por el sidecar `MemoryService`. Si escribes por ese MCP te saltas los gates de seguridad y la fuente de verdad (brain.db): no aparecera en el recall gobernado como esperas.\n- **El MCP `mcp__plugin_ecc_memory__` (knowledge graph de ECC) es otro sistema distinto**, del plugin. No es brain.db. No lo confundas con tu memoria.\n- **`ultron_sessions` (384d, `write_dead:true`)** es legado. Si lo ves en `doctor`, ignoralo para recall.\n\n---\n\n## Chuleta\n\n```powershell\n# alias de sesion\nfunction um { & \"$env:USERPROFILE\\.ultron\\bin\\ultron-memory.exe\" @args }\n\num doctor                              # salud (10 checks; warn=exit 1 aunque este sano)\num stats                               # active / candidates_pending / deprecated\num recall \"tu consulta\"                # recall hibrido (proyecto actual)\num recall \"tu consulta\" --cross        # + memorias global\num recall \"tu consulta\" --all-projects # ignora filtro de proyecto\num eval                                # recall@k + leaks (el caso negativo)\num reconcile                           # ids huerfanos/faltantes SQLite<->Qdrant\num catalog --agents                    # reindexa agentes (78)\num catalog --skills                    # reindexa skills (47)\num deprecate --type codebase_fact --dry-run   # cuenta antes de purgar\n```\n\nInbox y gobernanza viven en la pestana **Memory** de la GUI (`memory_candidate_approve/reject`, `memory_inbox_approve_clean`, `memory_item_pin`, `memory_item_deprecate`, `memory_forget`, `memory_items_list`). El sidecar es para inspeccion, eval y mantenimiento; la UI para el dia a dia de Accept/Reject/Pin. Config del auto-approve de 3 bandas: `cockpit\\memory-settings.json` (hoy `auto_approve:true`, `threshold:0.9`)."
  },
  {
    "topic": "ai-routing",
    "title": "AI Routing",
    "icon": "🧭",
    "order": 7,
    "summary": "Que es y que NO es el AI Router: rutea solo las tareas internas de ULTRON (no tu sesion Claude Code), con politica CLI-first, cuotas free-tier reales y la metrica honesta real_fallback_rate.",
    "markdown": "## AI Routing en ULTRON — la verdad primero\n\nEl **AI Router** vive en `control-center/src-tauri/src/ai_router.rs` (Rust, 3.945 lineas) y guarda su config en tres JSON bajo `C:\\Users\\USER\\.ultron\\cockpit\\ai-router\\`:\n\n| Fichero | Que es |\n|---|---|\n| `zones.json` | Lista de **zonas**: cada una con `primary` + `fallbacks[]` (provider_id + model + max_tokens) |\n| `providers.json` | Catalogo de proveedores (id, kind, base_url, key_env_var, models, cost_per_mtok) |\n| `metrics.json` | Contadores agregados (tokens, latencia p50/p95, fallback real, fail_reasons) |\n\n### ⚠️ Lo que el router NO hace (leelo antes de tocar nada)\n\n**El AI Router NO rutea el Claude Code CLI.** Esta conversacion que estas teniendo ahora mismo va **directa a Anthropic** — el router ni la ve. El comentario del propio `route()` lo dice literal (`ai_router.rs:2120-2121`):\n\n> *\"route() governs internal UTILITY tasks (summaries, naming, intent); the Node proxy governs the Claude Code CLI host's own traffic.\"*\n\n`route(zone_id, prompt)` (definido en `ai_router.rs:2138`) solo gobierna las **llamadas LLM internas de la app Tauri**. Callers reales hoy (lista verificada en el audit, `ai_router.rs:2114-2119`):\n\n- `cost_watchdog.rs:279` — resumir gasto\n- `hooks_admin.rs:1490` — telemetria de hooks\n- `workdays.rs:1595/1698` — naming y resumen de jornadas\n- `plugins_info.rs:1031`, `library.rs:1107`\n- `project_agents.rs:471/734`, `sessions_tags.rs:298` — tags / intent\n\n> **Regla mental:** si quieres que GPT-5 o Gemini hagan un trabajo de codigo, lo eliges TU tecleando (`/codex`, lanzando una sesion `gemini`, etc.). El router decide el modelo barato para las **tareas de fontaneria de ULTRON**, no para tu trabajo de codigo en esta sesion.\n\n---\n\n## Anatomia de una zona\n\nLas 9 zonas que existen hoy en `zones.json` (verificadas linea a linea):\n\n| Zona | task_class | primary | fallbacks (en orden) |\n|---|---|---|---|\n| `chat` | light | `gemini-cli` (gemini-2.5-flash) | claude-haiku → groq |\n| `code-edit` | medium | `codex-cli` | codex (API) → deepseek |\n| `code-review` | light | `codex-cli` | gemini-cli → claude-haiku |\n| `research-web` | medium | `gemini-cli` | gemini (API) → groq |\n| `summarize` | trivial | `gemini-cli` | groq → gemini |\n| `routing-decision` | trivial | `gemini-cli` | claude-haiku → groq |\n| `code-fast-local` | light | `ollama` (qwen2.5-coder:32b) | — (sin fallback, offline a proposito) |\n| `utility` | trivial | `gemini-cli` | groq → claude-haiku |\n| `light` | light | `gemini-cli` | groq → ollama |\n\n`routing-decision` ademas trae un `system_prompt`: *\"You classify user prompts into one of the configured zones. Reply with the zone id only.\"*\n\n### Politica CLI-first (decision de USER, 2026-06-08)\n\nEl seed (`ai_router.rs:767-775`) lo documenta: **toda zona de trabajo arranca en un proveedor CLI** porque los CLI (`codex-cli`, `gemini-cli`) autentican via OAuth de tu **suscripcion** (ChatGPT Plus / Google One) y son **gratis en el punto de uso** (`cost_per_mtok: 0.0`). El cloud de pago queda como `fallback[0]` para degradar con elegancia. Excepcion: `code-fast-local` se queda en Ollama (su razon de ser es ser offline).\n\n**Gotchas honestos del propio codigo:**\n\n- `routing-decision` y `utility` son **hot paths internos**. El cold-start de un CLI mete ~1-2s por llamada. El comentario del seed avisa: *\"if the app feels sluggish on memory/hooks, revert those two to groq\"*. Si notas la app lenta en hooks/memoria, cambia esas dos zonas a `groq` (que en tus metrics mide ~134ms de media).\n- **`codex-cli` NO recibe un `model`** — ULTRON ni siquiera se lo pasa. El comentario (`ai_router.rs:834`, `1876-1879`) lo deja claro: la cuenta de ChatGPT elige su propio modelo y *pasar `--model gpt-5` es RECHAZADO por la cuenta* (\"model not supported\"). El `\"gpt-5\"` que ves en `code-edit`/`code-review` es **solo una etiqueta** en el JSON.\n- **APIs CLI divergentes** (`cli_invocation_args`, `ai_router.rs:1892-1898`, con test de regresion en `:2952-2967`):\n  - codex → `codex exec \"<prompt>\" --sandbox read-only --skip-git-repo-check` (prompt **posicional** tras `exec`). OJO: en codex `-p` significa `--profile`, NO prompt — por eso NO se usa.\n  - gemini → `gemini -p \"<prompt>\" --model <model>` (aqui `-p` SI es el prompt).\n- En Windows, los CLI npm aterrizan como shims `.cmd`; el router los invoca via `cmd /C` (`ai_router.rs:1957-1958`) y **sanea el prompt** sustituyendo `& | < > ^ % ( ) ! \"` por `_` (funcion `sanitize_for_cmd`, `ai_router.rs:1931-1942`) para evitar inyeccion de comandos (la vieja CVE KIRKARDO R11.1). Es seguro, pero por eso un prompt con esos simbolos llega \"neutralizado\" al CLI.\n\n---\n\n## Como configurar zonas (UI: pestaña AI Router)\n\nComponentes React en `control-center/src/components/AIRouter/`: `ZoneEditor.tsx`, `ProviderCatalog.tsx`, `RouterDashboard.tsx`, `RouterMetrics.tsx`, `ProxyControl.tsx` (+ `AIRouterIndex.tsx` y `AIRouterErrorBoundary.tsx`).\n\nComandos Tauri registrados (`lib.rs:505-516`): `ai_router_list_zones`, `ai_router_get_zone`, `ai_router_update_zone`, `ai_router_list_providers`, `ai_router_health`, `ai_router_metrics`, `ai_router_test`, `ai_router_route`, `ai_router_usage_summary`, `ai_router_validate_keys`, `ai_router_disabled_providers`.\n\n**Flujo en la GUI:**\n1. Pestaña **AI Router** → editor de zonas. Cambias `primary` y reordenas `fallbacks` (cmd `ai_router_update_zone`).\n2. Boton **Test** por zona: hace una llamada REAL (gasta tokens) y devuelve latencia + excerpt + tokens (`ai_router_test`). Valida la cadena end-to-end y muestra el `error` verbatim si falla.\n3. **Check** en el catalogo: health-check barato que NO gasta tokens (`ai_router_health`). Para CLI comprueba que el binario esta en PATH (`where codex` / `where gemini`, ver `ai_router.rs:1166-1167`); para cloud, que el endpoint responde.\n\n**Editar a mano (si prefieres):** tocas `zones.json` directamente. Dos salvaguardas del loader (`load_zones`, `ai_router.rs:1072`):\n- **Auto-cura:** si borras una zona que el codigo usa, el loader la **re-inyecta** desde el seed. Paso de verdad el 2026-06-04: una edicion manual elimino `utility` y `light` (las dos MAS invocadas desde codigo) y rompio en silencio la extraccion de memoria, el naming de sesiones y los resumenes (`ai_router.rs:1075-1081`).\n- **Migracion idempotente:** si una zona aun apunta al cloud viejo (`codex`/`gemini` como primary), la sube a `codex-cli`/`gemini-cli` e inserta el cloud como `fallback[0]`, persistiendo el cambio (`ai_router.rs:1089-1111`).\n\n> No puedes \"romper\" la lista de zonas borrandolas: vuelven. Pero SI puedes cambiar primary/fallbacks y eso se respeta (las zonas que tu editas se conservan).\n\n---\n\n## Proveedores y keys\n\nCatalogo real (`providers.json`), con tipo, auth y coste de display:\n\n| id | kind | Auth | Coste display | Modelo default |\n|---|---|---|---|---|\n| `codex-cli` | cli | OAuth ChatGPT Plus (binario `codex` en PATH) | 0.0 | gpt-5 (label) |\n| `gemini-cli` | cli | OAuth Google One (binario `gemini` en PATH) | 0.0 | gemini-2.5-flash |\n| `claude-haiku` | cloud | `ANTHROPIC_API_KEY` | 1.25 /Mtok | claude-haiku-4-5-20251001 |\n| `codex` (API) | cloud | `OPENAI_API_KEY` | 10.0 /Mtok | gpt-5 |\n| `gemini` (API) | cloud | `GEMINI_API_KEY` | 0.35 /Mtok | gemini-2.5-flash |\n| `groq` | cloud | `GROQ_API_KEY` | 0.59 /Mtok | llama-3.3-70b-versatile |\n| `deepseek` | cloud | `DEEPSEEK_API_KEY` | 0.14 /Mtok | deepseek-coder |\n| `ollama` | local | sin key (`localhost:11434`) | 0.0 | qwen2.5-coder:32b |\n\n**De donde salen las keys:** al arrancar, `run()` carga via `dotenvy` en este orden — gana el primero, NO sobreescribe env ya presente (`lib.rs:124-133`):\n1. `C:\\Users\\USER\\.ultron\\.env`  ← **el preferido**\n2. `C:\\Users\\USER\\.ultron\\control-center\\.env`\n3. `.env` del cwd (`dotenvy::dotenv()`)\n\nTambien desde **Settings → API Keys** (`set_env_vars_keys`, persiste en `~/.ultron/.env`). Ejemplo de `.env`:\n\n```dotenv\nANTHROPIC_API_KEY=sk-ant-...\nOPENAI_API_KEY=sk-...\nGEMINI_API_KEY=AIza...\nGROQ_API_KEY=gsk_...\nDEEPSEEK_API_KEY=sk-...\n```\n\nEl badge del catalogo lo calcula `compute_key_status` (`ai_router.rs:1040`):\n- **CLI** → `Configured` solo si el binario existe en PATH (no hay key; la auth es del propio CLI).\n- **Local** (ollama) → siempre `Configured`.\n- **Cloud** → `Configured` / `Missing` / `Placeholder`. Es `Placeholder` si la key contiene `your-key`, contiene `replace`, empieza por `xxx`, o es exactamente `sk-...` (funcion `looks_like_placeholder`, `ai_router.rs:1064-1070`).\n\n**Instalar los CLI** (si el Check los marca Missing):\n```powershell\nnpm install -g @openai/codex\nnpm install -g @google/gemini-cli\nwhere codex   # debe encontrar codex.cmd\nwhere gemini\n```\n\n### Cuota free-tier (esto causaba el 98% de fallback)\n\n`free_tier_daily_limit` (`ai_router.rs:554`) conoce los limites reales:\n- **`gemini` API = 20 req/dia** (confirmado por un 429 RESOURCE_EXHAUSTED en vivo el 2026-06-05; antes el codigo creia 1500, error de 75x — ver comentario `ai_router.rs:543-545`).\n- **`groq` ≈ 1000 req/dia**.\n- El resto (CLIs, ollama, paid-only) devuelve `None` → sin gauge de cuota.\n\nAntes de llamar, el router consulta el contador diario y **salta** el proveedor si la cuota esta agotada, sin quemar un 429 (`ai_router.rs:2188-2209`). Politica de 429 (`ai_router.rs:2211-2228`):\n- **Free-tier** (gemini/groq): el primer 429 = \"cuota agotada\" → salta al siguiente **sin sleep ni retry**.\n- **Pago**: reintenta el mismo proveedor con backoff+jitter antes de caer al siguiente.\n\nOverride sin recompilar (entero positivo; si no parsea, usa el default):\n```powershell\n$env:ULTRON_GEMINI_TIER_LIMIT = \"50\"\n$env:ULTRON_GROQ_TIER_LIMIT   = \"2000\"\n```\n\n> Truco: el `gemini-cli` (OAuth Google One) **no tiene el cap de 20/dia** de la API. Por eso `chat`/`research-web`/`summarize`/etc. usan `gemini-cli` de primary y dejan `gemini` API como fallback.\n\n---\n\n## La metrica honesta: `real_fallback_rate`\n\nEn el dashboard veras DOS numeros que parecen lo mismo y NO lo son (`AIRouter/RouterDashboard.tsx:24-27,77-81` y `AIRouter/types.ts:199-205`):\n\n| Metrica | Que mide | Util? |\n|---|---|---|\n| `attempt_failure_rate` (ex-`fallback_rate`) | EMA de fallos **por intento** individual | Ruidosa, infla con cada reintento |\n| **`real_fallback_rate`** | `real_fallback_count / routes_total` = fraccion de rutas que **acabaron** en un secundario | **Esta es la honesta** |\n\nSe computa en `ai_router_usage_summary` (`ai_router.rs:2925`). Tu `metrics.json` actual (2026-06-07):\n```\nroutes_total:        150\nreal_fallback_count: 26     →  real_fallback_rate ≈ 17%\nfallback_rate (EMA): 0.52   ← NO mires esta para juzgar salud\nfail_reasons:        { rate_limit: 70, error: 30 }\n```\n\n**Lectura:** 17% de las tareas internas cayeron a fallback (sano). El 0.52 del EMA es la metrica vieja mal nombrada — ignorala. Si `real_fallback_rate` sube, mira `fail_reasons`: `rate_limit` alto = cuotas; `error` alto = key mal o CLI caido.\n\n**Diagnostico desde terminal:**\n```powershell\nGet-Content C:\\Users\\USER\\.ultron\\cockpit\\ai-router\\metrics.json |\n  ConvertFrom-Json |\n  Select-Object routes_total, real_fallback_count, fail_reasons\n```\n\n> Ojo a tus metrics reales de hoy: `gemini-cli` (27 llamadas) y `claude-haiku` (3) muestran `success_count: 0` y `tokens: 0`, mientras `groq` lleva 136/151 exitos. Un CLI con cero exitos suele indicar cold-start, output vacio que el wrapper no cuenta como exito, o key que no llega al proceso Tauri (ya paso: `ANTHROPIC_API_KEY` no propagaba). Si una zona \"parece muerta\", dale a **Test** y lee el `error` verbatim antes de re-implementar nada.\n\n---\n\n## Recetas rapidas\n\n| Quiero... | Que hacer |\n|---|---|\n| **Tareas internas sin gastar API de pago** | Ya estan CLI-first. Verifica `where codex` / `where gemini` y que el Check los marca Configured. |\n| **La app va lenta en hooks/memoria** | En `zones.json`, cambia el `primary` de `routing-decision` y `utility` de `gemini-cli` a `groq` (mata el cold-start de 1-2s; groq ~134ms). Lo recomienda el propio seed. |\n| **Trabajar 100% offline** | Zona `code-fast-local` (Ollama qwen2.5-coder:32b). Arranca `ollama serve` y verifica `http://localhost:11434/api/tags`. |\n| **Una zona no responde** | Boton **Test** (gasta tokens, da el error real) antes que **Check** (gratis pero solo dice si el endpoint/binario existe). |\n| **Anadi una key y no la coge** | **Reinicia ULTRON.** Las keys se cargan con `dotenvy` en `run()` al arrancar; una key nueva en `.env` no entra en caliente. Y recuerda: si la app esta abierta, un rebuild NO relinka el binario Rust. |\n| **Subir un limite free-tier** | `$env:ULTRON_GEMINI_TIER_LIMIT` / `$env:ULTRON_GROQ_TIER_LIMIT` (sin recompilar). |\n"
  },
  {
    "topic": "workflows",
    "title": "Workflows y dual-mode",
    "icon": "🐺",
    "order": 8,
    "summary": "Cuándo soltar la jauría (workflows multi-agente Claude) vs. peers externos read-only (Codex/Gemini), qué teclear para disparar cada uno y cuándo NO orquestar.",
    "markdown": "# Workflows y dual-mode\n\nAquí no hablamos de botones. Hablamos de **cuándo soltar la jauría** (varios agentes/peers) y cuándo eso es matar moscas a cañonazos. Todo está verificado contra tu sistema: `control-center/src-tauri/src/agent_orchestration.rs`, `control-center/src-tauri/src/workflow_loader.rs`, `scripts/shared-duet.ps1`, `config/intent-rules.yaml`, `cockpit/agent-catalog.json` y los `.claude/skills/ultron/`.\n\n---\n\n## 1. El reflejo que tienes que matar\n\nTu instinto (y el mío por defecto) es resolver todo en el hilo principal. Para el 80% de lo que haces eso es correcto. El problema es el otro 20%: tareas **exhaustivas, auditorías y decisiones de arquitectura** donde un solo pase de un solo modelo es exactamente el escenario donde se cuela el bug que luego te hace gritar \"no se ha aplicado\".\n\nRegla mental de una línea:\n\n> **¿La tarea tiene una sola respuesta verificable de un vistazo? → directa. ¿Requiere cobertura por categorías o que alguien que no seas tú la rompa? → workflow o dual.**\n\n---\n\n## 2. Las tres palancas (qué son de verdad)\n\nTu sistema tiene **tres mecanismos distintos** que se confunden. No son lo mismo:\n\n| Palanca | Qué es | Dónde vive | Quién ejecuta |\n|---|---|---|---|\n| **Workflow multi-agente** | Cadena de sub-agentes Claude por pasos | `agent_orchestration.rs` `list_workflows_inner()` + YAML en `~/.ultron/cockpit/workflows/` | Claude spawnea sub-agentes (PTY) |\n| **Dual / Triple / \"MaxDual\"** | Peers **externos** (Codex, Gemini) como revisores read-only | `scripts/shared-duet.ps1` | CLIs externas en proceso oculto |\n| **Phase 0 diagnóstico** | Pipeline de agentes `ultron-*` que escanean un repo entero | agentes `ultron-metadata/security/arch/perf/context` (en `~/.claude/agents/`) | Sub-agentes Claude especializados |\n\nEl error típico: creer que \"dual\" lanza agentes Claude (no: lanza **Codex/Gemini**), o que un workflow llama a Codex (no: son **sub-agentes Claude**).\n\n---\n\n## 3. Workflows multi-agente (sub-agentes Claude)\n\n### Los 7 built-in reales\n\nEsto es lo que devuelve `list_workflows_inner()` HOY (verificado en `agent_orchestration.rs:703`). Los pasos son **agentes que existen en `~/.claude/agents/`** — NO personas. Las personas (`kirkardo`, `don-claudio`, `terry-davis`…) son **skills**, no agentes; en KIRKARDO 26 se remapearon a agentes reales porque `delegate_task` spawnea por `subagent_type` que debe resolver a un `.md` real en `agents/`, y delegar a un slug fantasma es un **no-op silencioso** (5 de 7 workflows estaban no-op'ando los pasos de persona).\n\n| id | Cuándo | Cadena de agentes (real) |\n|---|---|---|\n| `quick` | Bug obvio, fix rápido | `code-reviewer` → `qa-expert` |\n| `feature` | Feature nueva (diseña primero) | `architect-reviewer` → `fullstack-developer` → `code-reviewer` |\n| `debug` | Bug atascado >20 min | `debugger` → `error-detective` → `qa-expert` |\n| `security` | Pre-release o código de auth | `security-auditor` → `penetration-tester` → `code-reviewer` |\n| `research` | Hay que entender algo nuevo antes | `ai-engineer` → `llm-architect` → `architect-reviewer` → `fullstack-developer` |\n| `game` | Tortunabo / UE5 | `architect-reviewer` → `unreal-engine-engineer` → `cpp-pro` → `qa-expert` |\n| `learning` | Aprender a fondo, no copiar | `llm-architect` → `code-reviewer` → `qa-expert` |\n\nMapeo histórico persona→agente (comentado en `agent_orchestration.rs:711`): `terry-davis→code-reviewer`, `kirkardo→qa-expert`, `don-claudio→architect-reviewer`, `einstein→ai-engineer`, `novalbos→llm-architect`, `ue5-dev→unreal-engine-engineer`. Las personas siguen siendo invocables **por nombre desde el prompt** (eso activa la skill); este mapeo es estrictamente para la delegación a `subagent_type`.\n\n### Cómo se dispara\n\nTres caminos, de menos a más explícito:\n\n1. **Lenguaje natural al hilo principal** — \"esto es una feature nueva, móntame el flujo diseño→implementación→review\". Claude reconoce la forma y arranca los sub-agentes en cadena.\n2. **Pidiéndolo a saco** — *\"usa el workflow `feature`\"* o *\"lanza agentes en paralelo: uno audita auth, otro perf, otro los tipos compartidos\"*. Esto fuerza fan-out. Úsalo cuando las sub-tareas son **independientes** (sin estado compartido).\n3. **Workflow propio en YAML** — para cadenas que repites. Crea `~/.ultron/cockpit/workflows/mi-review.yaml`:\n\n```yaml\nid: mi-review\nlabel: Review memoria ULTRON\ndescription: Pase de seguridad + arquitectura sobre el sidecar\nsteps:\n  - agent: rust-engineer\n    prompt: \"Revisa los cambios en memory/ del último commit\"\n  - agent: security-auditor\n    prompt: \"Redaction y gates de sensibilidad en el write-path\"\n  - agent: code-reviewer\n    prompt: \"Veredicto final, severidad CRITICAL/HIGH/MEDIUM\"\n```\n\nReglas del loader (verificadas en `workflow_loader.rs`):\n- `id`: slug `[a-z0-9][a-z0-9-]*`, máx **64** chars, empieza por `[a-z0-9]`.\n- `label`: no vacío, máx **120** chars.\n- al menos **un** step; cada step requiere `agent` (máx 80 chars, sin `/ \\ .` → bloquea path-traversal). `prompt` es opcional (se mapea a `note`).\n- `env` (k/v) y `tags` son opcionales.\n- En colisión de `id`, **tu YAML gana al built-in** (`merge_with_builtin`): puedes sobrescribir `quick` sin tocar Rust.\n- Se carga **fresco en cada llamada** (no hay caché, no hay que reiniciar). Un YAML inválido se salta con warning a stderr — uno malo no tumba el resto.\n\n> Verifica antes de delegar: `ls ~/.claude/agents/<nombre>.md`. Si el agente no existe, Claude **no falla** — hace no-op silencioso. Ese fue el bug KIRKARDO 11/26.\n\n### Detalles del runtime (sincronía vs. fire-and-forget)\n\nHay dos rutas de delegación en el código, y tienen contratos distintos:\n- **`delegate_task_inner`** (síncrona): spawnea PTY, hace **poll cada 2 s** del buffer buscando el sentinel `[AGENT TASK COMPLETE]`, devuelve el output capturado. Timeout default **300 s** (5 min), `timeout_secs` configurable, **clamp a 3600 s** (1 h). En timeout mata la sesión y devuelve `Err` con output parcial para que el orquestador escale (p.ej. a `debugger`).\n- **`delegate_task_fire_and_forget`** (botón \"Launch agent\" de la UI): spawnea vía `wt.exe` (ventana nueva) y vuelve al instante — no lee output.\n\nDetalle multi-IA que probablemente no recuerdas: `provider_router::infer_pty_provider` decide **en qué CLI corre cada agente sin que lo pidas**. Agentes \"light\" (`code-reviewer`, `qa-expert`, `ultron-docs`, `ultron-news`…) o el flag `use_cheap_model` → **Gemini CLI** (gratis vía OAuth). Agentes \"code-heavy\" (`rust-engineer`, `cpp-pro`, `python-pro`, `fullstack-developer`…) → **Codex CLI**. El resto → **Claude**. Si el CLI elegido no está en PATH, cae **duro a Claude** (un codex/gemini sin instalar nunca rompe una delegación).\n\n### Paralelo de verdad\n\nPara fan-out real, sé explícito (regla de tu `~/.claude/rules/common/agents.md`):\n\n```\nLanza 3 agentes en paralelo:\n1. security-auditor: módulo de auth\n2. performance-engineer: subsistema de caché\n3. typescript-pro: tipos compartidos\n```\n\nIndependientes → paralelo. Si el paso 2 necesita el output del 1 → secuencial (no lo fuerces o tendrás contract drift que `tsc` no caza). Para esto también tienes la skill `superpowers:dispatching-parallel-agents`.\n\n---\n\n## 4. Dual / Triple / \"MaxDual\" — peers externos read-only\n\nEsto es lo gordo: cuando quieres que **otra inteligencia que no es Claude** intente romper tu trabajo. El motor es `scripts/shared-duet.ps1` (reemplazo unificado de los viejos `codex-duet.ps1` + `gemini-duet.ps1`; sigue existiendo `scripts/gemini-peer.ps1` para un second-opinion de **solo Gemini** que escribe su respuesta a fichero).\n\n### ⚠️ Honestidad #1: NO son slash commands\n\n`~/.claude/commands/` **no existe** para esto. `/dual`, `/maxdual`, `/triple`, `/minidual`, `/maxtriple`, `/contrast`… son **patrones de texto que ULTRON detecta en tu prompt**, no comandos registrados en el CLI (lo dice literal la skill `ultron/SKILL.md:41`). No esperes autocompletado. Si los tecleas, ULTRON los reconoce como triggers léxicos y aplica el overlay; si no, describe la intención y el dispatcher hace el resto.\n\n### Qué hace cada modo\n\n| Frase / token que tecleas | `-Peers` / `-Mode` reales | Quién revisa | Para qué |\n|---|---|---|---|\n| **dual** / `/dual` / `/minidual` | `codex` / `Dual` (o `Mini`) | Solo Codex (`gpt-5.5`) | Iteración rápida, verificar un fix |\n| **triple** / `/triple` / **maxdual** / `/maxdual` | `both` / `Triple` (alias de `Max`+`both`) | Codex **y** Gemini en paralelo | Decisión de peso, auditoría, \"no me fío de un revisor\" |\n| `ULTRON ULTRA TRIPLE` / `full army` (de tu MEMORY.md) | overnight | parallel agents + Codex + Gemini + commit + push autónomo | Bloque nocturno, autorización total |\n\n`-Mode Triple` el propio script lo normaliza a `-Mode Max -Peers both` en las primeras líneas (`shared-duet.ps1:59`). Overlays de modo y su escalado (de `ultron/SKILL.md`): `/minidual`→MEDIUM, `/dual`→HIGH, `/maxdual`→ULTRA. Flags de overlay: `--codex`, `--gemini`, `--rounds=N`.\n\n### ⚠️ Lo crítico: son READ-ONLY\n\n- Codex: `codex exec --sandbox read-only`.\n- Gemini: `--approval-mode plan` (o `auto_edit` **solo** en modo PoF, y solo para escribir su propio JSON de respuesta a fichero).\n\n**No tocan tu repo.** Devuelven un veredicto JSON (schema `references/codex-duet-schema.json`). Tú decides qué aplicar. El peer es un **fiscal, no un co-autor**. (Honestidad #2: el `--output-schema` apunta a `.ultron/references/codex-duet-schema.json` relativo al script; ese fichero vive en `.claude/skills/ultron/references/`, así que si lanzas el `.ps1` directamente desde `scripts/` Codex degrada a JSON sin esquema forzado. Sigue devolviendo JSON; solo no se valida contra el schema.)\n\n### Cómo se dispara desde el hilo principal\n\nEl dispatcher tiene la regla `codex-verify` (`intent-rules.yaml:326`): frases como **\"verifícalo con codex\"**, **\"lanza modo dual\"**, **\"lanza maxdual\"**, **\"válidalo con codex\"** activan la skill `codex:codex-rescue` en modo HIGH (confidence 0.90). En la práctica:\n\n```\nAcabo de cambiar ai_router.rs. Lanza dual: que Codex verifique que la cadena\nprimary→fallback no se rompe y que real_fallback_rate sigue bien calculado.\n```\n\no para algo serio:\n\n```\nmaxdual sobre el write-path de memoria: Codex y Gemini revisan redaction +\ngates de sensibilidad. Quiero los dos veredictos antes de commitear.\n```\n\nInvocación directa (control fino) — ojo a las firmas reales:\n\n```powershell\n# Dual (solo Codex), ronda 1, prompt inline\npwsh scripts/shared-duet.ps1 -Peers codex -Mode Dual -Round 1 -Prompt \"...\"\n\n# Triple (ambos en paralelo), prompt desde fichero\npwsh scripts/shared-duet.ps1 -Peers both -Mode Triple -Round 1 -PromptFile prompt.txt\n```\n\n### Detalles que te van a morder\n\n- **Caps soft a 20/día** por modo (`dual-caps.json` / `triple-caps.json` en `~/.ultron/sessions/<fecha>/`). Son **informativos**: emiten `Write-Warning` al pasarse, **no bloquean**. `-BypassCaps` los salta; `-Async` no consume slot (fix Codex H1).\n- **Modelos** (de los params reales): Codex `-CodexModel gpt-5.5` por defecto; Gemini `-GeminiModel pro` → `gemini-3.1-pro-preview`. Alias: `pro-3`→`gemini-3-pro-preview`, `pro-2.5`→`gemini-2.5-pro` (estable, fallback si quota), `flash`→`gemini-3-flash-preview`, `flash-2.5`/`flash-lite`. O pasa un nombre literal.\n- **PoF** (`-PoF`): los preview `gemini-3.1-*` fallan en headless stdout (\"No capacity available\"); PoF pone Gemini en `auto_edit` y le instruye escribir su JSON **directo a fichero**. Úsalo si Gemini te devuelve `error: no_output` con un modelo preview.\n- **Rondas**: Round 1 abre sesión y emite `CODEX_SESSION_ID=...` / `GEMINI_SESSION_ID=...` (también a `last-codex-session-id.txt`). Para Round 2+ pasa `-SessionIds '{\"codex\":\"...\",\"gemini\":\"...\"}'` (Codex hace `exec resume`; Gemini usa `--resume latest`, no UUID).\n- **Async** (`-Async`): escribe la petición a `~/.ultron/multimodel/requests/<id>.yaml` y sale con `{\"async\":true,\"request_id\":...}` — para encolar en overnight sin esperar.\n- **Timeout 240 s** por defecto (`-TimeoutSec`); si un peer se cuelga lo mata y devuelve `error: no_output` (fail-safe, no rompe tu sesión).\n- Proceso **oculto**: `cmd.exe /c` + `CreateNoWindow` — ni ventana ni entrada en taskbar.\n\n---\n\n## 5. El patrón Kirkardo (evaluación por categorías + adversarial)\n\n\"Kirkardo\" es tu vocabulario propio (MEMORY.md): **auditoría estricta estilo profesor**. El dispatcher lo enruta a la skill `repo-evaluator` (existe como `repo-evaluator.disabled`, lazy). Reglas que lo activan: `review-audit` (`intent-rules.yaml:312`, frases tipo *\"full review kirkardo\"* + audit/nota/corrige, conf 0.92) y `academic-eval`/`academic-grade` (*\"dame una nota\"*, *\"corrígeme como profesor estricto\"*, conf 0.90).\n\nEl patrón en sí, lo apliques con o sin la skill:\n\n1. **Evaluar por categorías, no en bloque.** No \"¿está bien?\" sino nota por frente: correctness, seguridad, tests, arquitectura, perf, docs. Eso eran tus rondas R4–R11 (cada eje con score, ej. AI 6.8 / Memory 5.4 / Tokens 4.8).\n2. **Verificación adversarial.** El evaluador **intenta refutar** cada claim de \"hecho\". Aquí dual/triple multiplica: Codex y Gemini son los abogados del diablo externos. Tu memoria registra que esto cazó bugs HIGH reales (read-before-write en `by_model`, data-loss en `cleanup-quarantine.ps1 -Apply`).\n3. **El veredicto manda sobre el optimismo.** CRITICAL bloquea, HIGH avisa. Nada \"Done\" sin evidencia runtime.\n\nTus **puntos de verificación permanentes** (`kirkardo-verification-points`), el corazón del patrón:\n\n> eval desde cero · evidencia runtime (no claims) · binario fresco = aplicado · nada sin cablear · build verde de verdad · docs que no mienten · tests herméticos · el feedback literal ES el entregable · verificación visual de UI · caso negativo · detectar lo no detectado.\n\nFlujo Kirkardo típico:\n\n```\nKirkardo sobre la rama iter-10: evalúa por categorías (correctness, seguridad,\ntests, arquitectura, docs), nota por eje. Luego maxdual: Codex y Gemini intentan\nrefutar cada \"hecho\". Lista de punch CONCRETA con fichero:línea.\n```\n\n---\n\n## 6. Phase 0 diagnóstico (el pipeline de agentes `ultron-*`)\n\nDocumentado en `cockpit/agent-catalog.json`: el conjunto de diagnóstico repo-wide. Los agentes **SÍ existen** en `~/.claude/agents/` (verificado: `ultron-metadata.md`, `ultron-security.md`, `ultron-arch.md`, `ultron-perf.md`, `ultron-context.md`, y además `ultron-docs/changelog/news/refactor/test/self-improve/skill-editor`).\n\n- **`ultron-metadata`** (Phase 0a) — git history, inventario de ficheros, manifests de dependencias. **Siempre primero.**\n- En paralelo (Phase 0b), sobre esos metadatos, **cada uno devuelve JSON estructurado**:\n  - **`ultron-security`** — OWASP, secretos hardcoded, injection, configs inseguras.\n  - **`ultron-arch`** — acoplamiento alto, baja cohesión, violaciones SOLID, complejidad ciclomática.\n  - **`ultron-perf`** — O(n²), N+1, I/O síncrono en hot-paths, crecimiento de memoria sin cota.\n  - **`ultron-context`** — comprime el codebase a JSON (para alimentar a Codex en repos **>50 ficheros**).\n\nHonestidad: el catálogo describe esto como \"Phase 0 de `/maxdual`\", pero (ver §4) `/maxdual` **no es un comando** — es el trigger léxico. En la práctica esta fase la arrancas pidiéndolo: *\"scan repo-wide: corre ultron-metadata y luego security/arch/perf en paralelo\"*, o describiendo una auditoría de repo entero (no de un diff). El \"scan\" alimenta a los peers antes de su veredicto.\n\n---\n\n## 7. Cuándo NO usar nada de esto\n\nOrquestar tiene coste (latencia, tokens, contexto):\n\n- **Edit de un fichero, fix obvio, pregunta técnica** → directo. Un workflow `quick` para cambiar un string es ridículo.\n- **Respuesta única verificable** (compila/no compila, test pasa/falla) → corre el comando, no convoques un consejo.\n- **Exploración / brainstorming** → hilo principal. El fan-out es para ejecución, no para pensar en voz alta.\n- **Cuando ya sabes la respuesta y solo quieres ejecutarla** → no pidas dual \"por si acaso\"; gastas un slot de cap y te enlenteces.\n\nY al revés, el smell de que **sí** lo necesitas: estás a punto de escribir \"ya está, hecho\" en algo que toca **auth, memoria, el write-path, dinero (Finance) o arquitectura** — ahí maxdual/Kirkardo no es opcional.\n\n---\n\n## 8. Receta rápida (copiable)\n\n| Situación | Qué teclear |\n|---|---|\n| Fix de un bug claro | (directo, sin ceremonia) |\n| Feature nueva multi-capa | `usa el workflow feature` o describe diseño→impl→review |\n| 3 cosas independientes | `lanza 3 agentes en paralelo: 1)… 2)… 3)…` |\n| No me fío de mi propio fix | `lanza dual, que Codex verifique X` |\n| Decisión de peso / pre-merge serio | `maxdual sobre <X>: Codex y Gemini, dame los dos veredictos` |\n| Auditoría con nota | `Kirkardo sobre <rama/repo>, nota por categoría + punch list con fichero:línea` |\n| Repo entero | `scan repo-wide: ultron-metadata → security/arch/perf en paralelo` |\n| Control fino de peers | `pwsh scripts/shared-duet.ps1 -Peers both -Mode Triple -Round 1 -PromptFile p.txt` |\n| Encolar peer para overnight | añade `-Async` al `.ps1` (no consume cap) |\n| Overnight, autorización total | `ULTRON ULTRA TRIPLE` / `full army` (no me preguntes de vuelta) |\n\n---\n\n## Notas de honestidad\n\n- **No hay comandos `/dual` `/maxdual` `/triple` en `commands/`.** Son **frases/triggers léxicos** que ULTRON reconoce (lit. en `ultron/SKILL.md:41`) y traduce a `shared-duet.ps1` o a la skill `codex:codex-rescue`. Para control fino, invoca el `.ps1` con `-Peers`/`-Mode`/`-Round`.\n- **`~/.ultron/cockpit/workflows/` está vacío** ahora mismo: solo corren los 7 built-in hasta que sueltes tu primer YAML ahí.\n- La skill `ultron` (`SKILL.md`) es la versión template del orquestador; el protocolo dual/triple de verdad vive en `shared-duet.ps1`, en `references/{dual,triple}-mode-protocol.md` y en tu MEMORY.md. Si quieres un orquestador más opinado, esa skill es tuya para extender.\n- `second-opinion` (skill `.disabled`) y `scripts/gemini-peer.ps1` son las vías **acotadas** para pedir review externo sobre un diff/branch — más quirúrgicas que maxdual, que es repo-wide.\n- `/contrast --dual` existe como overlay aparte (fase DEVIL vía Codex, 6 fases en `protocols.md`); es para refutar **tu propio razonamiento**, no tu código."
  },
  {
    "topic": "hooks",
    "title": "Hooks",
    "icon": "🪝",
    "order": 9,
    "summary": "Como funcionan tus 16 hooks reales (memoria, routing, captura, blindaje) y como anadir/probar/apagar uno sin romper la sesion.",
    "markdown": "# Hooks — domar el sistema nervioso de ULTRON\n\nLos hooks convierten Claude Code en *tu* ULTRON: comandos que dispara **el harness** (no el modelo) alrededor de cada herramienta y de cada evento del ciclo de vida de la sesion. No los invocas tu. Por eso, si quieres que algo pase \"siempre que X\" (siempre que arranque una sesion, siempre que envies un prompt, siempre que falle una tool), eso vive en hooks — no en CLAUDE.md ni en memoria.\n\nEsta guia es sobre **tus** hooks reales (los 16 vivos ahora mismo en `~/.claude/settings.json`), verificados contra el disco, no sobre teoria generica.\n\n---\n\n## 1. Los 9 tipos de evento (y cuales usas)\n\n| Evento | Cuando dispara | Puede bloquear? | Tus hooks aqui |\n|--------|----------------|-----------------|----------------|\n| `SessionStart` | Al abrir una sesion | No (solo inyecta contexto) | 6 |\n| `UserPromptSubmit` | Cada prompt, antes de que el modelo lo vea | Si (exit 2 lo aborta) | 3 |\n| `PreToolUse` | Antes de ejecutar una tool | **Si** (`permissionDecision: deny`) | 1 (`deny-secrets.py`) |\n| `PostToolUse` | Tras terminar una tool | No | 1 (`posttoolfail-capture.js`) |\n| `Stop` | Cuando Claude termina de responder | No | 5 |\n| `SubagentStop` | Cuando un subagente (Task) termina | No | 1 (`subagent-harvest.js`) |\n| `PreCompact` | Justo antes de compactar el contexto | No (no aborta la compactacion) | 1 (`precompact-preserve-l0.js`) |\n| `SessionEnd` | Al cerrar la sesion | No | 1 (`session-end-summary.js`) |\n| `Notification` | Cuando CC pide permiso / esta idle | No | 1 (`notify-relay.js`) |\n\nLa pestana **Hooks** (dentro de `Library`) pinta cada evento con su color y fase — util para ver de un vistazo en que momento corre cada cosa.\n\n---\n\n## 2. La regla de oro: los hooks PROPONEN, no escriben memoria\n\nLo mas importante de tu sistema. **El unico escritor de `brain.db` (la fuente de verdad) es `MemoryService`** (el sidecar Rust `ultron-memory`). Ningun hook escribe memoria canonica directa. Solo pueden:\n\n- emitir `additionalContext` (texto que se inyecta en la sesion), o\n- dejar **candidates** via `ultron-memory candidate` que caen `pending` al inbox gobernado (panel **Memory Inbox**) para que tu los apruebes/rechaces.\n\n`~/.ultron/hooks/manifest.json` declara para cada hook un `writer_path` que solo puede ser `\"MemoryService\"` o `\"NONE\"`. Estan **PROHIBIDOS** `qdrant_direct` y `mem0`. Si un hook se declara `writes_memory:true` con un writer prohibido, el runner lo **bloquea fail-closed** (lo veras como `event:\"blocked\", reason:\"forbidden-writer-path\"` en su log). Esto es lo que mato a `mem0-sync.js` (escribia a Mem0 cloud, fuera del SoT — de-registrado).\n\n> Por eso `session-end-summary`, `subagent-harvest` y `posttoolfail-capture` tienen `writes_memory:true` pero `writer_path:\"MemoryService\"`: no tocan la DB, llaman a `ultron-memory candidate` que **propone**, y tu decides.\n\n---\n\n## 3. Que hace cada hook vivo (en cristiano)\n\n### SessionStart (6) — arrancar con contexto caliente\n\nCorren **en serie**, en el orden de settings.json:\n\n| Hook | Que hace |\n|------|----------|\n| `ensure-qdrant.ps1` | Lanza el Qdrant nativo (`D:\\Ultron\\qdrant\\`) si esta caido. Sin esto el recall denso esta muerto. |\n| `memory-warmup.js` | **Pre-calienta E5.** El modelo `multilingual-e5-large` (1024d, ~1.3GB ONNX en `~/.ultron/.fastembed_cache/`) se carga lazy en el primer embed; en frio el recall mide **~2987ms** y puede reventar el timeout -> contexto vacio. Hace `spawnDetached(['warmup'])` + `spawnDetached(['recall','warmup','--project','__warmup__'])` para que el SO cachee el `.onnx` en page-cache. Retorna en <50ms (no espera). **Honesto:** cada `.exe` es un proceso nuevo, asi que NO deja E5 residente en RAM — solo calienta el page-cache; la cura real seria un daemon `serve`. |\n| `project-card.js` | Inyecta la ficha cacheada del proyecto. |\n| `load-cross-project-memory.js` | Inyecta el indice resumido de `MEMORY.md` de proyectos recientes. |\n| `session-start-override.js` | Fallback de resumen de sesion previa por nombre de proyecto (cuando el match por worktree de ECC falla). |\n| `memory-session-resume.js` | El resume canonico: `ultron-memory resume --project <id>` -> workflows activos, tareas abiertas, decisiones, pinned y `next_action`. Es lo que ves al arrancar. Solo lectura del sidecar. |\n\n**Como lo aprovechas:** abre la sesion *dentro* del directorio del proyecto. El `project_id` se saca del basename del cwd, asi que el resume y la orquestacion vienen filtrados al proyecto. Si abres CC en `~`, el resume sera generico.\n\n### UserPromptSubmit (3) — cada prompt pasa por aqui\n\n| Hook | Que hace |\n|------|----------|\n| `routing-dispatcher.js` | **Tu motor de routing.** El que corre desde `~/.claude/scripts/` es la **v3**: capa semantica sobre la **v2** determinista (`require('./routing-dispatcher.v2.js')`). **Paso 1 (v2, <50ms, sin LLM):** puntua el prompt contra 14 personas, la tabla de skills de plugin/ECC y ~14 agentes. Pesos reales: `W_TRIGGER=100` (trigger explicito), `W_STRONG=60` (frase de dominio fuerte), `W_CONTEXT=25` (contexto generico). Bandas: `HIGH_THRESHOLD=0.80` (sugiere uno), `MED_THRESHOLD=0.50` (sugiere dos), <50% calla. **Paso 2 (v3, solo si la confianza top < 0.80):** consulta la coleccion Qdrant `ultron_skills` via `embed_skills.py query \"<text>\" --top 3` (modelo `paraphrase-multilingual-mpnet-base-v2`) y anade un bloque `[semantic-fallback]`. Subproceso acotado por deadline (~4500ms); si tarda (cold start 9-12s tras reboot) se omite en silencio y queda solo el hint determinista. |\n| `save-user-prompt.js` | Archiva cada prompt no trivial en el inbox markdown diario (`~/.claude/memory/inbox`) — candidate a promover por `consolidate-memory`. |\n| `memory-orchestrate.js` | `ultron-memory orchestrate <prompt>`: intent -> workflow -> agentes a delegar -> memorias relevantes, como `<orchestration-context>`. Solo lectura del sidecar. |\n\n**Aprovechalo (importante):** el routing es **advisory** (un hint, no una orden). El matching de personas es por **trigger literal** y el de dominio por *word-boundary* (no substring), asi que frases vagas puntuan poco. Cuando importa de verdad, **teclea el trigger** — son reales y dan 100 puntos de golpe:\n\n```\nmodo alfred            -> persona alfred (SO/archivos/procesos)\ntio gilito / gilito    -> persona tio-gilito (finanzas personales)\ndon claudio            -> persona don-claudio\nkirkardo / ponme nota  -> skill repo-evaluator (te corrige y puntua)\nhookify / crea un hook -> skill ecc:hookify (te monta el JSON del hook)\n```\n\nPara activar una skill sugerida por el fallback semantico: `/use <skill-name>` o menciona su nombre.\n\n### PreToolUse (1) — el unico que puede DECIR QUE NO\n\n`deny-secrets.py` (matcher `Read|Edit|Write|NotebookEdit|Bash`, via `uv run python`). Tu tripwire: bloquea `.env` (pero NO `.env.example`/`.sample`/`.template`/`.dist`), claves privadas (`.pem .pfx .p12 .key .keystore .jks`), `id_rsa`/`id_ed25519`/`id_dsa`/`id_ecdsa`, cualquier cosa dentro de `.ssh/`, `~/.aws/credentials`, y `secrets.json`/`credentials.json`/`.secrets.json`/`.credentials.json`/`service-account*.json`. Para `Bash`, escanea los tokens del comando con la misma logica. Devuelve `permissionDecision:\"deny\"` + `systemMessage`. Critico bajo `--dangerously-skip-permissions`. Usa patrones **especificos** a proposito (no `*secret*`) para no marcar `secrets_scanner.py`.\n\n> Si necesitas leer un `.env` de verdad, abrelo desde un shell manual — el agente no puede.\n\n### PostToolUse (1) — aprender de los fallos\n\n`posttoolfail-capture.js` corre tras CADA tool pero es **barato en el camino feliz**: si la tool tuvo exito sale en ~120ms sin tocar nada. Solo si detecta fallo (`is_error`, `status:error`, `success:false`, exit code != 0) propone un candidate `error_resolution` con la tool + snippet del error, para que una sesion futura recuerde \"ya pegamos contra esto\".\n\n### Stop (5) — al terminar de responder\n\n| Hook | Que hace |\n|------|----------|\n| `stop-compress-session.js` | Comprime la sesion a hechos (Groq/Anthropic con redaction previa, o heuristico sin key) y los deja en `decisions-pending.jsonl` (panel **Decisions**). El upsert directo a Qdrant `ultron_sessions` esta **RETIRADO** (estaba fuera del SoT, commit d3a16ff). |\n| `kanban-update-reminder.js` | Si detecta tarea completada, recuerda actualizar el kanban del proyecto activo. Solo lee transcript + emite texto. |\n| `batch-capture.js` | Captura comandos REJECTED/FAILED/`ai_cannot_execute` a `~/.ultron/batches/queue-pending.jsonl` (panel **Run Batch**). Cola operativa, no memoria. |\n| `qdrant-mirror-sync.js` | Proyecta el espejo read-only `ultron_mcp_mirror` (1024d, Cosine) para que el MCP generico de Qdrant pueda leer; corre **despues** de la captura a proposito. |\n| `route_quality_aggregator.py` | `uv run python ... aggregate --today`: agrega la telemetria de routing del dia. |\n\n### Los cuatro de FASE 6 (iter-10) — multi-tipo, corren desde `~/.ultron/hooks/scripts/`\n\n| Hook | Evento | Que hace |\n|------|--------|----------|\n| `session-end-summary.js` | SessionEnd | Resumen rule-based del tail -> candidate `session_summary` (pending, nunca auto-promovido). |\n| `subagent-harvest.js` | SubagentStop | Append siempre a `~/.ultron/.tmp/subagent-harvest.jsonl` (writer NONE); si el resultado es no trivial propone candidate `agent_note`. |\n| `precompact-preserve-l0.js` | PreCompact | Antes de compactar, vuelca hechos clave heuristicos del tail a `~/.ultron/.tmp/context.md`. Scratch, NO memoria gobernada. |\n| `notify-relay.js` | Notification | Reenvia cada notificacion (idle/permiso) a `~/.ultron/.tmp/notifications.log` con rotacion. |\n\n---\n\n## 4. El blindaje: por que un hook roto nunca te rompe la sesion\n\nTodos comparten `~/.ultron/hooks/scripts/lib/hook-runner.js` (Node stdlib, cero deps). Verificado en codigo:\n\n1. **Circuit-breaker** persistido en `~/.ultron/cockpit/hooks/breaker-state.json`: tras **5 fallos en 10 min** (`FAILURE_THRESHOLD`/`FAILURE_WINDOW_MS`) el breaker ABRE y el hook se salta (no_op). Tras **15 min de cooldown** (`COOLDOWN_MS`) pasa a half-open y permite UNA prueba; si va bien cierra y limpia fallos, si falla re-abre y reinicia el cooldown.\n2. **Fail-safe duro**: `runHook` NUNCA lanza — un `try/catch` garantiza `exit 0`. Un hook que peta llama a tu `onSkip` para emitir el payload neutro (p.ej. `additionalContext` vacio) y la sesion sigue.\n3. **Logging JSONL** por hook en `~/.ultron/cockpit/hooks/logs/<id>.jsonl` (`ts`, `event` = ok/fail/skipped/blocked, `reason`, `duration_ms`). Escrituras atomicas (tmp+rename).\n4. **Guardrail de SoT**: bloquea writers prohibidos (seccion 2).\n\nPatron minimo de un hook nuevo blindado:\n\n```js\nconst { runHook } = require('./lib/hook-runner');\nfunction emit(ctx) { process.stdout.write(JSON.stringify({\n  hookSpecificOutput: { hookEventName: 'Stop', additionalContext: ctx || '' } })); }\n\nrunHook('mi-hook', async () => { /* cuerpo: leer stdin, hacer trabajo, emit(...) */ },\n  { onSkip: () => emit('') })          // emite neutro si el breaker abre o el cuerpo peta\n  .finally(() => { process.exitCode = 0; });\n```\n\n> Diagnostico rapido: si un hook \"ya no hace nada\", mira `breaker-state.json` — puede estar `open`. Y mira su `.jsonl` para el ultimo error. (Logs vacios = breaker sano.)\n\n---\n\n## 5. Como anadir / desactivar / probar un hook\n\nDos caminos. Elige segun el caso.\n\n### Camino A — la pestana Hooks (rapido, para toggles y altas simples)\n\nLee `~/.claude/settings.json` via el comando Rust `list_hooks`:\n\n- **Disable / Enable** (`toggle_hook`) — apaga un hook sin borrarlo. **Toda mutacion** (toggle/create/update/delete) pasa por `settings_save_inner`, que hace **backup timestampeado + escritura atomica** de settings.json. Esto es lo que usas para silenciar un hook un rato.\n- **Create hook** (`add_hook_inner`) — Evento + Matcher (regex: `Bash`, `Read|Edit|Write`, `mcp__.*`, `*`) + Command. El evento es inmutable tras crear; el evento y el comando pasan por `validate_command`, que **rechaza fragmentos peligrosos** (p.ej. ` IEX `, `; rm -rf`).\n- **Test** (`test_hook`) — corre el comando en un sandbox PowerShell (`-NoProfile -NonInteractive -ExecutionPolicy Bypass`) con **timeout 5s** (`TEST_TIMEOUT`), exponiendo un payload mock via `CLAUDE_HOOK_PAYLOAD` (+ `ULTRON_HOOK_TEST=1`). **OJO:** casi todos tus hooks leen stdin (`fs.readFileSync(0)`); en el Test no hay stdin -> bloquean en EOF y dan **TIMED OUT**. Es esperado y esta documentado en el propio codigo. El Test sirve mejor para hooks que no consumen stdin.\n- **Add with AI** (`request_hook_via_ai_inner`) / **Auto-name all** (`bulk_analyze_hook_names_inner`) — redacta el JSON o pone nombres kebab-case via AI Router zona \"utility\" (Haiku/Gemini Flash), con fallback heuristico.\n- **Delete** (`delete_hook`) — con backup.\n\n### Camino B — editar el .js + settings.json (para tus hooks versionados)\n\nLa **fuente de verdad versionada** de tus hooks de memoria/routing/captura es `~/.ultron/hooks/scripts/*.js`. **Ojo al doble origen en settings.json:**\n\n- Algunos corren **en sitio** desde `~/.ultron/hooks/scripts/` (`memory-warmup`, `project-card`, `memory-session-resume`, `memory-orchestrate`, `qdrant-mirror-sync` y los 4 de FASE 6) -> editas y ya esta.\n- Otros corren desde la **copia** `~/.claude/scripts/` (`stop-compress-session`, `kanban-update-reminder`, `batch-capture`, `routing-dispatcher`, `save-user-prompt`, `load-cross-project-memory`, `session-start-override`) -> hay que **propagar la copia**.\n\nFlujo correcto para los de copia:\n\n```powershell\n# 1. Editas SIEMPRE en ~/.ultron/hooks/scripts/<hook>.js (la SoT)\n#    (routing-dispatcher.js es la v3; necesita su hermano routing-dispatcher.v2.js al lado)\n# 2. Recalculas su checksum y lo pones a mano en manifest.json:\nnode -e \"const fs=require('fs'),c=require('crypto');console.log(c.createHash('sha256').update(fs.readFileSync('C:/Users/USER/.ultron/hooks/scripts/<hook>.js')).digest('hex'))\"\n# 3. Propagas la copia a ~/.claude/ :\npowershell -ExecutionPolicy Bypass -File C:\\Users\\USER\\.ultron\\hooks\\install-hooks.ps1\n```\n\n`install-hooks.ps1` solo **copia los `.js`** a `~/.claude/scripts` y `~/.claude/hooks` (`Copy-Item *.js -Force`); **NO toca settings.json** (las rutas se resuelven con `os.homedir()`) y **NO recalcula checksums** (eso es el paso 2, manual). Para **registrar/quitar un hook del ciclo de vida** si editas el bloque `hooks` de `~/.claude/settings.json` (o desde la pestana Hooks).\n\n**Apagado de emergencia sin tocar config** (env vars, por el `env_allowlist` del manifest):\n\n```\nCLAUDE_NO_HOOKS=1            # apaga la mayoria\nSTOP_COMPRESS_DISABLED=1     BATCH_CAPTURE_DISABLED=1\nNOTIFY_RELAY_DISABLED=1      SUBAGENT_HARVEST_DISABLED=1\nPRECOMPACT_L0_DISABLED=1     SESSION_END_SUMMARY_DISABLED=1\nPOSTTOOLFAIL_CAPTURE_DISABLED=1   WORKDAY_LINKER_DISABLED=1\n```\n\n> En lenguaje natural: teclea `hookify` / \"crea un hook\" y la skill **`ecc:hookify`** (+ `routing-dispatcher`, que la tiene en su tabla) te monta el JSON.\n\n---\n\n## 6. Riesgos reales (timeouts, async, orden)\n\n- **Los timeouts de `SessionStart`/`UserPromptSubmit` son latencia que esperas tu** (corren sincronos). `memory-session-resume` y `memory-orchestrate` usan **12s** en settings.json con colchon interno para el embed E5 en frio (~2987ms verificado). Si los bajas sin daemon `serve` residente, veras contexto vacio (degradacion silenciosa). De ahi `memory-warmup`.\n- **`async:true`** (los 5 Stop, PostToolUse, SessionEnd, SubagentStop, Notification) = CC no espera. Bien para no bloquear, pero un candidate de Stop puede no estar listo justo al cerrar. **No marques async un hook cuyo resultado necesitas ya.**\n- **El orden dentro de un evento es el de settings.json.** `qdrant-mirror-sync` corre tras la captura a proposito; si reordenas, piensa en dependencias.\n- **stdin se lee una vez** (`fs.readFileSync(0)`). Por eso el boton Test da TIMED OUT con ellos.\n- **PreCompact no aborta la compactacion** aunque falle, pero si tarda la retrasa. Mantenlo barato.\n- **El unico hook que puede bloquearte trabajo es `deny-secrets` (PreToolUse).** Si te corta un acceso legitimo, el `systemMessage` te dice por que; ajusta el patron en `~/.ultron/scripts/hooks/deny-secrets.py` (patrones especificos, no genericos, o disparas falsos positivos).\n\n---\n\n## 7. Receta: \"quiero que registre cada comando Bash\"\n\n1. Crea `~/.ultron/hooks/scripts/bash-audit.js` (la SoT): lee stdin, escribe a `~/.ultron/.tmp/bash-audit.jsonl`, y `exit 0` SIEMPRE (copia el patron fail-safe de `notify-relay.js`).\n2. Pestana Hooks -> **Create hook** -> Evento `PreToolUse`, Matcher `Bash`, Command:\n   `node C:/Users/USER/.ultron/hooks/scripts/bash-audit.js` (corre en sitio; no necesita install-hooks).\n3. Anade su entrada al `manifest.json` con su checksum (`node -e ...` de la seccion 5).\n4. Verifica en `~/.ultron/cockpit/hooks/logs/bash-audit.jsonl` que dispara con `event:\"ok\"`.\n\nAsi mantienes la disciplina del sistema: versionado, blindado por el runner, y visible en la UI."
  },
  {
    "topic": "codegraph",
    "title": "Código y CodeGraph",
    "icon": "🕸️",
    "order": 10,
    "summary": "Cómo darle a la IA un mapa AST del código (CodeGraph MCP) para que pregunte al grafo en vez de quemar contexto leyendo ficheros enteros.",
    "markdown": "# Código y CodeGraph\n\n> **El problema que resuelve:** cada vez que abres una sesión, la IA empieza **a ciegas**. Para saber quién llama a `route()` o qué se rompe si tocas `MemoryService`, se pone a leer ficheros enteros con `Read`/`Grep` — quema contexto y a veces ni encuentra el dato. **CodeGraph** le da un mapa precalculado del código (AST real, no regex): qué símbolos existen, quién llama a quién, qué se rompe si cambias algo. La IA pregunta al grafo en milisegundos en vez de leer 3 ficheros de 2000 líneas.\n\nEsto **ya NO es el \"casero\" de ULTRON**. El grafo casero v4 (regex + tablas `edges`/`unresolved_refs` en `brain.db` + hook `capture-symbols.js` + sub-pestaña *Code Graph* en System) se **jubiló el 2026-06-07** (commit `34c4689` \"eliminar casero v4 completo\"): se borraron `commands/memory/codegraph.rs`, `components/system/CodeGraph.tsx`, el hook `capture-symbols.js` y los 4 comandos Tauri. Aportaba poco y **no se inyectaba a los agentes**. Hoy el grafo de código lo provee **CodeGraph** (`@colbymchenry/codegraph`, v0.9.9), un servidor MCP externo instalado por npm global.\n\n> **Nota de honestidad sobre las tablas viejas:** la *migración* `schema_v4.rs` se conservó a propósito (las tablas `edges`/`unresolved_refs` siguen creándose en `brain.db` como \"historia\", y `insert_edge` se mantuvo porque lo usa el sidecar), pero **toda la capa de lectura se borró** (`query_edges`, `graph_metrics`, `drain_unresolved_refs`, `compute_transitive_closure`). En la práctica: tablas presentes pero **inertes para el recall** — no las uses, nadie lee de ellas.\n\n---\n\n## 1. Verifica que está vivo (30 segundos)\n\n```bash\ncd C:/Users/USER/.ultron\ncodegraph status\n```\n\nSalida real en este repo ahora mismo:\n\n```\nCodeGraph Status\nProject:   C:\\Users\\USER\\.ultron\nIndex Statistics:\n  Files:     347\n  Nodes:     8324\n  Edges:     18.534\n  DB Size:   21.99 MB\n  Backend:   node:sqlite - built-in (full WAL)\n  Journal:   wal\nNodes by Kind:\n  function 3511 · import 1236 · property 1019 · variable 630 · constant 464\n  file 336 · struct 336 · type_alias 299 · method 255 · enum_member 103 ...\nFiles by Language:\n  rust 135 · tsx 91 · python 51 · javascript 31 · typescript 26 · yaml 11 · xml 2\n[OK] Index is up to date\n```\n\nSi dice `[OK] Index is up to date`, la IA tiene el mapa fresco. Si no existe el comando: el binario vive en `C:\\Users\\USER\\AppData\\Roaming\\npm\\codegraph(.cmd)` (paquete `@colbymchenry/codegraph@0.9.9`, npm global).\n\n**Comprobar que el MCP está conectado** (esto es lo que de verdad importa para que la IA lo use):\n\n```bash\nclaude mcp list\n# ...\n# codegraph: codegraph serve --mcp - ✓ Connected\n```\n\nEl servidor está registrado **global** en `~/.claude.json` (no por-proyecto), tal cual:\n\n```json\n\"codegraph\": { \"type\": \"stdio\", \"command\": \"codegraph\", \"args\": [\"serve\", \"--mcp\"] }\n```\n\nAl estar global, **funciona en cualquier repo que tenga su carpeta `.codegraph/`**. El proyecto se resuelve por el `rootUri` del cliente (el cwd de Claude Code), **no está hardcodeado** a `.ultron` — `serve --mcp` lee el proyecto del cliente, no de un `--path`.\n\n---\n\n## 2. Activar CodeGraph en un proyecto nuevo\n\nDos cosas separadas: **(a) indexar** el repo, **(b) conectar** el MCP a tu agente. El MCP global ya está conectado; normalmente solo te falta (a).\n\n| Paso | Comando (desde la raíz del repo) | Qué hace |\n|---|---|---|\n| Indexar (1ª vez) | `codegraph init` | Crea `.codegraph/` y construye el índice AST completo |\n| Re-indexar todo | `codegraph index` | Reconstruye el índice entero (full rebuild) |\n| Sync incremental | `codegraph sync` | Solo reprocesa lo cambiado desde el último index |\n| Estado | `codegraph status` | Stats + si está al día |\n| Quitar índice | `codegraph uninit` | Borra la carpeta `.codegraph/` (no toca el MCP) |\n| Lock pegado | `codegraph unlock` | Borra un lock obsoleto que bloquea indexado |\n\n```bash\n# Activar CodeGraph en, p.ej., otro repo:\ncd D:/ruta/al/proyecto\ncodegraph init        # tarda según tamaño; deja .codegraph/codegraph.db\ncodegraph status      # confirma Files/Nodes/Edges\n```\n\n**Conectar / re-conectar el MCP a un agente** (solo si reinstalas Claude Code o añades Cursor/Codex):\n\n```bash\ncodegraph install --target claude --location global --yes\n# ver el snippet sin escribir nada en disco:\ncodegraph install --print-config claude\n# quitarlo de un agente (no toca los índices .codegraph/):\ncodegraph uninstall --target claude\n```\n\n`--target` acepta `auto`, `all`, `none`, o una lista CSV de: `claude`, `cursor`, `codex`, `opencode`, `hermes`, `gemini`, `antigravity`, `kiro`. `--yes` lo deja en `--location=global --target=auto` y auto-permite las tools.\n\n### Qué NO se commitea\n\n`.codegraph/` es **local a cada máquina** y está en `.gitignore` del repo (línea 22: `.codegraph/`). Contiene `codegraph.db` + `-wal`/`-shm` + `daemon.log` + `daemon.pid` (y un `.gitignore` interno propio que ignora `*.db`, `*.log`, `cache/`, `.dirty`). Si clonas ULTRON en otra máquina tienes que `codegraph init` allí de nuevo — el índice **no viaja por git** (correcto: es un artefacto derivado, 22 MB).\n\n---\n\n## 3. Las herramientas que usa el agente (MCP)\n\nCuando la IA tiene el MCP conectado, dispone de **8 tools** (nombres reales del paquete v0.9.9). El agente las llama solo; tú las invocas indirectamente por cómo prompteas. En Claude Code aparecen con prefijo `mcp__codegraph__`:\n\n| Tool MCP | CLI equivalente | Para qué | Pregunta típica que la dispara |\n|---|---|---|---|\n| `codegraph_explore` | *(sin CLI directa)* | **PRIMARIA.** Responde casi todo en **una** llamada: \"cómo funciona X\", un flujo (\"cómo X llega a Y\"), o explorar un área — devuelve el código fuente de los símbolos relevantes agrupado por fichero + mapa de relaciones + blast radius | \"¿Cómo funciona el AI Router?\" |\n| `codegraph_search` | `codegraph query <s>` | Solo **localizar** un símbolo por nombre/fuzzy | \"¿Dónde está definido `route`?\" |\n| `codegraph_callers` | `codegraph callers <s>` | Quién **llama** a este símbolo | \"¿Quién usa `MemoryService::get`?\" |\n| `codegraph_callees` | `codegraph callees <s>` | Qué llama **este** símbolo (hacia abajo) | \"¿De qué depende `route`?\" |\n| `codegraph_impact` | `codegraph impact <s>` | Qué se **rompe** si lo cambio (blast radius) | \"Si cambio la firma de `route`, ¿qué toco?\" |\n| `codegraph_node` | *(sin CLI directa)* | Detalle + código fuente completo de **un** símbolo (devuelve todas las sobrecargas si el nombre es ambiguo) | \"Dame el cuerpo entero de `route`\" |\n| `codegraph_status` | `codegraph status` | Estado del índice + sección `Pending sync:` si va atrasado | (sanity check) |\n| `codegraph_files` | `codegraph files` | Árbol del proyecto desde el índice (más rápido que escanear el FS) | mapa rápido sin `ls -R` |\n\n> **Importante:** la herramienta de la que tira el agente para casi todo es `codegraph_explore`, no `search`. `explore` resuelve la pregunta de una y \"colapsa\" implementaciones intercambiables a firmas, así que la respuesta va dimensionada a la *respuesta*, no al tamaño del fichero. Reserva `search`/`callers`/`callees` para navegación puntual del grafo de llamadas.\n\n**`affected` es solo CLI** (no hay tool MCP): `codegraph affected <ficheros...>` te dice qué tests cubren esos ficheros. Úsalo tú en terminal.\n\n### Ejemplos REALES contra este repo (copiables, verificados hoy)\n\n```bash\ncodegraph query \"route\" -l 5\n# function route       (11248%)  control-center/src-tauri/src/ai_router.rs:2138\n#   (zone_id: &str, prompt: &str) -> Result<String, String>\n# function router_dir  (5750%)   ai_router.rs:591\n# function RouterMetrics          src/components/AIRouter/RouterMetrics.tsx:105\n```\n\n```bash\ncodegraph callers \"route\"\n# Callers of \"route\" (1):\n# function ai_router_route   control-center/src-tauri/src/ai_router.rs:2594\n```\n\n```bash\ncodegraph impact \"route\" -d 1\n# Impact of changing \"route\" — 3 affected symbols:\n#   function route:2138 · function ai_router_route:2594 · file ai_router.rs:1\n```\n\n```bash\ncodegraph callees \"route\"\n# Callees of \"route\" (10): load_zones · disabled_providers_set · load_providers ·\n#   load_metrics · free_tier_daily_limit · get (memory/service.rs:417) ...\n```\n\nFíjate en el último: el grafo conecta `route` (en `ai_router.rs`) con `get` (en `memory/service.rs`) **cruzando módulos** — justo lo que la IA no ve leyendo un solo fichero.\n\n**Flags reales por subcomando** (verificados con `--help`, NO son universales):\n\n| Flag | Disponible en | Significado |\n|---|---|---|\n| `-l, --limit N` | `query` (def. 10), `callers` (def. 20) | Máx. resultados |\n| `-k, --kind <kind>` | `query` | Filtrar por tipo (`function`, `struct`, `class`, `method`, `enum`, `interface`...) |\n| `-d, --depth N` | `impact` (def. 2), `affected` (def. 5) | Profundidad de traversal. **`query`/`callers`/`callees` NO tienen depth** |\n| `-j, --json` | todos | Salida JSON para scripting |\n| `-p, --path <path>` | todos | Apuntar a otro repo sin hacer `cd` |\n| `-f, --filter <glob>`, `--stdin`, `-q, --quiet` | `affected` | Glob de tests / leer ficheros de stdin / solo rutas |\n\n---\n\n## 4. Cuándo re-indexar\n\nCodeGraph corre un **daemon con file watcher** (mira `.codegraph/daemon.log` + `daemon.pid`) que hace `sync` automático al guardar. En `daemon.log` verás en vivo cosas como `Auto-synced 1 file(s) in 178ms`. En la práctica casi nunca tocas el índice a mano. Re-indexa manualmente cuando:\n\n| Situación | Comando |\n|---|---|\n| `status` dice \"out of date\" / sección `Pending sync:` tras un cambio grande | `codegraph sync` |\n| `git checkout`/`merge`/`rebase` movió muchos ficheros de golpe | `codegraph sync` |\n| Resultados raros o símbolos viejos que ya borraste | `codegraph index` (full rebuild) |\n| Lock pegado bloqueando indexado | `codegraph unlock` |\n| Watcher lento (p.ej. WSL2 `/mnt`) y quieres apagarlo | `codegraph serve --mcp --no-watch` y sincroniza tú con `codegraph sync` |\n\n> **Disciplina ULTRON:** el **índice** se refresca al guardar el `.rs`, pero eso es **independiente del binario**. Un `codegraph sync` verde **no** significa que el `.exe` esté recompilado — siguen siendo dos cosas (recuerda: el `.exe` no relinka si ULTRON está abierto). El grafo te dice la *estructura*; el rebuild te da el *comportamiento*.\n\n---\n\n## 5. Cómo PEDIRLE a la IA que use el grafo (lo importante)\n\nQue el MCP esté conectado **no garantiza** que la IA lo use; por defecto muchos modelos tiran de `Read`/`Grep`. **Nómbralo en el prompt.** Patrones que funcionan:\n\n- **Forzar exploración por grafo en vez de leer:**\n  > \"Antes de tocar nada, usa CodeGraph (`codegraph_explore`) para entender `route` y quién lo llama. No leas ficheros enteros hasta tener el mapa.\"\n\n- **Onboarding rápido a un módulo (la jugada estrella de `explore`):**\n  > \"Quiero entender el AI Router. Llama a `codegraph_explore` sobre `route` y resúmeme el flujo (primary→fallback, dónde lee keys, dónde toca memoria) sin volcarme los ficheros.\"\n\n- **Análisis de impacto antes de refactor:**\n  > \"Voy a cambiar la firma de `MemoryService::get`. Corre `codegraph_impact` sobre ese símbolo y dime el blast radius **antes** de editar.\"\n\n- **Qué tests correr (solo CLI):**\n  > \"He tocado `ai_router.rs`. Corre en terminal `codegraph affected control-center/src-tauri/src/ai_router.rs` y dime qué tests cubren ese fichero.\"\n  > En este repo concreto devuelve **\"No test files affected by the changed files.\"** — señal honesta de que `ai_router.rs` no tiene cobertura de tests asociada (dato útil en sí mismo).\n\n**Regla mental:** grafo para *estructura* (quién/dónde/qué-rompe), `Read` para *leer la lógica concreta* de las 2-3 funciones que el grafo te señaló. `codegraph_explore` ya te devuelve el cuerpo de los símbolos relevantes, así que muchas veces ni necesitas `Read`. El grafo te ahorra el 80% de lecturas exploratorias.\n\n### Encaja con el resto del sistema\n\nCodeGraph es **complementario** a la memoria de ULTRON, no la sustituye:\n\n- **CodeGraph** = estructura *actual y precisa* del código (efímera, se regenera con `init`/watcher).\n- **`brain.db` + Qdrant** = *por qué* se hizo algo, decisiones, ubicación histórica de funciones (recall híbrido FTS5 + E5 vía `ultron-memory recall`). **Persiste** entre máquinas y sesiones.\n\nFlujo típico de un cambio: `codegraph_impact` para el alcance → recall de memoria para el contexto/decisiones previas → editar → **rebuild** (cerrar ULTRON) → verificar (`cargo`/`tsc`/`eval`/`doctor`). Si delegas a un agente (`rust-engineer`, `refactoring-specialist`), **dile explícito que arranque por el grafo**; si no, gastará turnos releyendo ficheros.\n\n---\n\n## 6. Troubleshooting rápido\n\n| Síntoma | Causa probable | Fix |\n|---|---|---|\n| `claude mcp list` → codegraph no conectado | binario fuera de PATH o registro borrado | `codegraph install --target claude --yes` y reiniciar Claude Code |\n| `query`/`explore` no encuentra un símbolo que existe | índice viejo | `codegraph sync` o `codegraph index` |\n| \"lock file\" / indexado colgado | daemon murió sucio | `codegraph unlock` |\n| Resultados de otro repo | `rootUri` mal resuelto | abre Claude Code con cwd en el repo correcto, o pasa `-p <path>` en CLI |\n| Watcher consume CPU en disco lento | filesystem montado (WSL2 `/mnt`) | `serve --mcp --no-watch` + `sync` manual |\n| El agente lee ficheros en vez de usar el grafo | no se lo pediste | nómbralo en el prompt (`usa codegraph_explore primero`) |\n\n**Verificación de cordura en una línea:** `codegraph status` debe mostrar `Files` ≈ número de ficheros de código del repo (aquí **347** para ULTRON) y `[OK] Index is up to date`. Si los números no cuadran con la realidad, el índice mintió: re-indexa."
  }
];
