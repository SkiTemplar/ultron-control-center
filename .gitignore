# ULTRON ~/.ultron/ — operational state + tooling
# Public release v15.2 — personal data must NEVER enter public history
# Initialized 2026-05-10 (v14.9 STRUCTURE), hardened 2026-05-16 (v15.2 pre-publish)

# === Python / build ===
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.uv-cache-rescue/

# === Ephemeral session state ===
.tmp/
.tmp.driveupload/
sessions/
multimodel/
quarantine/
skill_cache/

# === Brain index (rebuildable) ===
brain_index/*.db
brain_index/*.db-journal
brain_index/*.db-wal
brain_index/*.db-shm

# === Backups (may leak secrets/snapshots) ===
backups/

# === Logs ===
logs/
*.log

# === OS / editor noise ===
*.swp
*.bak
.DS_Store
Thumbs.db

# === Lockfiles ===
*.lock
!uv.lock

# === Audit / telemetry / metrics (user-specific) ===
audits/
telemetry/
metrics/
integrity/settings.snapshots.jsonl

# === Bus runtime data ===
bus/

# === Skill vault (only INDEX + keep-list tracked) ===
skill-vault/*
!skill-vault/INDEX.json
!skill-vault/keep-list.txt
skills/registry.json

# === Qdrant ===
qdrant-native/qdrant.exe
qdrant-native/.qdrant-initialized
qdrant-native/config/config
qdrant-native/config/development
qdrant-native/config/production.yaml
qdrant_storage/

# === Personal data (NEVER commit) ===
# Anclado a top-level + vault — NO bloquear código UI con nombre `personal/`
# (ej. control-center/src/components/personal/ es código TypeScript de la app)
/personal/
/personal/**
/control-center/personal/
/control-center/personal/**
!/control-center/src/**/personal/
!/control-center/src/**/personal/**
MEMORY.md
SYSTEM-MAP.md
alerts.jsonl
alerts.jsonl.archive
alerts.jsonl.lock
inbox.jsonl
background_tasks.json
agent_manifest.json
manifest.cache.json

# === Cockpit runtime (user-specific) ===
cockpit/projects.json
cockpit/projects-exclusions.json
cockpit/bridge-index.json
cockpit/features.json
cockpit/features.example.json
cockpit/apps.json
cockpit/scan-roots.json
cockpit/news_history.db
cockpit/news_history.db-journal
cockpit/news-topics.json
cockpit/mcp-audit.jsonl
cockpit/auto_updater.jsonl
cockpit/pending_actions.json
cockpit/backup-config.json
# AI Router runtime telemetry (per-install counters; mutates on every run).
cockpit/ai-router/metrics.json
cockpit/news/
cockpit/audits/
plans/PLANS.json
plans/_archive/

# === Historical context (may leak personal info) ===
archive/

# === Tauri build output ===
control-center/dist/
control-center/src-tauri/target/
control-center/node_modules/
node_modules/
dist/
target/

# === Playwright cache ===
.playwright-mcp/

# === Legacy ===
_legacy/

# === Cover images (not part of release artifacts) ===
cover*.png
v*-cover*.png
v*-s*.png
v*-demo*.png
v*-waiting*.png
v*-layerstack.png

# === v15.2.1 PUBLIC TRIM (2026-05-16) — keep personal flavor local ===
# Personal knowledge / study notes
knowledge/

# Internal roadmap / plans / decisions
plans/
roadmap/
GENESIS-RELEASE.md

# Personal projects (keep template only)
projects/*
!projects/templates/
!projects/templates/**

# Cockpit personal/operational state
cockpit/standup/
cockpit/trending/
cockpit/last-run/
cockpit/changelog.ndjson
cockpit/changelog_table.md
cockpit/cleanup-report-*.md
cockpit/deadlines.json
cockpit/DASHBOARD.md
cockpit/ide-mappings.json
cockpit/project-hotkeys.json
cockpit/schedule-config.json
cockpit/skill_graph.json
cockpit/tui/

# Skill state caches (rebuildable, personal inventory)
skill_manifest.json
skill-provenance.json
skills.manifest.yaml
skills-registry.json

# Internal release-process docs
docs/personas-release-decision.md
docs/RELEASE-CHECKLIST-v*.md
docs/REPO-SPLIT-PLAN.md
docs/version-touchpoints.md

# Internal instructions (personal flavor)
instructions/

# Personal telemetry baselines
metrics/

# Personal IDE workspace
*.code-workspace

# Legacy
_legacy/

# Config with personal references
config/constitution.json
config/skill-trust.yaml

# === Defensive: never commit backup or temp files ===
*.bak
*.bak.*
*.tmp
*~
*.lock.tmp

# === v15.2.12: web landing + download.html (personal/desactualizado) ===
# Solo el web/ de RAIZ (landing viejo). docs/web/ (web del backend) SI se versiona.
/web/
docs/download.html

# === v15.2.18: deprecated cockpit/ docs + icons (Control Center has its own) ===
cockpit/README.md
cockpit/TUTORIAL.md
cockpit/icons/

# === v15.2.18: docs sin uso ===
docs/brain-index-chunks.md
docs/memory-layers.md

# === v2.0.0: superpowers research/walkthrough logs (ephemeral) ===
docs/superpowers/research/

# === v15.2.18: scripts/cockpit/ one-shot legacy ===
scripts/cockpit/audit_plans.py
scripts/cockpit/audit_plans_pass2.py
scripts/cockpit/audit_plans_pass3.py
scripts/cockpit/plans_master_v15_2.py
# v15.5.13: version_propagate.py rewritten as the SSOT drift guard wired into CI
# (see .github/workflows/ci.yml § version-drift). MUST be tracked so CI can run it.
# scripts/cockpit/version_propagate.py  ← intentionally not ignored as of v15.5.13
scripts/cockpit/changelog_backfill.py
scripts/cockpit/test_cockpit.py
scripts/cockpit/_categorize_skills.py

# === v15.2.18: root legacy duplicates ===
scripts/consistency-check.py
scripts/register_wake_triggers.ps1
cockpit/audits/

# review / subagent audit artifacts (transient JSON / TXT dumps)
control-center/release_runs.json
control-center/release_yml.txt
control-center/releases.json
control-center/run*.json
control-center/tags.json
control-center/workflows.json

# v2.4: archived vault dirs
skill-vault-archived-*/

# v2.11.2: fastembed ONNX model cache (auto-downloaded, ~22MB)
control-center/.fastembed_cache/
.fastembed_cache/

# Secrets — local env (nunca commitear). Anadido 2026-05-30.
.env
.env.local
.env.*.local
control-center/.env.local
!.env.example

# === 2026-05-30: runtime per-maquina + sidecar + telemetria personal ===
# (auditoria de cierre de sesion. El repo se publica: "personal data must
#  NEVER enter public history". .git ya pesa ~151M por el sidecar binario.)
# Sidecar compilado regenerable via scripts/install-sidecars.ps1
bin/ultron-embed.exe
# Estado runtime trackeado por arrastre (des-trackeado con git rm --cached)
cockpit/quota-state.json
cockpit/plugin-update-cache.json
cockpit/projects/*/terminal-layout.json
cockpit/projects/*/work-sessions.jsonl
cockpit/workdays/_pending-links.jsonl
# Estado runtime nuevo, per-instalacion (lo reescribe la app en cada arranque)
cockpit/meta.json
cockpit/workdays/wd-*.json
cockpit/_pending-*.json
# Config local opcional de Mem0 (bank/finanzas ya opted-out en mem0-sync.js)
.mem0-opt-out.json
# Telemetria de maquina (hostname/CPU/disco) y backups zip.
# Los .md de diagnostico de arquitectura SI se conservan (docs).
cockpit/diagnostics/*.json
cockpit/diagnostics/*.zip

# Pending de decisions auto-capturadas (runtime, drenado por el backend) + sidecar de drain
cockpit/projects/*/decisions-pending.jsonl
cockpit/projects/*/decisions-pending.draining
# campana fullize: sidecar proxy vendor + backups kanban
/proxy/
.tmp/kanban-backup-*.json

# === 2026-06-04: higiene fullize — runtime de memoria + colas + sidecars ===
# (el repo se publica; nada de DBs/binarios runtime ni backups en el historial)
# brain.db = DB canonica de memoria (runtime, per-maquina). NUNCA versionar.
brain.db
brain.db-journal
brain.db-wal
brain.db-shm
brain.db.bak-*
# Sidecar ultron-memory compilado (regenerable: cargo build / install-sidecars)
bin/ultron-memory.exe
bin/*.exe
# Deploy backups del sidecar (regenerables; creados al desplegar a ~/.ultron/bin)
bin/*.bak-*
# Cola de Run Batch (runtime per-instalacion)
batches/queue.jsonl
batches/queue-pending.jsonl
batches/queued-*.ps1
# Estado runtime del proxy/router/tabs/workflows (lo reescribe la app)
cockpit/proxy-state.json
cockpit/open-tabs.json
cockpit/workflow-runs.db
cockpit/workflow-runs.db-journal
cockpit/workflow-runs.db-wal
cockpit/workflow-runs.db-shm

# ============================================================================
# === MERGE 2026-06-04 (desde .gitignore.proposed / CLEANUP-PLAN) — solo AÑADE
# ============================================================================

# --- HALLAZGO CRITICO: ficheros de validacion one-shot de IAs externas ---
# (Codex/Gemini review dumps). Hoy UNTRACKED pero NO ignorados -> un `git add -A`
# los subiria. Pueden contener fragmentos de codigo/secretos del paquete enviado.
*-validation.txt
.codex-validation.txt
.gemini-validation.txt
.validation-package.md
cockpit/memory-rework/.validation-package.md

# --- Dirs basura por ruta mal-expandida (script que no resolvio ~/.ultron) ---
# Aparecen como carpetas literales "C:Users<user>.ultron.tmpevals*" en la raiz.
/C:Users*

# --- Endurecimiento de claves/secretos (defensivo, repo se publica) ---
*.key
*.pem
*.p12
*.pfx
*.keystore
id_rsa
id_ed25519
secrets/
**/secrets.*
.credentials.json
**/.credentials.json
.mem0-opt-out.json

# --- brain.db tambien fuera de la raiz por si se mueve el runtime ---
**/brain.db
**/brain.db-journal
**/brain.db-wal
**/brain.db-shm
**/brain.db.bak-*

# --- Caches uv (todas las variantes), fastembed y modelos descargables ---
.uv-cache*
.uv-cache-rescue/
**/.fastembed_cache/
*.onnx
*.gguf
*.safetensors

# --- Reafirmar dist/ y target/ en cualquier nivel ---
**/dist/
**/target/

# --- Backups y temporales (reafirmado, defensivo) ---
*.bak
*.bak.*
*.bak-*
*.tmp
*~
*.old
*.orig

# --- Config per-maquina opcional (template paths.example.toml SI se versiona) ---
config/paths.toml
config/constitution.json
config/skill-trust.yaml
.claude/

# Cuarentena de limpieza (no versionar)
_cleanup_quarantine_*/

# === 2026-06-05: agujeros detectados por auditor Kirkardo ===
# Datos personales-financieros (audit HIGH: repo PUBLICO, no deben entrar al historial)
cockpit/project-cards/bank.txt
cockpit/project-cards/
# Kanban personal con tasks/hallazgos internos de seguridad
cockpit/projects/*/kanban.json
# qdrant-native: almacen local stale con binario qdrant.exe (85M, huerfano)
qdrant-native/

# === finance feature: local-only files (KutxaBank personal data path hardcoded) ===
# Build with `cargo build --features finance` + `VITE_FINANCE=1 npm run build` to enable.
control-center/src-tauri/src/finance.rs
control-center/src/components/Finance.tsx

# === 2026-06-06: sanitizacion ola 5-pilares (datos personales colados al tracking) ===
# Catalogo de skills auto-generado (incluye personas personales). Rebuild local: skill_catalog_gen.py
cockpit/skill-catalog.json
# Notas y proyectos personales (config de agentes preferidos, decisiones, notas)
cockpit/notes/
cockpit/projects/*/notes.md
cockpit/projects/*/decisions.jsonl
cockpit/projects/*/pinned-agents.json
cockpit/projects/*/agent-roster.json
# NOTE: scripts/cockpit/audit_personal_data.py is intentionally TRACKED — the CI
# `personal-info-leak` gate runs it on every push, so it must ship in the repo.
# It carries no real PII: the personal email/handle are loaded at runtime from
# the ULTRON_PII_EXTRA env var, never hardcoded. The detector allow-lists itself.

# === Internal process artifacts (diagnostics, rework specs, evals) ===
# Working notes / session audits / design specs — NOT part of the installable
# product. Kept on disk locally but excluded from the public repo so it stays
# clean of personal paths and references. (Kirkardo R4 P5 — public-repo hygiene.)
cockpit/diagnostics/
cockpit/memory-rework/
docs/superpowers/specs/
scripts/cockpit/PROPOSED-hook-wiring.md
control-center/docs/free-claude-code-port-plan-*.md
control-center/docs/kirkardo-*.md
control-center/docs/memory-verification-*.md
control-center/docs/projects-redesign-wireframe-*.md
control-center/docs/regression-check-*.md
control-center/docs/v2.5-MCP-AUDIT.md
control-center/docs/workdays-rework-spec-*.md
control-center/docs/workflows-best-practices-*.md
