# ULTRON v14.7 BACKUP-WATCH — exclusion list (gitignore-style)
#
# Format: one pattern per line.
#   * Lines starting with #  = comment
#   * `dir/` (trailing slash) = directory exclusion (robocopy /XD)
#   * `*.ext`                 = file exclusion (robocopy /XF)
#   * `name`                  = both (defensive)
#
# Patterns are split into /XD (dir names) and /XF (file globs) by
# weekly-backup.ps1. Anything that doesn't fit either bucket is treated as a
# directory name.

# === Unreal Engine 5 ===
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
*.pdb
*.exe

# === Unity ===
Library/
Temp/
Logs/
Builds/
obj/
*.csproj
*.sln

# === Web / Node ===
node_modules/
.next/
.nuxt/
dist/
build/
.turbo/
.vercel/
.svelte-kit/

# === Android / Gradle ===
.gradle/
local.properties
*.apk
*.aab

# === Python ===
__pycache__/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.uv-cache/
.uv-cache-rescue/
*.pyc
*.pyo
*.egg-info/

# === Secrets / credentials (NUNCA al mirror — re-obtenibles via re-login) ===
# card-sec-backup-secrets-exclusion (Semana 0). El robocopy /MIR copiaba
# .claude/.credentials.json (token OAuth de Claude Code) en claro a D:\BACKUP.
# Un mirror plano no debe contener credenciales: si se restaura, Claude Code
# pide login otra vez (coste trivial) y el token vivo nunca queda duplicado.
.credentials.json
.runner-secret
*.pem
*.key
*.secret
# .env family — fuga inminente del MEM0_API_KEY al activar Mem0 (security-auditor 2026-05-29 HIGH-1)
.env
.env.local
*.env
.envrc
# settings.local.json puede llevar tokens de plugins; *.credentials por si acaso
settings.local.json
*.credentials
# keystores / claves privadas con extension
*.pfx
*.p12
*.jks
*.keystore
*.ppk
# netrc defensivo (cae en /XF por tener punto). OJO: archivos de clave SSH/AWS
# sin extension (id_rsa, AWS 'credentials') NO se pueden excluir con este parser
# gitignore-style (los manda a /XD = directorios). Pendiente parser fix si se
# anade .ssh/.aws a backup-config.json -> ver card-sec-backup-followup.
.netrc

# === Rust / Cargo ===
target/

# === Generic build / artifacts ===
out/
.cache/
.parcel-cache/
coverage/
.nyc_output/

# === ULTRON internals (regenerable / re-installable) ===
.ultron/.tmp/
.ultron/archive/skill_cache/
.ultron/archive/v6.x-legacy/
.ultron/quarantine/
.ultron/.uv-cache-rescue/
.claude/plugins/cache/
.claude/file-history/
.claude/projects/
.claude/sessions/
.claude/session-env/
.claude/skills/ultron/.venv/

# === UE5 cache & temp extras ===
Saved/Autosaves/
Saved/Logs/
Plugins/.uplugin.cache/
*.target

# === Generic temp / locks ===
*.tmp
*.temp
*.lock
*.lck
*.log

# === IDE / OS noise ===
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini

# === Large binaries that don't compress and aren't auth-of-truth ===
*.zip
*.7z
*.rar
*.iso
*.dmg
*.bak
*.swp
